/* ═══════════════════════════════════════════════════════════════
   selenium.js — Selenium Fundamentals
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['selenium-fundamentals'] = {
  title: '🔧 Selenium Fundamentals',
  description: 'Selenium WebDriver architecture, browser commands, and core concepts',
  questions: [
    {id:'SE001',category:'Selenium',topic:'Architecture',subtopic:'WebDriver Architecture',question:'Explain Selenium WebDriver Architecture.',whyAsked:'This is a must-know question — shows you understand HOW Selenium works, not just how to use it.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'Selenium WebDriver communicates with the browser through browser-specific drivers using the W3C WebDriver protocol. It sends HTTP commands to the driver, which translates them into browser actions.',interviewAnswer:'Selenium WebDriver has a client-server architecture with four components: 1) **Selenium Client Libraries** — the Java/Python/etc. API we write our tests in. 2) **JSON Wire Protocol / W3C WebDriver Protocol** — the communication protocol between client and driver. 3) **Browser Drivers** — ChromeDriver, GeckoDriver, etc. — they receive commands from the client and translate them into browser-specific calls. 4) **Browser** — the actual browser where actions are executed. When I write `driver.findElement(By.id("login"))`, the client library sends an HTTP POST request to ChromeDriver with the locator details. ChromeDriver communicates with Chrome using Chrome DevTools Protocol (CDP) to find the element and returns the result. This architecture is important because it explains common issues — like version mismatches between ChromeDriver and Chrome causing SessionNotCreatedException.',detailedExplanation:'Flow: Test Code → Client Library → HTTP Request → Browser Driver (server on localhost) → Browser. The driver acts as a bridge. Since Selenium 4, W3C WebDriver Protocol replaced the older JSON Wire Protocol, providing better standardization.',simpleExplanation:'Your test code talks to a translator (browser driver), who tells the browser what to do. Like using a translator to communicate in a foreign country.',realWorldExample:'When you write driver.get("https://example.com"), the Java client sends an HTTP POST to ChromeDriver at localhost:9515, ChromeDriver tells Chrome to navigate, and Chrome loads the page.',projectExample:'Understanding this architecture helped me debug an issue where our Jenkins CI tests failed because the ChromeDriver version was 2 versions behind the installed Chrome — the W3C protocol had breaking changes.',codeCommand:'// Basic Selenium WebDriver setup in Java\nWebDriverManager.chromedriver().setup();\nChromeOptions options = new ChromeOptions();\noptions.addArguments("--headless", "--no-sandbox");\nWebDriver driver = new ChromeDriver(options);\ndriver.get("https://example.com");\nSystem.out.println(driver.getTitle());\ndriver.quit();',expectedOutput:'Page title is printed to console',followUpQ:'What is the difference between Selenium 3 and Selenium 4?',followUpA:'Key differences: 1) Selenium 4 uses W3C WebDriver Protocol natively (Selenium 3 used JSON Wire Protocol), 2) Relative locators (above, below, near, toLeftOf, toRightOf), 3) New Window/Tab API — driver.switchTo().newWindow(WindowType.TAB), 4) Chrome DevTools Protocol (CDP) support, 5) Improved Selenium Grid with better architecture, 6) Better IDE support. In my projects, I migrated from Selenium 3 to 4 and the main breaking change was DesiredCapabilities being replaced by browser-specific Options classes.',seniorFollowUp:'Explain how Selenium Grid works in your project.',seniorAnswer:'We use Selenium Grid for parallel cross-browser testing. The Grid has a Hub (central node) and multiple Nodes (machines with different browsers). Our TestNG suite is configured for parallel execution — thread-count="5" in testng.xml. The Hub distributes tests to available Nodes based on requested capabilities. In Jenkins, we have a Docker-based Grid that spins up Chrome and Firefox containers on demand. This reduced our regression execution time from 4 hours (sequential) to 45 minutes (parallel across 5 nodes).',commonMistake:'Not understanding the architecture and just saying "Selenium automates browsers."',bestPractice:'Draw the architecture diagram during the interview — show Client → Protocol → Driver → Browser. It demonstrates deep understanding.'},
    {id:'SE002',category:'Selenium',topic:'Commands',subtopic:'Browser Commands',question:'What is the difference between get() and navigate().to()?',whyAsked:'Classic Selenium interview question — tests attention to detail.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'get() waits for the page to fully load before proceeding. navigate().to() also loads the page but additionally provides back(), forward(), and refresh() methods for browser navigation.',interviewAnswer:'Both `driver.get(url)` and `driver.navigate().to(url)` load a URL, but they have subtle differences: `get()` is a direct command that waits for the page load to complete (waits for document.readyState to be "complete"). `navigate().to()` does the same but is part of the Navigation interface that also provides `back()`, `forward()`, and `refresh()`. In practice, `get()` doesn\'t maintain browser history for the first page, while `navigate().to()` treats it as a navigation event. I typically use `get()` for the initial page load and `navigate()` methods when I need to simulate browser navigation — like going back to the previous page. However, in modern Selenium, the difference is minimal for most use cases.',detailedExplanation:'Navigation interface methods: navigate().to(url) — load URL, navigate().back() — browser back, navigate().forward() — browser forward, navigate().refresh() — reload page. get() is shorthand for navigate().to() in most implementations.',simpleExplanation:'get() = type a URL and press Enter. navigate().to() = same but also gives you back/forward buttons.',realWorldExample:'Testing a checkout flow: use get() for initial page, navigate().back() to test going back to cart, navigate().forward() to return to checkout.',projectExample:'In my framework, the Base class uses get() for initial URL loading, and individual tests use navigate().back()/forward() for testing browser navigation scenarios.',codeCommand:'// get() - loads URL, waits for page load\ndriver.get("https://example.com");\n\n// navigate() - full navigation interface\ndriver.navigate().to("https://example.com/page2");\ndriver.navigate().back();    // go back\ndriver.navigate().forward(); // go forward\ndriver.navigate().refresh(); // reload page',expectedOutput:'Page navigates accordingly',followUpQ:'What is the difference between close() and quit()?',followUpA:'close() closes the current browser window/tab only — if you have multiple windows, others remain open and the WebDriver session continues. quit() closes ALL browser windows AND ends the WebDriver session (kills the driver process). Always use quit() in your @AfterMethod or teardown to prevent memory leaks from orphaned browser processes. In my framework, I use quit() in the AfterSuite and close() only when testing multi-window scenarios.',seniorFollowUp:'What happens if you forget to call quit()?',seniorAnswer:'Forgetting quit() causes resource leaks — the ChromeDriver process stays alive, consuming memory. In CI environments, this can quickly exhaust server resources. I\'ve seen Jenkins servers crash because hundreds of orphaned ChromeDriver processes accumulated. Our framework handles this with: 1) quit() in @AfterMethod with try-catch, 2) A cleanup script in Jenkins that kills stale driver processes before each run, 3) A shutdown hook in the Base class as a safety net.',commonMistake:'Using close() in teardown instead of quit(), causing driver process leaks.',bestPractice:'Always use quit() in teardown. Use close() only for multi-window scenarios.'},
    {id:'SE003',category:'Selenium',topic:'Commands',subtopic:'findElement vs findElements',question:'What is the difference between findElement() and findElements()?',whyAsked:'Tests understanding of element finding strategies and error handling.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'findElement() returns the first matching WebElement and throws NoSuchElementException if not found. findElements() returns a List of all matching elements and returns an empty list if none found.',interviewAnswer:'This is an important distinction I use frequently. `findElement()` returns a single WebElement — the first match. If no element is found, it throws NoSuchElementException. `findElements()` returns a List<WebElement> of ALL matches. If no elements are found, it returns an empty list — no exception. I use findElements() in several practical scenarios: 1) Checking if an element exists without throwing an exception — `driver.findElements(By.id("popup")).size() > 0`, 2) Working with lists — iterating through search results, table rows, or menu items, 3) Counting elements — verifying the number of items in a list. In my framework, I have a utility method `isElementPresent()` that uses findElements() to check existence without try-catch blocks.',detailedExplanation:'findElement: Returns WebElement, throws NoSuchElementException, respects implicit wait. findElements: Returns List<WebElement>, returns empty list, respects implicit wait (waits for at least one match or timeout).',simpleExplanation:'findElement = find me THE button (error if not found). findElements = find me ALL buttons (empty list if not found).',realWorldExample:'Testing a search results page: findElement for the search box, findElements for all result items to count and iterate.',projectExample:'In my e-commerce automation, I use findElements() to get all product cards on a category page, iterate through them, and validate each product\'s name, price, and availability.',codeCommand:'// findElement - single element\nWebElement loginBtn = driver.findElement(By.id("login"));\n\n// findElements - multiple elements\nList<WebElement> items = driver.findElements(By.className("product-card"));\nSystem.out.println("Found " + items.size() + " products");\n\n// Check element existence without exception\nboolean exists = driver.findElements(By.id("popup")).size() > 0;\nif (exists) {\n    driver.findElement(By.id("popup-close")).click();\n}',expectedOutput:'Element found or list of elements returned',followUpQ:'What happens if findElement() doesn\'t find the element?',followUpA:'It waits for the implicit wait duration (if set) and then throws NoSuchElementException. If explicit wait is used instead, it throws TimeoutException. The exception message includes the locator used, which helps debugging. In my framework, I wrap findElement() with explicit waits to get better control over timeout behavior.',seniorFollowUp:'How do you handle dynamic lists in your framework?',seniorAnswer:'For dynamic lists (like search results that load via AJAX), I: 1) Wait for the list container to be present, 2) Wait for list items to appear using explicit wait, 3) Use findElements() to get all items, 4) Iterate with proper waits for each item\'s sub-elements. I also handle the case where the list might re-render during iteration (causing StaleElementReferenceException) by re-fetching elements when needed.',commonMistake:'Using try-catch with findElement() to check existence instead of using findElements().',bestPractice:'Use findElements().size() > 0 for existence checks. Use findElement() when you expect the element to be there.'},
    {id:'SE004',category:'Selenium',topic:'Setup',subtopic:'WebDriverManager',question:'How do you manage browser drivers in your framework?',whyAsked:'Shows you understand the practical setup challenges of Selenium.',difficulty:2,importance:'important',interviewType:'Technical',thirtySecAnswer:'I use WebDriverManager library to automatically download and manage browser driver binaries, eliminating manual driver management and version mismatch issues.',interviewAnswer:'In my current framework, I use WebDriverManager by Boni Garcia. It automatically downloads the correct version of ChromeDriver, GeckoDriver, or EdgeDriver based on the installed browser version. Before WebDriverManager, we manually downloaded drivers and set the system property — `System.setProperty("webdriver.chrome.driver", path)` — which was error-prone, especially when Chrome auto-updated. WebDriverManager solves this: `WebDriverManager.chromedriver().setup()` detects Chrome version, downloads the matching driver, and configures the path. In our CI environment, this was a game-changer — we no longer had builds failing because of driver version mismatches after Chrome updates.',detailedExplanation:'WebDriverManager: 1) Detects installed browser version, 2) Downloads matching driver, 3) Caches it locally, 4) Sets system property. Supports Chrome, Firefox, Edge, Opera. Can also specify driver version explicitly: WebDriverManager.chromedriver().driverVersion("114").setup().',simpleExplanation:'WebDriverManager is like an automatic tool installer — it gets the right version of the tool you need without you searching for it.',realWorldExample:'Without WebDriverManager: Chrome updates to 115, old ChromeDriver 114 breaks tests, developer manually downloads new driver. With WebDriverManager: It auto-detects and downloads matching driver.',projectExample:'In our Jenkins pipeline, WebDriverManager resolved 90% of our "SessionNotCreatedException" errors caused by driver-browser version mismatches. We added it as a Maven dependency and it just works.',codeCommand:'// Old way (manual)\nSystem.setProperty("webdriver.chrome.driver", "/path/to/chromedriver");\nWebDriver driver = new ChromeDriver();\n\n// New way (WebDriverManager)\nWebDriverManager.chromedriver().setup();\nWebDriver driver = new ChromeDriver();\n\n// Selenium 4 built-in manager\nWebDriver driver = new ChromeDriver(); // auto-manages in Selenium 4.6+',expectedOutput:'Browser driver is automatically managed',followUpQ:'What about Selenium 4.6+ built-in driver management?',followUpA:'Selenium 4.6 introduced Selenium Manager — a built-in driver management tool that automatically downloads the correct driver. This means you no longer need WebDriverManager as a third-party dependency. Just create `new ChromeDriver()` and Selenium Manager handles the rest. However, many production frameworks still use WebDriverManager for its additional features like explicit version control and caching.',seniorFollowUp:'How do you handle driver management in Docker/CI environments?',seniorAnswer:'In Docker, we use official Selenium Docker images that come with pre-installed browsers and drivers — no driver management needed. For Jenkins without Docker, we use WebDriverManager with caching — drivers are cached in a shared directory and reused across builds. We also pin browser versions in CI to avoid unexpected updates. If there\'s a browser update, we test the suite locally first before updating the CI image.',commonMistake:'Hardcoding driver paths or committing driver binaries to source control.',bestPractice:'Use WebDriverManager or Selenium Manager. Never hardcode driver paths.'},
    {id:'SE005',category:'Selenium',topic:'Concepts',subtopic:'Headless Execution',question:'What is headless browser execution and when do you use it?',whyAsked:'Common in CI/CD — shows understanding of running tests without a GUI.',difficulty:2,importance:'important',interviewType:'Technical',thirtySecAnswer:'Headless execution runs the browser without a visible GUI, which is faster and uses less memory — ideal for CI/CD pipelines where no display is available.',interviewAnswer:'Headless execution runs Chrome, Firefox, or other browsers without rendering the UI on screen. I use it primarily in CI/CD pipelines where: 1) There\'s no display server (headless Linux servers), 2) We need faster execution — headless is typically 20-30% faster since it doesn\'t render pixels, 3) We need to save resources — especially when running parallel tests. In my framework, headless mode is controlled by a configuration property: `browser.headless=true`. The DriverFactory reads this and adds `--headless` to ChromeOptions. However, I always run the full suite with headed browsers periodically because some bugs only manifest in headed mode — like animations blocking clicks, or visual rendering issues.',detailedExplanation:'Chrome headless: options.addArguments("--headless=new"). Firefox headless: options.addArguments("-headless"). Headless may miss: visual bugs, CSS rendering issues, some JavaScript execution timing differences.',simpleExplanation:'Running tests with an invisible browser — the browser does everything normally but doesn\'t show a window. Like working with your eyes closed but still typing perfectly.',realWorldExample:'Jenkins runs 500 Selenium tests on a Linux server with no GUI. Headless mode allows this without installing a display server or virtual display (Xvfb).',projectExample:'In our Jenkins pipeline, all nightly regression runs use headless Chrome. Before merging PRs, I run smoke tests in headless mode for speed, and the full regression runs in headed mode weekly to catch visual issues.',codeCommand:'// Chrome Headless\nChromeOptions options = new ChromeOptions();\noptions.addArguments("--headless=new");\noptions.addArguments("--window-size=1920,1080");\noptions.addArguments("--no-sandbox");\noptions.addArguments("--disable-dev-shm-usage");\nWebDriver driver = new ChromeDriver(options);\n\n// Firefox Headless\nFirefoxOptions ffOptions = new FirefoxOptions();\nffOptions.addArguments("-headless");\nWebDriver driver = new GeckoDriver(ffOptions);',expectedOutput:'Browser runs without visible window, tests execute normally',followUpQ:'Are there differences between headed and headless execution?',followUpA:'Yes: 1) Some elements may have different dimensions in headless (always set --window-size), 2) Font rendering can differ slightly, 3) Some pop-ups or authentication dialogs behave differently, 4) Performance is better in headless, 5) Screenshots look different (no anti-aliasing sometimes). I\'ve seen tests pass in headless but fail in headed due to element overlap issues that only occur with actual rendering.',seniorFollowUp:'How do you debug test failures that only happen in headless mode?',seniorAnswer:'Debugging headless failures: 1) Take screenshots at failure points — essential for headless debugging, 2) Capture page source for DOM inspection, 3) Use browser console logs: LoggingPreferences to capture JavaScript errors, 4) Try running the same test headed on the CI machine using VNC or Xvfb with screenshot capture, 5) Check window size — headless defaults can differ, 6) Add explicit waits for timing issues specific to headless rendering.',commonMistake:'Running only in headless and never validating with headed browsers.',bestPractice:'Use headless for CI speed, but periodically run headed for visual validation. Always set window size in headless mode.'}
  ]
};
