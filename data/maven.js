/* ═══════════════════════════════════════════════════════════════
   maven.js — Maven Build Tool
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['maven'] = {
  title: '📋 Maven Build Tool',
  description: 'Maven lifecycle, POM structure, dependency management, and plugins for test automation',
  questions: [
    {id:'MVN001',category:'Maven',topic:'Basics',subtopic:'What is Maven',question:'What is Maven and why do you use it in your automation framework?',whyAsked:'Fundamental question — every SDET must know their build tool.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'Maven is a build automation and dependency management tool. It manages project structure, dependencies (like Selenium, TestNG), compilation, test execution, and reporting through a POM.xml file.',interviewAnswer:'Maven is a build automation tool based on the concept of Project Object Model (POM). In my automation framework, Maven serves several critical purposes: 1) **Dependency Management** — I declare all dependencies (Selenium, TestNG, RestAssured, ExtentReports) in pom.xml with version numbers, and Maven downloads them from the Central Repository automatically. 2) **Standardized Project Structure** — src/main/java for framework code, src/test/java for test classes, src/test/resources for config and test data. 3) **Build Lifecycle** — compile, test, package phases execute in order. 4) **Plugin Ecosystem** — maven-surefire-plugin runs TestNG tests, maven-compiler-plugin sets Java version. 5) **CI/CD Integration** — Jenkins simply runs `mvn clean test` to execute the full suite. Maven eliminates the "works on my machine" problem — every developer gets the same dependencies and environment.',detailedExplanation:'Maven uses Convention over Configuration. Default project layout: src/main/java (source), src/test/java (tests), pom.xml (root). Dependencies are downloaded to local repository (~/.m2/repository) and cached.',simpleExplanation:'Maven is like a project manager for your code — it fetches libraries you need, organizes your project, and runs your tests, all from one configuration file.',realWorldExample:'Without Maven, you would manually download 15+ JAR files, add them to your classpath, and ensure everyone on the team has the same versions. Maven does this with a single pom.xml.',projectExample:'In my Selenium framework, pom.xml manages 12+ dependencies including Selenium, TestNG, WebDriverManager, ExtentReports, Apache POI, and RestAssured. Jenkins runs `mvn clean test -DsuiteFile=regression.xml` to trigger the full regression suite.',codeCommand:'<!-- Basic pom.xml structure -->\n<project>\n  <groupId>com.company.automation</groupId>\n  <artifactId>selenium-framework</artifactId>\n  <version>1.0-SNAPSHOT</version>\n  \n  <dependencies>\n    <dependency>\n      <groupId>org.seleniumhq.selenium</groupId>\n      <artifactId>selenium-java</artifactId>\n      <version>4.15.0</version>\n    </dependency>\n    <dependency>\n      <groupId>org.testng</groupId>\n      <artifactId>testng</artifactId>\n      <version>7.8.0</version>\n      <scope>test</scope>\n    </dependency>\n  </dependencies>\n</project>',expectedOutput:'Project builds and dependencies are resolved',followUpQ:'What is the difference between compile and test scope?',followUpA:'compile scope: Available everywhere — compile, test, and runtime. Used for Selenium, Apache POI. test scope: Available only during test compilation and execution. Used for TestNG, JUnit. provided scope: Available at compile time but NOT packaged — the runtime environment provides it. runtime scope: Not needed for compilation but needed at runtime (e.g., JDBC drivers).',commonMistake:'Not understanding scopes — putting TestNG in compile scope wastes resources.',bestPractice:'Always specify the correct scope. Use test scope for testing libraries and compile scope for framework utilities.'},
    {id:'MVN002',category:'Maven',topic:'Lifecycle',subtopic:'Maven Lifecycle',question:'Explain Maven Build Lifecycle phases.',whyAsked:'Tests understanding of how Maven builds and runs tests.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'Maven has 3 lifecycles: default (build), clean (cleaning), and site (documentation). The default lifecycle has phases: validate → compile → test → package → verify → install → deploy, executed in order.',interviewAnswer:'Maven has three built-in lifecycles: 1) **Default Lifecycle** (most important for us): `validate` → `compile` → `test` → `package` → `verify` → `install` → `deploy`. When you run `mvn test`, Maven executes validate, compile, AND test — all phases up to and including the specified phase. 2) **Clean Lifecycle**: `pre-clean` → `clean` → `post-clean`. Removes the target/ directory. That\'s why we always run `mvn clean test` — first clean old artifacts, then build and test. 3) **Site Lifecycle**: `pre-site` → `site` → `post-site` → `site-deploy`. Generates project documentation. For test automation, the key phases are: `compile` — compiles our framework code, `test-compile` — compiles test classes, `test` — runs tests using Surefire plugin. When I run `mvn clean test` in Jenkins, it cleans the target folder, compiles everything, and runs the TestNG suite specified in the Surefire plugin configuration.',detailedExplanation:'Each phase is bound to plugins. compile → maven-compiler-plugin, test → maven-surefire-plugin, package → maven-jar-plugin. You can bind additional plugins to any phase.',simpleExplanation:'Maven lifecycle is like a recipe — steps must happen in order. You can\'t bake a cake (package) before mixing ingredients (compile).',realWorldExample:'Running `mvn package` automatically runs validate, compile, test, AND package. You don\'t need to run each step separately.',projectExample:'In Jenkins, I run `mvn clean test -DsuiteFile=smoke.xml` for PR validation (fast) and `mvn clean test -DsuiteFile=regression.xml -Dbrowser=chrome -Denv=staging` for nightly regression runs.',codeCommand:'# Clean and run tests\nmvn clean test\n\n# Skip tests during build\nmvn clean package -DskipTests\n\n# Run specific suite\nmvn clean test -DsuiteFile=regression.xml\n\n# Run with specific profile\nmvn clean test -Pstaging',expectedOutput:'Tests execute based on lifecycle phase',followUpQ:'What is the difference between -DskipTests and -Dmaven.test.skip=true?',followUpA:'-DskipTests: Compiles test classes but does NOT run them. -Dmaven.test.skip=true: Does NOT compile AND does NOT run tests. Use -DskipTests when you want to verify compilation but skip execution. Use maven.test.skip when you want to skip everything test-related.',commonMistake:'Not understanding that phases run in order — running mvn package also runs test phase.',bestPractice:'Always use mvn clean test for CI. Use -DskipTests only for local builds when testing compilation.'},
    {id:'MVN003',category:'Maven',topic:'Plugins',subtopic:'Surefire Plugin',question:'How does maven-surefire-plugin work with TestNG?',whyAsked:'Critical for understanding how tests are triggered in CI/CD.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'Maven Surefire Plugin is responsible for running tests during the test phase. It integrates with TestNG by specifying the TestNG suite XML file in the plugin configuration.',interviewAnswer:'The maven-surefire-plugin is the default test runner in Maven\'s test phase. For TestNG integration, I configure it in pom.xml to use a specific TestNG suite file. The plugin configuration includes: 1) **Suite file path** — pointing to testng.xml or a custom suite file, 2) **System properties** — browser type, environment, etc., passed via `-D` flags, 3) **Parallel execution** — thread count and parallel mode, 4) **Listeners** — for reporting and retry logic. In my framework, I use Maven profiles to switch between different suite files — smoke, regression, sanity. Jenkins passes the suite file as a parameter: `mvn clean test -DsuiteFile=${SUITE_FILE}`. The Surefire plugin picks up this property and runs the corresponding TestNG suite.',detailedExplanation:'Surefire recognizes TestNG test classes by: 1) Classes with @Test annotation, 2) Suite XML file specified in configuration. Reports are generated in target/surefire-reports/.',simpleExplanation:'Surefire is the engine that Maven uses to actually run your TestNG tests. You configure it in pom.xml to tell it which tests to run.',realWorldExample:'In Jenkins, the build step is `mvn clean test -DsuiteFile=regression.xml`. Surefire picks up this parameter and runs the regression suite.',projectExample:'My pom.xml has Surefire configured with a ${suiteFile} property that defaults to smoke.xml but can be overridden from command line for different test runs.',codeCommand:'<!-- pom.xml Surefire configuration -->\n<build>\n  <plugins>\n    <plugin>\n      <groupId>org.apache.maven.plugins</groupId>\n      <artifactId>maven-surefire-plugin</artifactId>\n      <version>3.2.2</version>\n      <configuration>\n        <suiteXmlFiles>\n          <suiteXmlFile>${suiteFile}</suiteXmlFile>\n        </suiteXmlFiles>\n        <systemPropertyVariables>\n          <browser>${browser}</browser>\n          <env>${env}</env>\n        </systemPropertyVariables>\n      </configuration>\n    </plugin>\n  </plugins>\n</build>\n\n<properties>\n  <suiteFile>testng.xml</suiteFile>\n  <browser>chrome</browser>\n  <env>qa</env>\n</properties>',expectedOutput:'TestNG suite executes via Maven',followUpQ:'How do you run tests in parallel using Maven and TestNG?',followUpA:'Two levels of parallelism: 1) TestNG level — in testng.xml: parallel="methods" thread-count="5", 2) Maven Surefire level — forkCount and reuseForks for JVM-level parallelism. I typically use TestNG parallel execution within a single JVM for most cases. For large suites, I use Surefire forkCount=2 to run in separate JVMs, preventing memory issues.',commonMistake:'Hardcoding suite file path instead of using a Maven property for flexibility.',bestPractice:'Use Maven properties with defaults for suite file, browser, and environment. This makes the framework configurable from command line.'},
    {id:'MVN004',category:'Maven',topic:'Dependencies',subtopic:'Dependency Management',question:'How do you manage dependencies and resolve conflicts in Maven?',whyAsked:'Shows practical experience with real-world dependency issues.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'Dependencies are declared in pom.xml with groupId, artifactId, and version. Conflicts are resolved using dependency exclusions, dependencyManagement section, or by analyzing the dependency tree with mvn dependency:tree.',interviewAnswer:'In my framework, dependency management involves: 1) **Declaring dependencies** with specific versions in pom.xml, 2) **Using dependencyManagement** in parent POM for multi-module projects to enforce consistent versions, 3) **Resolving conflicts** — when two dependencies require different versions of the same library, Maven uses the "nearest definition" strategy. I use `mvn dependency:tree` to analyze the full dependency tree and identify conflicts. For example, Selenium might bring in guava 31.x while another library needs guava 30.x. I resolve this by: a) Using `<exclusion>` to exclude the conflicting transitive dependency, b) Explicitly declaring the required version in my pom.xml. 4) **BOM (Bill of Materials)** — I use import scope for managing large sets of related dependencies. Common conflict I\'ve resolved: RestAssured and Selenium both pulling different versions of Apache HttpClient — I excluded the transitive one and declared the compatible version explicitly.',detailedExplanation:'Dependency resolution order: 1) Direct dependencies win over transitive, 2) Nearest in tree wins, 3) First declaration wins for same depth. Use mvn dependency:tree -Dverbose to see conflicts.',simpleExplanation:'Maven dependencies are like ingredients in a recipe — you list what you need and Maven fetches them. Conflicts happen when two ingredients don\'t mix, and you resolve them by picking the right version.',realWorldExample:'Your project needs Library A and Library B. Both depend on Commons-IO but different versions. Maven might pick the wrong one, causing ClassNotFoundException at runtime.',projectExample:'I once debugged a ClassNotFoundException in CI that was caused by Selenium 4 and RestAssured pulling different versions of Gson. I used mvn dependency:tree to find the conflict and added an exclusion to fix it.',codeCommand:'<!-- Dependency with exclusion -->\n<dependency>\n  <groupId>io.rest-assured</groupId>\n  <artifactId>rest-assured</artifactId>\n  <version>5.3.2</version>\n  <exclusions>\n    <exclusion>\n      <groupId>org.apache.groovy</groupId>\n      <artifactId>groovy</artifactId>\n    </exclusion>\n  </exclusions>\n</dependency>\n\n<!-- Analyze dependency tree -->\n<!-- mvn dependency:tree -Dverbose -->',expectedOutput:'Dependencies resolved without conflicts',followUpQ:'What is a transitive dependency?',followUpA:'A transitive dependency is a dependency of your dependency. If your project depends on Selenium, and Selenium depends on Guava, then Guava is a transitive dependency. Maven automatically resolves these, but they can cause version conflicts. Use mvn dependency:tree to see all transitive dependencies.',commonMistake:'Ignoring dependency conflicts until they cause runtime errors in CI.',bestPractice:'Regularly run mvn dependency:tree and resolve conflicts early. Pin versions explicitly for critical libraries.'},
    {id:'MVN005',category:'Maven',topic:'Profiles',subtopic:'Maven Profiles',question:'How do you use Maven profiles in your test automation?',whyAsked:'Shows ability to configure framework for different environments.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'Maven profiles allow different configurations for different environments. I activate profiles with -P flag to switch between browsers, environments, and test suites.',interviewAnswer:'Maven profiles are a powerful way to manage environment-specific configurations. In my framework, I use profiles for: 1) **Environment switching** — profiles for QA, staging, production with different base URLs and credentials, 2) **Browser selection** — chrome, firefox, edge profiles that set browser properties, 3) **Test suite selection** — smoke, regression, sanity profiles that point to different TestNG suite files, 4) **CI vs Local** — CI profile enables headless mode and specific reporting. Profiles can be activated via: `-P` flag (`mvn test -Pstaging`), environment variables, JDK version, or OS. In Jenkins, I pass the profile as a build parameter, allowing the QA team to select the environment and suite from a dropdown.',detailedExplanation:'Profiles can override properties, dependencies, plugins, and build settings. They can be defined in pom.xml, profiles.xml, or settings.xml (~/.m2/settings.xml for global profiles).',simpleExplanation:'Profiles are like outfit presets — casual for home (local), formal for office (CI), sporty for gym (load testing). Same wardrobe, different combinations.',realWorldExample:'QA team needs to run regression on staging — they trigger Jenkins with -Pstaging -Pregression. The profile sets the URL, credentials, and suite file automatically.',projectExample:'My framework has 3 environment profiles (qa, staging, prod) and 3 suite profiles (smoke, regression, sanity). Jenkins job has dropdown parameters that map to these profiles.',codeCommand:'<!-- pom.xml profiles -->\n<profiles>\n  <profile>\n    <id>qa</id>\n    <properties>\n      <env>qa</env>\n      <base.url>https://qa.example.com</base.url>\n    </properties>\n  </profile>\n  <profile>\n    <id>staging</id>\n    <properties>\n      <env>staging</env>\n      <base.url>https://staging.example.com</base.url>\n    </properties>\n  </profile>\n  <profile>\n    <id>headless</id>\n    <properties>\n      <headless>true</headless>\n    </properties>\n  </profile>\n</profiles>\n\n<!-- Usage: mvn clean test -Pstaging -Pheadless -->',expectedOutput:'Tests run with profile-specific configuration',followUpQ:'How do you handle secrets in Maven profiles?',followUpA:'Never put secrets in pom.xml (committed to Git). I use: 1) settings.xml (~/.m2/settings.xml) for local secrets — this file is not committed, 2) Jenkins credentials — injected as environment variables, 3) System properties — passed via -D flags from Jenkins, 4) Encrypted properties using jasypt or similar libraries. In CI, Jenkins injects credentials as masked environment variables that the framework reads at runtime.',commonMistake:'Hardcoding environment URLs and credentials in pom.xml.',bestPractice:'Use profiles for non-sensitive config. Use Jenkins credentials or settings.xml for secrets.'}
  ]
};
