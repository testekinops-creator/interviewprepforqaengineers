/* ═══════════════════════════════════════════════════════════════
   coding-round.js — Coding Round Prep
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['coding-round'] = {
  title: '⌨️ Coding Round',
  description: 'Common programming questions (Java/Python) asked in SDET coding rounds',
  questions: [
    {id:'CR001',category:'Coding Round',topic:'Strings',subtopic:'String Reversal',question:'Write a program to reverse a String without using built-in reverse functions.',whyAsked:'Classic warm-up question testing basic logic and array/string manipulation.',difficulty:2,importance:'must',interviewType:'Coding',thirtySecAnswer:'Convert the string to a character array. Loop through the array from the end to the beginning (length - 1 down to 0), appending each character to a new String or StringBuilder.',interviewAnswer:'I would solve this using a `StringBuilder` and a `for` loop. \n\nApproach: \n1) Convert the input string to a char array: `char[] chars = str.toCharArray();`\n2) Create an empty `StringBuilder`.\n3) Iterate backwards: `for (int i = chars.length - 1; i >= 0; i--)`\n4) Append each char to the builder: `sb.append(chars[i]);`\n5) Return `sb.toString();`.\n\nWhy `StringBuilder`? Because String is immutable in Java. Using `+=` in a loop creates a new String object in memory on every iteration, which is highly inefficient O(N^2) space. `StringBuilder` modifies the same object, keeping it O(N) time and O(N) space.',detailedExplanation:'Alternative approach (In-place swap): Use two pointers, one at start (i=0) and one at end (j=length-1). Swap chars[i] and chars[j], increment i, decrement j, until i>=j. Space complexity is O(1) for the swap, though you still need O(N) to store the result array.',simpleExplanation:'Imagine a line of blocks with letters on them. To reverse it, you start at the last block and pick them up one by one, placing them in a new line from left to right.',realWorldExample:'N/A - basic algorithmic logic test.',projectExample:'N/A',codeCommand:'public String reverseString(String str) {\n    if (str == null || str.isEmpty()) {\n        return str;\n    }\n    \n    char[] chars = str.toCharArray();\n    StringBuilder reversed = new StringBuilder();\n    \n    for (int i = chars.length - 1; i >= 0; i--) {\n        reversed.append(chars[i]);\n    }\n    \n    return reversed.toString();\n}\n\n// Alternative: Two-pointer in-place\npublic String reverseInPlace(String str) {\n    char[] chars = str.toCharArray();\n    int left = 0;\n    int right = chars.length - 1;\n    \n    while (left < right) {\n        char temp = chars[left];\n        chars[left] = chars[right];\n        chars[right] = temp;\n        left++;\n        right--;\n    }\n    return new String(chars);\n}',expectedOutput:'"hello" becomes "olleh"',followUpQ:'How would you reverse the words in a sentence instead of characters? (e.g., "I love Java" -> "Java love I")',followUpA:'Split the string by spaces: `String[] words = str.split(" ");`. Then use the same backwards loop approach, but iterating over the `words` array and appending a space after each word. E.g., `for (int i = words.length - 1; i >= 0; i--) { sb.append(words[i]).append(" "); }`. Remember to `trim()` the final result.',commonMistake:'Using String concatenation (`str += char`) inside the loop, showing a lack of understanding of String immutability and memory management.',bestPractice:'Always mention edge cases (null, empty string) at the beginning of your code. Use StringBuilder.'},
    {id:'CR002',category:'Coding Round',topic:'Arrays',subtopic:'Find Duplicates',question:'Write a program to find duplicate elements in an array.',whyAsked:'Tests knowledge of data structures, specifically Sets and HashMaps.',difficulty:2,importance:'must',interviewType:'Coding',thirtySecAnswer:'The most efficient way is to use a HashSet. Iterate through the array; if `set.add(element)` returns false, it means the element is already in the set and is a duplicate.',interviewAnswer:'There are three ways to do this, but I\'ll focus on the most optimal.\n\n1) **Brute Force (Nested Loops)**: Compare every element with every other element. O(N^2) time. Bad approach.\n2) **Sorting**: Sort the array first O(N log N), then iterate once checking if `arr[i] == arr[i+1]`. Better, but modifies the array.\n3) **HashSet (Best Approach)**: Create an empty `HashSet`. Iterate through the array. Try to add each element to the set: `if (!set.add(element))`. Since Sets don\'t allow duplicates, `add()` returns `false` if the element exists. This is O(N) time complexity and O(N) space complexity.\n\nIf the interviewer asks to also count the *number* of occurrences, I would use a `HashMap<Integer, Integer>` instead, storing the element as the key and its frequency as the value.',detailedExplanation:'If the array contains numbers from 1 to N, you can achieve O(N) time and O(1) space by using the array elements as indices and negating the values at those indices.',simpleExplanation:'It\'s like checking guests at a party. You stand at the door with a list (the Set). When someone arrives, you check the list. If their name is already there, you found a duplicate!',realWorldExample:'N/A',projectExample:'I once used a HashSet approach in my automation framework to verify that a web table containing Order IDs did not contain any duplicate IDs.',codeCommand:'public void findDuplicates(int[] arr) {\n    if (arr == null || arr.length == 0) return;\n    \n    Set<Integer> uniqueElements = new HashSet<>();\n    Set<Integer> duplicates = new HashSet<>();\n    \n    for (int num : arr) {\n        // set.add() returns false if element already exists\n        if (!uniqueElements.add(num)) {\n            duplicates.add(num);\n        }\n    }\n    \n    System.out.println("Duplicates: " + duplicates);\n}\n\n// HashMap approach (if you need counts)\npublic void printDuplicateCounts(int[] arr) {\n    Map<Integer, Integer> counts = new HashMap<>();\n    for (int num : arr) {\n        counts.put(num, counts.getOrDefault(num, 0) + 1);\n    }\n    \n    for (Map.Entry<Integer, Integer> entry : counts.entrySet()) {\n        if (entry.getValue() > 1) {\n            System.out.println(entry.getKey() + " appears " + entry.getValue() + " times");\n        }\n    }\n}',expectedOutput:'[1, 2, 3, 1] prints Duplicates: [1]',followUpQ:'What is the time and space complexity of the HashSet approach?',followUpA:'Time complexity is O(N) because we iterate through the array of size N exactly once. Set insertion is O(1) on average. Space complexity is O(N) because in the worst-case scenario (no duplicates), we store all N elements in the HashSet.',commonMistake:'Starting with the nested loop approach and not being able to optimize it to O(N).',bestPractice:'Immediately suggest the HashSet approach and explain the Time/Space complexity benefits over the brute-force method.'}
  ]
};
