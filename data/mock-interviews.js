/* ═══════════════════════════════════════════════════════════════
   mock-interviews.js — Mock Interview Scenarios
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['mock-interviews'] = {
  title: '🎙️ Mock Interviews',
  description: 'Simulated interview rounds and rubrics to practice your delivery',
  questions: [
    {id:'MOCK001',category:'Mock',topic:'System Design',subtopic:'Framework Architecture',question:'Draw and explain the architecture of your current test automation framework.',whyAsked:'The ultimate test for a Senior SDET. Proves you didn\'t just write tests, but actually built or deeply understand the framework.',difficulty:5,importance:'must',interviewType:'Mock',thirtySecAnswer:'Start from the bottom up: Tools (Selenium/Java), Design Pattern (POM), Core Utilities (Driver setup, Config reader), Test Layer (TestNG, Data Providers), Reporting (Allure), and CI/CD (Jenkins). Explain how data flows from execution to reporting.',interviewAnswer:'*Be prepared to draw this on a whiteboard or virtual pad.*\n\nMy framework is a hybrid, data-driven POM framework using Java, Selenium, and TestNG. I structure it into distinct layers:\n\n1) **Configuration Layer**: `config.properties` files manage environment URLs, browser types, and credentials. A `ConfigReader` singleton parses this.\n\n2) **Core Engine / Base Layer**: Contains the `DriverFactory` (handles ThreadLocal WebDriver instances for parallel execution) and `BaseTest` (setup/teardown hooks).\n\n3) **Utilities Layer**: Custom wrappers. `WaitUtils` (explicit waits), `DBUtils` (JDBC connections), `APIUtils` (REST Assured clients for test data setup).\n\n4) **Page Object Model (POM) Layer**: Encapsulates web elements and page actions. No assertions live here. \n\n5) **Test Data Layer**: JSON or Excel files read by TestNG `@DataProvider` to feed data into tests.\n\n6) **Test Execution Layer**: Test classes extending `BaseTest`. They call POM methods and perform TestNG Assertions. Grouped by `testng.xml` (Smoke, Regression).\n\n7) **Reporting Layer**: Listeners catch test results and generate Allure reports with embedded screenshots on failure.\n\n8) **CI/CD Layer**: A `Jenkinsfile` orchestrates the run. It pulls from Git, executes `mvn clean test -Denv=QA`, and emails the Allure report to the team.\n\nThis modularity ensures that if the UI changes, I only update the POM. If the browser changes, I only update DriverFactory.',detailedExplanation:'Practice drawing boxes for each of these layers and drawing arrows showing the dependency (e.g., Test calls POM, POM uses Core Engine).',simpleExplanation:'Think of it like a restaurant. Config is the menu, Core Engine is the kitchen appliances, POM is the chefs, Tests are the waiters taking orders, and Reporting is the receipt given to the customer.',realWorldExample:'N/A',projectExample:'N/A',codeCommand:'',expectedOutput:'',followUpQ:'If you had to migrate this framework to Playwright, what layers would change?',followUpA:'The Configuration, Test Data, Reporting, and CI/CD layers would remain largely untouched. The Core Engine (DriverFactory) would be entirely replaced by Playwright fixtures. The POM layer would change locators (from `By` to `page.locator`), but the method signatures might stay the same. The Utilities layer (WaitUtils) would mostly be deleted since Playwright auto-waits.',commonMistake:'Describing a script-based approach instead of a layered architecture. Not mentioning CI/CD or reporting.',bestPractice:'Use keywords like ThreadLocal, Singleton, Encapsulation, and Modularity. Be ready to justify *why* you chose specific tools (e.g., "Why TestNG over JUnit?").'}
  ]
};
