/* ═══════════════════════════════════════════════════════════════
   selenium-locators.js — Locators, XPath, CSS Selectors
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['selenium-locators'] = {
  title: '🎯 Selenium Locators & XPath',
  description: 'All locator strategies including 50+ XPath interview questions',
  questions: [
    {id:'SL001',category:'Selenium',topic:'Locators',subtopic:'Locator Types',question:'Explain all Selenium locator strategies and when to use each.',whyAsked:'Fundamental Selenium knowledge — must demonstrate practical understanding.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'Selenium supports 8 locators: ID (fastest, preferred), Name, ClassName, TagName, LinkText, PartialLinkText, CSS Selector (fast, flexible), and XPath (most powerful, can traverse DOM).',interviewAnswer:'I prioritize locators in this order: 1) **ID** — fastest and most reliable, I always ask developers to add IDs to interactive elements. 2) **CSS Selector** — fast, supports complex selectors, I use this most frequently. 3) **XPath** — most powerful, can traverse up/down the DOM, I use it when CSS can\'t reach the element. 4) **Name** — useful for form elements. 5) **ClassName** — use with caution as classes can be shared. 6) **LinkText/PartialLinkText** — only for hyperlinks. 7) **TagName** — rarely used alone, mostly for findElements to get all elements of a type. In my framework, I use a consistent strategy: custom data-testid attributes first (if available), then IDs, then CSS selectors, and XPath only when necessary.',detailedExplanation:'Performance order: ID > Name > CSS > XPath. CSS can\'t traverse upward (no parent selection). XPath can traverse any direction. For dynamic elements, both CSS and XPath support partial matching.',simpleExplanation:'Locators are like addresses — ID is the exact house number (best), CSS is the street description, XPath is full GPS coordinates (powerful but verbose).',realWorldExample:'Login page: ID for username/password fields, CSS for styled buttons, XPath for elements inside nested divs.',projectExample:'In our Salesforce automation, many elements lacked IDs, so I used XPath with contains() for dynamic Salesforce element IDs that change between releases.',codeCommand:'// By ID\ndriver.findElement(By.id("username"));\n\n// By CSS Selector\ndriver.findElement(By.cssSelector("input[data-testid=\'email\']"));\ndriver.findElement(By.cssSelector(".login-form > button.submit"));\n\n// By XPath\ndriver.findElement(By.xpath("//input[@placeholder=\'Enter email\']"));\ndriver.findElement(By.xpath("//div[contains(@class,\'user\')]//span"));\n\n// By Name, Class, LinkText\ndriver.findElement(By.name("password"));\ndriver.findElement(By.className("btn-primary"));\ndriver.findElement(By.linkText("Sign Up"));',expectedOutput:'Element is located and returned as WebElement',followUpQ:'Why is CSS Selector faster than XPath?',followUpA:'CSS selectors are natively supported by all browsers through the document.querySelectorAll() engine, which is highly optimized. XPath requires a separate XPath engine that the browser needs to evaluate, which adds overhead. However, the difference is usually milliseconds per lookup — it matters at scale (thousands of lookups) but not for individual elements.',seniorFollowUp:'How do you maintain locators in a large framework?',seniorAnswer:'I use Page Object Model with locators defined as class variables. Naming convention: by_elementPurpose (e.g., by_loginButton). I avoid hardcoded locators in test code. For frequently changing locators, I use data-testid attributes and coordinate with developers to add them. We also have a locator health check — a utility that validates all locators in the POM classes are still valid against the current build.',commonMistake:'Using XPath for everything instead of using simpler, faster locators.',bestPractice:'Use the simplest locator that uniquely identifies the element. ID > CSS > XPath.'},
    {id:'SL002',category:'Selenium',topic:'XPath',subtopic:'Absolute vs Relative',question:'What is the difference between Absolute and Relative XPath?',whyAsked:'Basic XPath question — everyone should know this.',difficulty:1,importance:'must',interviewType:'Phone Screen',thirtySecAnswer:'Absolute XPath starts from the root (html/) with exact path — fragile. Relative XPath starts from anywhere (//) using attributes — flexible and recommended.',interviewAnswer:'Absolute XPath starts from the root element and traces the exact path: `/html/body/div[2]/form/input[1]`. It\'s extremely fragile — any change in the DOM structure breaks it. I never use absolute XPath in real automation. Relative XPath starts with `//` and searches the entire DOM: `//input[@id="username"]`. It\'s flexible, maintainable, and the standard approach. In my projects, I exclusively use relative XPath with descriptive attributes. I also combine multiple attributes for uniqueness: `//input[@type="text" and @name="email"]`.',detailedExplanation:'Absolute: /html/body/div/form/input — exact path from root. Relative: //input[@id="email"] — searches anywhere in DOM. Relative is: shorter, more readable, more maintainable, less likely to break.',simpleExplanation:'Absolute = give directions from the city entrance (fragile if any road changes). Relative = use a landmark (robust — "near the hospital").',realWorldExample:'A developer adds a wrapper div — absolute XPath breaks. Relative XPath using attributes continues to work.',projectExample:'When I joined the project, the previous team used absolute XPath extensively. I refactored 200+ locators to relative XPath, which reduced locator maintenance by 80%.',codeCommand:'// Absolute XPath (NEVER use)\n/html/body/div[1]/div[2]/form/div[3]/input\n\n// Relative XPath (ALWAYS use)\n//input[@id="email"]\n//input[@name="password"]\n//button[text()="Login"]\n//div[@class="container"]//input[@type="text"]',expectedOutput:'Both find the same element, but relative XPath survives DOM changes',followUpQ:'When might absolute XPath be the only option?',followUpA:'Almost never in real testing. The only scenario might be automated testing of the DOM structure itself, or when an element has absolutely no distinguishing attributes and a very specific position. Even then, I would request developers to add data-testid attributes rather than rely on absolute XPath.',seniorFollowUp:'How do you write robust XPath that doesn\'t break easily?',seniorAnswer:'My principles: 1) Use unique attributes (id, name, data-testid), 2) Avoid index-based XPath (//div[3]) unless absolutely necessary, 3) Use contains() for dynamic attributes: //div[contains(@class,"user-card")], 4) Use text() sparingly — text changes with localization, 5) Combine multiple attributes for uniqueness, 6) Keep XPath short — long chains are fragile.',commonMistake:'Using absolute XPath from browser DevTools copy XPath feature.',bestPractice:'Never copy XPath from browser — always write relative XPath manually.'},
    {id:'SL003',category:'Selenium',topic:'XPath',subtopic:'XPath Functions',question:'Explain contains(), text(), starts-with() in XPath.',whyAsked:'Essential for handling dynamic elements — very frequently asked.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'contains() matches partial attribute values, text() matches visible text, starts-with() matches the beginning of attribute values. All are essential for dynamic elements.',interviewAnswer:'These XPath functions are my primary tools for dynamic element identification: **contains()** — I use it when only part of an attribute value is static. Example: Salesforce generates IDs like "lead_12345_name" where 12345 changes — `//input[contains(@id,"_name")]` matches. **text()** — matches visible text content. I use it for buttons and labels: `//button[text()="Submit"]`. For text with spaces, I use normalize-space(): `//button[normalize-space()="Submit"]`. **starts-with()** — matches the beginning of an attribute: `//div[starts-with(@class,"user-")]` matches "user-card", "user-profile", etc. I often combine these: `//div[contains(@class,"card") and starts-with(@data-id,"user")]`.',detailedExplanation:'contains(@attr,"value") — partial match anywhere in attribute. text() — matches exact visible text. normalize-space() — trims whitespace. starts-with(@attr,"value") — matches beginning. Note: XPath 1.0 has no ends-with() — use contains() instead.',simpleExplanation:'contains = "does this address have \'Main Street\' anywhere?" starts-with = "does this address start with \'123\'?" text = "does this sign say \'Open\'?"',realWorldExample:'Dynamic table row: //tr[contains(@class,"row-item")]//td[text()="Active"]. This finds the "Active" status cell in any table row.',projectExample:'In Salesforce, element IDs are dynamic like "00Q5g00000ABC:j_id0:editForm:name". I use contains(@id,":name") to match the stable suffix part.',codeCommand:'// contains() - partial attribute match\n//input[contains(@class, "search")]\n//div[contains(@id, "user_")]\n\n// text() - exact text match\n//button[text()="Sign In"]\n//a[text()="Home"]\n\n// normalize-space() - text with whitespace\n//span[normalize-space()="Total Amount"]\n\n// starts-with() - beginning of attribute\n//div[starts-with(@class, "nav-")]\n//input[starts-with(@name, "field_")]\n\n// Combined\n//div[contains(@class,"card") and text()="Active"]',expectedOutput:'Element matching the XPath criteria is found',followUpQ:'What is normalize-space() and when do you need it?',followUpA:'normalize-space() strips leading/trailing whitespace and collapses internal whitespace. It\'s needed when the rendered text has extra spaces or newlines that aren\'t visible in the UI but exist in the DOM. Example: text might be "  Submit  " in DOM but shows as "Submit". `//button[normalize-space()="Submit"]` handles this reliably.',seniorFollowUp:'How do you handle elements with completely dynamic attributes?',seniorAnswer:'When attributes are fully dynamic, I use: 1) Traverse from a stable parent: //div[@id="static-parent"]//input (any input inside a known parent), 2) Use visible text: //td[text()="John"]/following-sibling::td/button, 3) Use position relative to stable elements: //label[text()="Email"]/following::input[1], 4) Coordinate with dev to add data-testid attributes, 5) Use structural XPath with specific element types and positions as last resort.',commonMistake:'Using contains() with values that are too generic, matching multiple elements.',bestPractice:'Make contains() values specific enough to match uniquely. Combine with other conditions for precision.'},
    {id:'SL004',category:'Selenium',topic:'XPath',subtopic:'XPath Axes',question:'Explain XPath axes: parent, child, ancestor, descendant, following-sibling, preceding-sibling.',whyAsked:'Advanced XPath — shows you can locate elements through DOM relationships.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'XPath axes allow traversal in any direction: parent (one up), ancestor (all up), child (one down), descendant (all down), following-sibling (same-level after), preceding-sibling (same-level before).',interviewAnswer:'XPath axes are essential when an element has no unique attributes but its neighbors do. Here are the axes I use most: **parent::** — go one level up: `//input[@id="email"]/parent::div` finds the parent div of the email input. **ancestor::** — go any number of levels up: `//span[text()="Error"]/ancestor::form` finds the form containing the error span. **following-sibling::** — find the next sibling: `//label[text()="Name"]/following-sibling::input` finds the input field next to the "Name" label. **preceding-sibling::** — opposite direction: `//input[@id="email"]/preceding-sibling::label` finds the label before the email input. **child::** — direct children only: `//div[@class="container"]/child::span`. **descendant::** — all nested elements: `//div[@id="main"]/descendant::button` finds all buttons inside main div. I use these primarily for: table cell navigation, form field identification by label, and error message association.',detailedExplanation:'Full list: self, child, descendant, descendant-or-self, parent, ancestor, ancestor-or-self, following, following-sibling, preceding, preceding-sibling. Most commonly used: parent, ancestor, following-sibling, preceding-sibling, descendant.',simpleExplanation:'Axes are directions: parent = look up one floor, ancestor = look up any floor, child = look down one floor, descendant = look down any floor, sibling = look same floor left/right.',realWorldExample:'Table: find the "Edit" button for user "John" — //td[text()="John"]/following-sibling::td/button[text()="Edit"]',projectExample:'In our Salesforce page, to click the edit icon for a specific record, I use: //a[text()="Record Name"]/ancestor::tr//button[@title="Edit"]',codeCommand:'// parent - go up one level\n//input[@id="email"]/parent::div\n\n// ancestor - go up any levels\n//span[text()="Error"]/ancestor::form\n\n// child - direct children\n//ul[@id="menu"]/child::li\n\n// descendant - all nested\n//div[@id="content"]/descendant::a\n\n// following-sibling - next sibling\n//label[text()="Email"]/following-sibling::input\n\n// preceding-sibling - previous sibling  \n//td[text()="Active"]/preceding-sibling::td[1]\n\n// Real-world: table row navigation\n//td[text()="John Smith"]/following-sibling::td/button[@class="edit"]',expectedOutput:'Elements found through DOM relationship traversal',followUpQ:'What is the difference between // and / in XPath?',followUpA:'/ selects direct children only (one level down). // selects descendants at any level (searches entire subtree). Example: /html/body/div selects only direct child divs of body. //div selects ALL divs anywhere in the document. //form//input finds all inputs at any depth inside any form.',seniorFollowUp:'How do you optimize complex XPath expressions for performance?',seniorAnswer:'1) Start with the most specific ancestor to narrow the search scope: //div[@id="unique"]/descendant::button instead of //button, 2) Use direct child (/) instead of descendant (//) when possible, 3) Avoid overly long axis chains — if your XPath has 4+ axes, the locator strategy needs rethinking, 4) Prefer CSS selectors for simple queries — they\'re faster, 5) Cache elements that are reused multiple times, 6) Use WebDriverWait instead of repeated findElement calls.',commonMistake:'Writing overly complex XPath chains that are fragile and hard to maintain.',bestPractice:'Keep XPath expressions as short and readable as possible. Use meaningful attributes.'},
    {id:'SL005',category:'Selenium',topic:'XPath',subtopic:'Dynamic XPath',question:'How do you handle dynamic elements in Selenium?',whyAsked:'Real-world automation challenge — shows problem-solving ability.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'I handle dynamic elements using: contains()/starts-with() for partial attribute matching, XPath axes for relative positioning, CSS attribute selectors, and explicit waits for AJAX-loaded elements.',interviewAnswer:'Dynamic elements are one of the biggest challenges in automation. I handle them through multiple strategies: 1) **Partial attribute matching** — When IDs change partially: `//input[contains(@id,"user_name")]` instead of `//input[@id="user_123_name"]`. 2) **Relative positioning** — Use stable neighbors: `//label[text()="Email"]/following-sibling::input`. 3) **Custom attributes** — I collaborate with developers to add `data-testid` attributes that never change. 4) **Explicit waits** — For elements that load dynamically via AJAX: `WebDriverWait wait = new WebDriverWait(driver, 10); wait.until(ExpectedConditions.visibilityOfElementLocated(By.id("results")))`. 5) **JavaScript execution** — For elements hidden from Selenium: `((JavascriptExecutor)driver).executeScript("return document.querySelector(\'[data-value]\')"). In my Salesforce project, almost every element had dynamic IDs, so I built a comprehensive locator strategy combining all these techniques.',detailedExplanation:'Dynamic element types: Dynamic IDs (session-based), AJAX-loaded elements, elements that appear/disappear based on conditions, stale elements after page refresh, elements inside iframes or shadow DOM.',simpleExplanation:'Dynamic elements are like a moving target — instead of aiming at its exact position, you aim based on its pattern of movement.',realWorldExample:'A React app generates class names like "css-1a2b3c" that change every build. Solution: use data-testid or structural XPath.',projectExample:'Salesforce Lightning components generate IDs like "69:2;a" that change on every page load. I created a locator utility that finds elements by their stable attributes (label, placeholder, title) instead of IDs.',codeCommand:'// Strategy 1: Partial match\n//input[contains(@id, "username")]\n//div[starts-with(@class, "user-card")]\n\n// Strategy 2: Relative to stable element\n//label[text()="Email"]/following-sibling::input\n\n// Strategy 3: Custom data attributes\n//button[@data-testid="submit-order"]\n\n// Strategy 4: Explicit Wait for AJAX\nWebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(10));\nWebElement el = wait.until(\n    ExpectedConditions.visibilityOfElementLocated(\n        By.cssSelector("[data-testid=\'search-results\']")\n    )\n);',expectedOutput:'Dynamic element located reliably despite changing attributes',followUpQ:'How do you handle StaleElementReferenceException with dynamic elements?',followUpA:'StaleElementReferenceException occurs when the DOM refreshes and the previously found element reference becomes invalid. Solutions: 1) Re-find the element before interacting, 2) Use explicit waits with staleness check: wait.until(ExpectedConditions.stalenessOf(oldElement)), then find again, 3) In my framework, I have a RetryOnStale utility that retries the operation up to 3 times with re-fetch.',seniorFollowUp:'How would you architect a locator strategy for a large, dynamic application?',seniorAnswer:'My approach: 1) Define a locator priority hierarchy: data-testid > ID > CSS > XPath, 2) Work with developers to add data-testid to all interactive elements (this is the single most impactful improvement), 3) Create a locator factory that generates locators based on element type and context, 4) Implement a locator health check that runs weekly to detect broken locators, 5) Store locators in Page Object classes with descriptive names, 6) Review locators in code reviews — reject brittle ones.',commonMistake:'Hardcoding full dynamic IDs and wondering why tests break nightly.',bestPractice:'Invest in data-testid attributes. The 30 minutes of developer time saves hundreds of hours of automation maintenance.'},
    {id:'SL006',category:'Selenium',topic:'XPath',subtopic:'XPath and/or',question:'How do you use and/or operators in XPath?',whyAsked:'Practical XPath skill for complex element identification.',difficulty:2,importance:'important',interviewType:'Technical',thirtySecAnswer:'Use "and" to require multiple conditions, "or" for any condition match. Example: //input[@type="text" and @name="email"] requires both; //input[@type="text" or @type="email"] matches either.',interviewAnswer:'I frequently use `and`/`or` operators to create precise locators: **and** — both conditions must be true: `//input[@type="text" and @name="email"]` finds an input that is both text type AND named email. **or** — either condition is sufficient: `//button[text()="Submit" or text()="Save"]` finds a button with either text. I combine these with functions: `//div[contains(@class,"active") and @data-type="user"]`, `//input[starts-with(@id,"field_") and not(@disabled)]`. The `not()` function is also useful: `//input[not(@type="hidden")]` finds all non-hidden inputs.',detailedExplanation:'Operators: and (both true), or (either true), not() (negation). Can be combined: //div[(contains(@class,"a") or contains(@class,"b")) and @role="button"]. Precedence: not > and > or.',simpleExplanation:'and = both must be true (I want a red AND large shirt). or = either is fine (I want a red OR blue shirt).',realWorldExample:'Find active non-disabled buttons: //button[contains(@class,"active") and not(@disabled)]',projectExample:'In my e-commerce app, product filters use: //div[@class="filter" and @data-category="electronics"]//input[@type="checkbox" and not(@checked)]',codeCommand:'// AND - both conditions required\n//input[@type="text" and @name="email"]\n//div[contains(@class,"active") and @data-id="123"]\n\n// OR - either condition\n//button[text()="Submit" or text()="Save"]\n//input[@type="text" or @type="email"]\n\n// NOT - negation\n//input[not(@disabled)]\n//div[not(contains(@class,"hidden"))]\n\n// Combined complex\n//div[(contains(@class,"card") or contains(@class,"item")) \n     and not(contains(@class,"hidden"))]',expectedOutput:'Element matching the combined conditions is found',followUpQ:'Can you use and/or with contains() and text()?',followUpA:'Yes, they combine freely: //div[contains(@class,"user") and text()="Active"] finds divs with class containing "user" AND text "Active". //span[contains(text(),"Error") or contains(text(),"Warning")] finds spans containing either "Error" or "Warning" text.',seniorFollowUp:'How do you handle locators that match multiple elements?',seniorAnswer:'If a locator matches multiple elements, I narrow it down by: 1) Adding more conditions with "and", 2) Using a more specific parent scope, 3) Adding positional index as last resort: (//div[@class="item"])[3] for the third match, 4) Using ancestor/descendant axes to scope the search, 5) If elements are truly identical, I use findElements() and iterate to find the right one based on other attributes.',commonMistake:'Using "&&" or "||" instead of "and"/"or" — these are JavaScript operators, not XPath.',bestPractice:'Use "and" for precision, "or" for flexibility, "not()" for exclusion. Always test your XPath in browser DevTools first.'},
    {id:'SL007',category:'Selenium',topic:'CSS',subtopic:'CSS Selectors',question:'Explain commonly used CSS Selectors for Selenium.',whyAsked:'CSS is faster than XPath — shows modern locator strategy knowledge.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'CSS selectors: #id, .class, tag, [attribute=value], [attr*=value] (contains), [attr^=value] (starts-with), parent > child, ancestor descendant, :first-child, :nth-child(n).',interviewAnswer:'I prefer CSS selectors over XPath for most scenarios because they\'re faster and more readable. Key selectors I use: **#id** — `#username` (same as By.id). **\\.class** — `.btn-primary`. **Attribute** — `input[name="email"]`, `[data-testid="login"]`. **Partial match** — `[class*="active"]` (contains), `[id^="user_"]` (starts-with), `[id$="_name"]` (ends-with). **Hierarchy** — `form > input` (direct child), `form input` (any descendant). **Pseudo** — `li:first-child`, `li:last-child`, `li:nth-child(3)`. **Combined** — `form.login-form > input[type="text"]:first-child`. The main limitation of CSS is it can\'t traverse upward (no parent selection) and can\'t match by text content — for those, I use XPath.',detailedExplanation:'CSS advantages: Faster (native browser support), shorter syntax, widely understood. CSS limitations: No parent/ancestor selection, no text matching, no preceding-sibling equivalent. Use CSS as default, XPath for upward traversal and text matching.',simpleExplanation:'CSS selectors are like describing an element\'s appearance and position: "the blue button inside the login form" becomes "form.login button.blue".',realWorldExample:'React app with data-testid: [data-testid="checkout-button"]. Tables: table.results tr:nth-child(2) td:first-child.',projectExample:'In my framework, 70% of locators are CSS selectors. I use CSS for: form elements, buttons, navigation items. I switch to XPath only for text-based and parent-based lookups.',codeCommand:'// CSS Selectors in Selenium\ndriver.findElement(By.cssSelector("#username"));           // ID\ndriver.findElement(By.cssSelector(".btn-primary"));        // Class\ndriver.findElement(By.cssSelector("input[name=\'email\']")); // Attribute\ndriver.findElement(By.cssSelector("[data-testid=\'login\']")); // Custom attr\ndriver.findElement(By.cssSelector("[class*=\'active\']"));   // Contains\ndriver.findElement(By.cssSelector("[id^=\'user_\']"));       // Starts with\ndriver.findElement(By.cssSelector("[id$=\'_name\']"));       // Ends with\ndriver.findElement(By.cssSelector("form > input"));        // Direct child\ndriver.findElement(By.cssSelector("ul li:nth-child(3)")); // 3rd item',expectedOutput:'Elements found using CSS selector strategies',followUpQ:'When should you use CSS over XPath and vice versa?',followUpA:'Use CSS when: simple attribute matching, class/id selection, child/descendant navigation, performance matters. Use XPath when: you need to traverse upward (parent/ancestor), match by text content, use complex logical conditions (and/or/not), or navigate between siblings. In practice, I use CSS 70% of the time and XPath 30%.',seniorFollowUp:'How do you decide the locator strategy for a new project?',seniorAnswer:'My decision framework: 1) If the app has data-testid → CSS [data-testid="x"] (best), 2) If elements have unique IDs → By.id (fast), 3) If I need attribute matching → CSS (clean syntax), 4) If I need text matching → XPath with text(), 5) If I need parent traversal → XPath with ancestor::. I document the strategy in the framework README and enforce it in code reviews.',commonMistake:'Not knowing CSS selectors and using XPath for everything, including simple ID lookups.',bestPractice:'Learn both CSS and XPath. Use CSS as your default and XPath as a powerful fallback.'}
  ]
};
