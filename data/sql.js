/* ═══════════════════════════════════════════════════════════════
   sql.js — SQL for QA/SDET
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['sql'] = {
  title: '📊 SQL',
  description: 'SQL fundamentals, joins, subqueries, and database concepts for QA engineers',
  questions: [
    {id:'SQL001',category:'SQL',topic:'Basics',subtopic:'SQL Fundamentals',question:'What SQL queries do you use as a QA engineer?',whyAsked:'Database verification is essential for thorough testing.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'I use SELECT (read data), INSERT (create test data), UPDATE (modify), DELETE (cleanup), JOIN (combine tables), and aggregate functions (COUNT, SUM) for data validation and test data management.',interviewAnswer:'As a QA engineer, I use SQL for: 1) **Data Validation** — After UI/API operations, verify data was correctly stored in the database: `SELECT * FROM users WHERE email = \'test@example.com\'`. 2) **Test Data Setup** — Create test data directly: `INSERT INTO users (name, email) VALUES (\'Test User\', \'test@example.com\')`. 3) **Test Data Cleanup** — Remove test data after test runs: `DELETE FROM users WHERE email LIKE \'test_%\'`. 4) **Bug Investigation** — When the UI shows wrong data, query the DB to determine if it\'s a frontend or backend issue. 5) **Reporting** — Count test records, verify aggregations match UI: `SELECT status, COUNT(*) FROM orders GROUP BY status`. Common queries: SELECT with WHERE, JOIN for related tables, GROUP BY with aggregate functions (COUNT, SUM, AVG), ORDER BY, LIKE for pattern matching, and subqueries for complex conditions.',detailedExplanation:'SQL statement order: SELECT → FROM → WHERE → GROUP BY → HAVING → ORDER BY → LIMIT. But execution order is: FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT.',simpleExplanation:'SQL is like asking the database specific questions — "Show me all users who signed up today" or "How many orders are pending?" — and getting precise answers.',realWorldExample:'QA testing order placement: Place order via UI → Query database to verify order record exists with correct items, quantities, prices, and status.',projectExample:'In my automation framework, I have a DatabaseUtil class that connects to the test database via JDBC. After every critical test (user creation, order placement), I run a SQL query to verify the data matches what was entered through the UI.',codeCommand:'-- Basic SELECT\nSELECT * FROM users WHERE status = \'active\';\n\n-- Filtered with LIKE\nSELECT name, email FROM users WHERE email LIKE \'%@gmail.com\';\n\n-- Aggregate functions\nSELECT status, COUNT(*) as count FROM orders GROUP BY status;\n\n-- INSERT test data\nINSERT INTO users (name, email, role) VALUES (\'Test User\', \'test@qa.com\', \'tester\');\n\n-- UPDATE\nUPDATE users SET status = \'active\' WHERE id = 100;\n\n-- DELETE test data\nDELETE FROM users WHERE email LIKE \'test_%@qa.com\';\n\n-- ORDER BY with LIMIT\nSELECT * FROM orders ORDER BY created_at DESC LIMIT 10;',expectedOutput:'Query results matching the criteria',followUpQ:'How do you connect to a database from your automation framework?',followUpA:'I use JDBC (Java Database Connectivity): 1) Add MySQL/PostgreSQL driver dependency in pom.xml. 2) Create a DatabaseUtil class with connection method using DriverManager.getConnection(url, user, pass). 3) Execute queries using PreparedStatement (prevents SQL injection). 4) Parse ResultSet for validation. 5) Close connections in finally block or use try-with-resources. Connection details come from config.properties per environment.',commonMistake:'Hardcoding database credentials in test code.',bestPractice:'Use config files for DB credentials. Use PreparedStatement to prevent SQL injection. Always close connections.'},
    {id:'SQL002',category:'SQL',topic:'Joins',subtopic:'SQL Joins',question:'Explain different types of SQL JOINs.',whyAsked:'Core SQL knowledge — crucial for querying related data.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'INNER JOIN returns matching rows from both tables. LEFT JOIN returns all rows from left + matches from right. RIGHT JOIN returns all from right + matches from left. FULL OUTER JOIN returns all rows from both tables.',interviewAnswer:'SQL JOINs combine rows from two or more tables based on a related column: 1) **INNER JOIN** — Returns only rows that have matching values in BOTH tables. Most common. `SELECT u.name, o.order_id FROM users u INNER JOIN orders o ON u.id = o.user_id` — only users who have orders. 2) **LEFT JOIN (LEFT OUTER JOIN)** — Returns ALL rows from the left table + matching rows from right. Non-matching right rows show NULL. `SELECT u.name, o.order_id FROM users u LEFT JOIN orders o ON u.id = o.user_id` — all users, even those without orders. 3) **RIGHT JOIN** — Returns ALL rows from right table + matching from left. Less common, can be rewritten as LEFT JOIN. 4) **FULL OUTER JOIN** — Returns ALL rows from BOTH tables. NULLs where no match. 5) **CROSS JOIN** — Cartesian product — every row from table A combined with every row from table B. Rarely used. 6) **SELF JOIN** — Table joined with itself — useful for hierarchical data (employee → manager). In testing, I use INNER JOIN most frequently — verifying that orders have valid users, products have correct categories.',detailedExplanation:'JOIN performance tip: Always join on indexed columns. For large tables, INNER JOIN is faster than OUTER JOIN. Use EXPLAIN to check query execution plan.',simpleExplanation:'JOINs are like matching pairs: INNER = only matching pairs, LEFT = all from left side (even unmatched), RIGHT = all from right side, FULL = everything from both sides.',realWorldExample:'Testing a report: JOIN users with orders to verify "User Order Summary" report shows correct data — LEFT JOIN ensures users with zero orders are included.',projectExample:'In my database validation tests, I use INNER JOIN to verify data integrity: every order must have a valid user, every line item must reference a valid product. Orphan records indicate bugs.',codeCommand:'-- INNER JOIN: Only matching rows\nSELECT u.name, o.order_id, o.total\nFROM users u\nINNER JOIN orders o ON u.id = o.user_id;\n\n-- LEFT JOIN: All users + their orders (NULL if no orders)\nSELECT u.name, o.order_id\nFROM users u\nLEFT JOIN orders o ON u.id = o.user_id;\n\n-- Multiple JOINs\nSELECT u.name, o.order_id, p.product_name\nFROM users u\nINNER JOIN orders o ON u.id = o.user_id\nINNER JOIN order_items oi ON o.id = oi.order_id\nINNER JOIN products p ON oi.product_id = p.id;\n\n-- SELF JOIN: Employee-Manager\nSELECT e.name as Employee, m.name as Manager\nFROM employees e\nLEFT JOIN employees m ON e.manager_id = m.id;',expectedOutput:'Combined data from multiple tables',followUpQ:'When would you use LEFT JOIN vs INNER JOIN?',followUpA:'Use INNER JOIN when you only want rows with data in BOTH tables — "show me users who have placed orders." Use LEFT JOIN when you want ALL rows from the main table regardless of match — "show me all users and their orders (including users with no orders)." In testing: INNER JOIN for positive validation, LEFT JOIN with WHERE right.id IS NULL for finding orphan records.',commonMistake:'Using wrong JOIN type and getting incorrect results without realizing it.',bestPractice:'Understand your data requirement first, then choose the JOIN type. Use table aliases for readability.'},
    {id:'SQL003',category:'SQL',topic:'Advanced',subtopic:'Group By and Having',question:'Explain GROUP BY and HAVING clauses.',whyAsked:'Important for data aggregation and reporting validation.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'GROUP BY groups rows by column values for aggregate functions (COUNT, SUM, AVG). HAVING filters groups after aggregation. WHERE filters before grouping, HAVING filters after.',interviewAnswer:'**GROUP BY** groups rows that have the same value in specified columns, allowing aggregate functions to operate on each group: `SELECT department, COUNT(*) FROM employees GROUP BY department` — counts employees per department. **HAVING** filters groups after aggregation — it\'s like WHERE but for groups. `SELECT department, COUNT(*) as emp_count FROM employees GROUP BY department HAVING emp_count > 10` — only departments with more than 10 employees. Key difference: **WHERE** filters individual rows BEFORE grouping. **HAVING** filters groups AFTER grouping. In testing, I use GROUP BY to: validate dashboard counts (orders per status), verify data distribution (products per category), find duplicates (email addresses appearing more than once), and validate aggregated reports (total sales by region).',detailedExplanation:'Aggregate functions: COUNT(*) (row count), SUM(column) (total), AVG(column) (average), MIN/MAX (smallest/largest), COUNT(DISTINCT column) (unique values). GROUP BY can have multiple columns: GROUP BY dept, role.',simpleExplanation:'GROUP BY is like sorting objects into buckets by category, then counting or summing what\'s in each bucket. HAVING is removing buckets that don\'t meet a threshold.',realWorldExample:'Testing a sales dashboard: GROUP BY month to verify monthly totals match the UI. HAVING total > 1000 to validate the "Top Performing Months" filter.',projectExample:'I used GROUP BY to find duplicate test users in the database: SELECT email, COUNT(*) c FROM users GROUP BY email HAVING c > 1. Found 15 duplicate emails causing test flakiness.',codeCommand:'-- Count per group\nSELECT status, COUNT(*) as order_count\nFROM orders\nGROUP BY status;\n\n-- Filter groups with HAVING\nSELECT category, COUNT(*) as product_count\nFROM products\nGROUP BY category\nHAVING product_count > 5;\n\n-- WHERE vs HAVING\nSELECT department, AVG(salary) as avg_sal\nFROM employees\nWHERE status = \'active\'          -- filters rows BEFORE grouping\nGROUP BY department\nHAVING avg_sal > 50000;            -- filters groups AFTER grouping\n\n-- Find duplicates\nSELECT email, COUNT(*) as count\nFROM users\nGROUP BY email\nHAVING count > 1;',expectedOutput:'Aggregated results per group',followUpQ:'Can you use WHERE and HAVING together?',followUpA:'Yes! WHERE filters individual rows first, then GROUP BY groups the remaining rows, then HAVING filters the groups. Example: "Find departments where the average salary of ACTIVE employees is above 50K" — WHERE status=\'active\' (filter rows), GROUP BY department (group), HAVING AVG(salary) > 50000 (filter groups). They work at different stages of query execution.',commonMistake:'Using WHERE instead of HAVING for aggregate conditions — WHERE COUNT(*) > 5 is invalid.',bestPractice:'Use WHERE for row-level filtering, HAVING for group-level filtering. Always use GROUP BY with aggregate functions.'},
    {id:'SQL004',category:'SQL',topic:'Advanced',subtopic:'Subqueries',question:'What are subqueries and when do you use them?',whyAsked:'Advanced SQL skill — shows depth of database knowledge.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'A subquery is a query nested inside another query. It can be in WHERE (filtering), SELECT (computed column), or FROM (derived table). Used when the filter value comes from another query.',interviewAnswer:'A subquery (inner query) is a query nested within another query. Types: 1) **WHERE subquery** — filter based on another query\'s result: `SELECT * FROM users WHERE id IN (SELECT user_id FROM orders WHERE total > 1000)` — users who placed high-value orders. 2) **FROM subquery (Derived Table)** — use query result as a virtual table: `SELECT dept, avg_sal FROM (SELECT department as dept, AVG(salary) as avg_sal FROM employees GROUP BY department) sub WHERE avg_sal > 50000`. 3) **SELECT subquery (Scalar)** — computed column: `SELECT name, (SELECT COUNT(*) FROM orders WHERE orders.user_id = users.id) as order_count FROM users`. 4) **Correlated subquery** — inner query references outer query, executes for each outer row. Used in testing for: finding users without orders (NOT IN/NOT EXISTS), comparing with averages, and complex data validation that can\'t be done with simple JOINs.',detailedExplanation:'Performance: Subqueries can be slower than JOINs for large datasets. EXISTS is generally faster than IN for large subquery results. Correlated subqueries run once per outer row — can be slow.',simpleExplanation:'A subquery is like a question within a question — "Show me all users (who have placed orders that cost more than $100)." The inner question answers first, then the outer one uses that answer.',realWorldExample:'Testing a "Top Customers" report: SELECT users WHERE total_orders > (SELECT AVG(total_orders) FROM users) — finds customers above average.',projectExample:'In data migration testing, I used subqueries to validate completeness: SELECT * FROM old_system.users WHERE id NOT IN (SELECT old_id FROM new_system.users) — finds unmigrated records.',codeCommand:'-- Subquery in WHERE\nSELECT * FROM users\nWHERE id IN (\n  SELECT user_id FROM orders WHERE total > 1000\n);\n\n-- NOT IN — find users without orders\nSELECT * FROM users\nWHERE id NOT IN (\n  SELECT DISTINCT user_id FROM orders\n);\n\n-- EXISTS (better performance)\nSELECT * FROM users u\nWHERE EXISTS (\n  SELECT 1 FROM orders o WHERE o.user_id = u.id AND o.total > 1000\n);\n\n-- Subquery in SELECT\nSELECT name,\n  (SELECT COUNT(*) FROM orders o WHERE o.user_id = u.id) as order_count\nFROM users u;',expectedOutput:'Filtered results based on subquery logic',followUpQ:'What is the difference between IN and EXISTS?',followUpA:'IN: Executes inner query once, stores results, then checks each outer row against the list. Better for small inner results. EXISTS: For each outer row, checks if inner query returns any row (stops at first match). Better for large inner results. NOT EXISTS is almost always faster than NOT IN (NOT IN has issues with NULLs). Rule of thumb: Use EXISTS for correlated subqueries with large datasets, IN for small static lists.',commonMistake:'Using NOT IN when the subquery might return NULLs — NOT IN with NULL always returns empty.',bestPractice:'Prefer EXISTS over IN for large datasets. Use JOINs when possible — they\'re usually faster than subqueries.'}
  ]
};
