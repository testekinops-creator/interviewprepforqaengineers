/* ═══════════════════════════════════════════════════════════════
   playwright-framework.js — Playwright Framework Design
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['pw-framework'] = {
  title: '🏗️ Playwright Framework Design',
  description: 'Page Object Model, Fixtures, Configuration, and CI integration in Playwright',
  questions: [
    {id:'PWF001',category:'Playwright',topic:'Framework',subtopic:'POM & Fixtures',question:'How do you implement Page Object Model in Playwright?',whyAsked:'Tests architecture and design pattern skills specific to Playwright.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'I create classes for each page, passing the `page` object in the constructor. I define locators as class properties and actions as async methods. I then use Playwright Fixtures to automatically instantiate these pages for tests.',interviewAnswer:'I implement POM in Playwright using ES6 Classes. \n1) **Page Class**: Create a class (e.g., `LoginPage`). \n2) **Constructor**: Pass the Playwright `page` object into the constructor. \n3) **Locators**: Define locators using `this.page.locator()` or `this.page.getByRole()` as class properties in the constructor. \n4) **Methods**: Create `async` methods for page actions (e.g., `async login(user, pass)`). \n5) **Fixtures (The Playwright Way)**: Instead of manually instantiating `const loginPage = new LoginPage(page)` in every test, I extend Playwright\'s `test` object using Custom Fixtures. The fixture automatically creates the POM instance and passes it to the test function. This keeps tests incredibly clean and handles setup/teardown elegantly.',detailedExplanation:'Fixtures in Playwright Test runner allow for dependency injection. They handle setup and teardown automatically. `test(\'my test\', async ({ loginPage }) => { await loginPage.login(); });`',simpleExplanation:'POM is putting all the details of a page (buttons, inputs) in one file. If the website changes, you fix it in one file, not in 50 tests.',realWorldExample:'A `CheckoutPage` class contains locators for credit card fields and a method `submitPayment()`. If the payment provider changes, only this class is updated.',projectExample:'In my framework, I had a `BasePage` that contained common methods (like waiting for loaders), and all other page objects extended it. Combined with custom fixtures, tests were usually under 10 lines of code.',codeCommand:'// 1. Page Object Class (LoginPage.js)\nexports.LoginPage = class LoginPage {\n  constructor(page) {\n    this.page = page;\n    this.usernameInput = page.getByLabel(\'Username\');\n    this.passwordInput = page.getByLabel(\'Password\');\n    this.loginBtn = page.getByRole(\'button\', { name: \'Login\' });\n  }\n  async login(username, password) {\n    await this.usernameInput.fill(username);\n    await this.passwordInput.fill(password);\n    await this.loginBtn.click();\n  }\n};\n\n// 2. Custom Fixture (fixtures.js)\nconst base = require(\'@playwright/test\');\nconst { LoginPage } = require(\'./LoginPage\');\n\nexports.test = base.test.extend({\n  loginPage: async ({ page }, use) => {\n    await use(new LoginPage(page));\n  }\n});\n\n// 3. Test File (login.spec.js)\nconst { test } = require(\'./fixtures\');\n\ntest(\'User can login\', async ({ loginPage }) => {\n  await loginPage.login(\'admin\', \'secret\');\n});',expectedOutput:'Clean POM implementation with dependency injection via fixtures.',followUpQ:'What is the advantage of using Fixtures over BeforeEach hooks for setup?',followUpA:'Fixtures provide Dependency Injection. Advantages: 1) **Encapsulation**: Setup and teardown are kept together in the fixture definition, not scattered in `beforeEach`/`afterEach`. 2) **Lazy Loading**: A fixture is only instantiated if the test explicitly requests it in its arguments. 3) **Parallelism**: Fixtures are isolated per worker, preventing state leakage. 4) **Composability**: Fixtures can depend on other fixtures.',commonMistake:'Using global variables or manually instantiating page objects in every test instead of leveraging fixtures.',bestPractice:'Use Playwright Test Fixtures for all Page Object instantiation and test state setup.'}
  ]
};
