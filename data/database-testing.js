/* ═══════════════════════════════════════════════════════════════
   database-testing.js — Database Testing
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['db-testing'] = {
  title: '🔍 Database Testing',
  description: 'Database testing strategies, data validation, migration testing, and JDBC integration in automation frameworks',
  questions: [
    {id:'DBT001',category:'Database Testing',topic:'Basics',subtopic:'What is DB Testing',question:'What is database testing and what do you validate?',whyAsked:'Shows end-to-end testing capability beyond UI.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'Database testing validates data integrity, CRUD operations, stored procedures, constraints, triggers, and data migration. I verify that application data is correctly stored, retrieved, and manipulated in the database.',interviewAnswer:'Database testing verifies the data layer of an application. I validate: 1) **Data Integrity** — After UI/API operations, data is correctly stored. Create user via UI → verify record in DB with correct fields. 2) **CRUD Operations** — Create, Read, Update, Delete work correctly and reflect in the database. 3) **Constraints** — NOT NULL, UNIQUE, FOREIGN KEY, CHECK constraints are enforced. Try inserting invalid data and verify rejection. 4) **Data Migration** — Source and destination data match after migration. Row counts, field values, data types all verified. 5) **Stored Procedures** — Verify logic and output of database procedures. 6) **Triggers** — Verify automatic actions fire correctly (audit log entries, calculated fields). 7) **Performance** — Query execution time for large datasets. In my framework, I use JDBC to connect to the database and run validation queries after test actions. This provides end-to-end validation — UI → API → Database.',detailedExplanation:'DB testing types: Structural (schema, constraints, indexes), Functional (CRUD, stored procs, triggers), Non-functional (performance, security, scalability).',simpleExplanation:'Database testing is like checking the warehouse after taking an order — verifying the item was actually stored correctly, not just that the cashier said it was.',realWorldExample:'Testing user registration: 1) Fill form and submit, 2) API returns success, 3) Query DB to verify: user record exists, password is hashed (not plain text), created_at timestamp is correct, default role is assigned.',projectExample:'In my e-commerce automation, after every order placement test, I query the orders table to verify: order_id, user_id, total, item_count, and status all match expected values. This caught 3 bugs that passed UI validation but stored wrong data.',codeCommand:'// JDBC Database Validation in Java\npublic class DatabaseUtil {\n  private static Connection getConnection() throws SQLException {\n    return DriverManager.getConnection(\n      Config.get("db.url"),\n      Config.get("db.user"),\n      Config.get("db.password"));\n  }\n  \n  public static String getFieldValue(String query) throws SQLException {\n    try (Connection conn = getConnection();\n         PreparedStatement stmt = conn.prepareStatement(query);\n         ResultSet rs = stmt.executeQuery()) {\n      if (rs.next()) return rs.getString(1);\n      return null;\n    }\n  }\n}\n\n// In test\n@Test\npublic void testUserCreation() {\n  // Create user via UI\n  registerPage.createUser("John", "john@test.com");\n  \n  // Verify in database\n  String dbEmail = DatabaseUtil.getFieldValue(\n    "SELECT email FROM users WHERE name = \'John\'");\n  assertEquals(dbEmail, "john@test.com");\n}',expectedOutput:'Database values match expected test data',followUpQ:'How do you handle test data cleanup in database testing?',followUpA:'Cleanup strategies: 1) @AfterMethod DELETE query to remove test data created during the test. 2) Transaction rollback — start transaction before test, rollback after (doesn\'t work across connections). 3) Dedicated test database that\'s reset before each suite. 4) Unique identifiers (timestamp/UUID in test data) for easy cleanup. 5) SQL script that runs before the suite to reset to a known state. I prefer unique identifiers + @AfterMethod cleanup for isolation.',commonMistake:'Only testing through UI without verifying database — missing data corruption bugs.',bestPractice:'Always verify critical data in the database after UI/API operations. Use parameterized queries to prevent SQL injection.'},
    {id:'DBT002',category:'Database Testing',topic:'Migration',subtopic:'Data Migration Testing',question:'How do you test data migration?',whyAsked:'Common in projects — shows experience with large-scale data validation.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'I validate: row count match, field-level data comparison, data type mapping, null handling, referential integrity, and business rule transformations between source and destination.',interviewAnswer:'Data migration testing ensures data moves correctly from source to destination: 1) **Row Count Validation** — Count records in source and destination for every table. Mismatches indicate data loss or duplication. 2) **Field-Level Comparison** — Compare actual values: `SELECT s.name, d.name FROM source.users s JOIN dest.users d ON s.old_id = d.migrated_from_id WHERE s.name != d.name`. 3) **Data Type Mapping** — Verify type conversions are correct (VARCHAR to TEXT, DATE format changes). 4) **NULL Handling** — Verify how NULLs in source are handled in destination (default values, NULLs preserved). 5) **Referential Integrity** — All foreign keys still valid after migration. No orphan records. 6) **Transformation Rules** — Business logic transformations (status code mapping, currency conversion) applied correctly. 7) **Boundary Data** — Max length strings, special characters, Unicode, very old dates. I automate these checks with SQL scripts that compare source and destination, generating exception reports for manual review.',detailedExplanation:'Migration testing phases: Pre-migration (backup, baseline counts), During migration (progress monitoring), Post-migration (validation, reconciliation), Rollback testing (verify rollback procedure works).',simpleExplanation:'Migration testing is like moving houses — after the move, you check every room to make sure nothing was lost, broken, or put in the wrong place.',realWorldExample:'Company migrates from legacy Oracle DB to PostgreSQL. QA validates: 50 tables, 10M+ records, data types correctly mapped, stored procedures converted, and all application features work with new DB.',projectExample:'I led testing for a CRM data migration (500K customer records). Created automated comparison scripts that found 150 records with corrupted phone numbers and 30 with missing address data. These were fixed before go-live.',codeCommand:'-- Row count comparison\nSELECT \'users\' as table_name,\n  (SELECT COUNT(*) FROM source.users) as source_count,\n  (SELECT COUNT(*) FROM dest.users) as dest_count,\n  (SELECT COUNT(*) FROM source.users) - (SELECT COUNT(*) FROM dest.users) as diff;\n\n-- Field-level comparison\nSELECT s.id, s.name as src_name, d.name as dest_name\nFROM source.users s\nJOIN dest.users d ON s.id = d.legacy_id\nWHERE s.name != d.name OR (s.name IS NULL AND d.name IS NOT NULL);\n\n-- Orphan records check\nSELECT * FROM dest.orders o\nWHERE NOT EXISTS (\n  SELECT 1 FROM dest.users u WHERE u.id = o.user_id\n);',expectedOutput:'Migration validation results with discrepancies highlighted',followUpQ:'What tools do you use for migration testing?',followUpA:'Tools: 1) Custom SQL scripts for comparison (most flexible), 2) ETL testing tools like QuerySurge or Informatica Data Validation, 3) Python/Java scripts for complex transformations, 4) Excel for manual reconciliation of small datasets, 5) Database comparison tools (dbForge, RedGate). In my project, I used Java + JDBC scripts for automated comparison with Allure reports for stakeholder review.',commonMistake:'Only comparing row counts without checking actual field values.',bestPractice:'Compare row counts AND field values. Test boundary data (max lengths, special chars). Always verify referential integrity.'}
  ]
};
