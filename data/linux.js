/* ═══════════════════════════════════════════════════════════════
   linux.js — Linux Commands for QA/SDET
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['linux'] = {
  title: '🐧 Linux Commands',
  description: 'Essential Linux commands for QA engineers — file operations, log analysis, process management, and server debugging',
  questions: [
    {id:'LNX001',category:'Linux',topic:'Basics',subtopic:'Essential Commands',question:'What Linux commands do you use daily as a QA engineer?',whyAsked:'Shows practical Linux experience — most CI/CD servers run Linux.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'I regularly use: cd/ls/pwd (navigation), cat/less/tail (file viewing), grep (searching), find (locating files), chmod/chown (permissions), ps/kill (processes), and tail -f (live log monitoring).',interviewAnswer:'As a QA engineer working with Linux-based CI servers, I use these commands daily: **Navigation**: `cd`, `ls -la`, `pwd`, `mkdir -p`. **File Viewing**: `cat` (small files), `less` (paginated), `tail -f` (live logs), `head -n 20` (first 20 lines). **Searching**: `grep -r "ERROR" logs/` (recursive search), `grep -i` (case-insensitive), `grep -c` (count matches). **File Operations**: `cp`, `mv`, `rm -rf`, `touch`, `find / -name "*.log"`. **Permissions**: `chmod 755 script.sh`, `chown user:group file`. **Process Management**: `ps aux | grep java`, `kill -9 PID`, `top` (resource monitor). **Networking**: `curl -X GET url`, `wget`, `netstat -tulpn`, `ping`. **Disk**: `df -h` (disk space), `du -sh *` (directory sizes). I use these most when: debugging test failures on Jenkins, analyzing server logs, managing test environments, and scripting automation tasks.',detailedExplanation:'Piping (|) chains commands: `cat log.txt | grep ERROR | wc -l` counts error lines. Redirection: > (overwrite), >> (append), 2>&1 (redirect stderr to stdout).',simpleExplanation:'Linux commands are like tools in a toolbox — each one does something specific, and chaining them together (piping) creates powerful combinations.',realWorldExample:'Jenkins test fails. SSH into the server → tail -f the log file → grep for ERROR → find the root cause → check process status → restart the service.',projectExample:'When debugging CI failures, I SSH into the Jenkins agent, use tail -f to monitor live logs, grep for specific error patterns, and check if ChromeDriver processes are stuck using ps aux | grep chrome.',codeCommand:'# Navigation and listing\nls -la                    # List with details\ncd /var/log               # Change directory\npwd                       # Print working directory\n\n# File viewing\ncat config.properties     # View entire file\ntail -f application.log   # Live log monitoring\ntail -n 100 app.log       # Last 100 lines\nless large-file.log       # Paginated view\n\n# Searching\ngrep -r "NullPointer" logs/    # Recursive search\ngrep -i "error" app.log        # Case insensitive\ngrep -c "FAILED" test-output/  # Count matches\n\n# Process management\nps aux | grep chromedriver     # Find process\nkill -9 12345                  # Force kill process\ntop                            # Resource monitor',expectedOutput:'Commands execute and return relevant output',followUpQ:'How do you use pipe and redirection?',followUpA:'Pipe (|) sends output of one command as input to another: `cat log.txt | grep ERROR | sort | uniq -c | sort -rn` — finds errors, sorts, counts unique ones, shows most frequent first. Redirection: `command > file` (overwrite), `command >> file` (append), `command 2>&1` (stderr to stdout), `command > /dev/null 2>&1` (suppress all output). I use pipes extensively for log analysis.',commonMistake:'Not using grep options effectively — always use -i for case-insensitive, -r for recursive.',bestPractice:'Master piping and grep. Learn awk and sed for advanced text processing. Use aliases for frequent commands.'},
    {id:'LNX002',category:'Linux',topic:'Log Analysis',subtopic:'Log Analysis',question:'How do you analyze log files on Linux for debugging test failures?',whyAsked:'Critical skill for debugging CI/CD failures.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'I use tail -f for live monitoring, grep to find error patterns, awk to extract specific fields, and combine commands with pipes to filter and analyze log data efficiently.',interviewAnswer:'My log analysis workflow: 1) **Live monitoring**: `tail -f application.log` — watch logs as tests run. 2) **Error search**: `grep -n "ERROR\\|EXCEPTION\\|FAIL" test.log` — find all errors with line numbers. 3) **Context**: `grep -B 5 -A 10 "NullPointerException" log.txt` — show 5 lines before and 10 after the error. 4) **Time range**: `awk \'/2024-01-15 14:00/,/2024-01-15 14:30/\' app.log` — extract logs for a time window. 5) **Count errors**: `grep -c "ERROR" app.log` — count occurrences. 6) **Unique errors**: `grep "ERROR" app.log | sort | uniq -c | sort -rn` — most frequent errors first. 7) **Multiple files**: `grep -r "timeout" logs/` — search across all log files. 8) **Compressed logs**: `zgrep "error" app.log.gz` — search in gzipped logs. I combine these for root cause analysis — find the timestamp of failure, check surrounding logs, identify the actual exception, and trace back to the trigger.',detailedExplanation:'grep -B (before), -A (after), -C (context = both). awk for field extraction: awk \'{print $1, $4}\' extracts 1st and 4th fields. sed for text transformation: sed \'s/old/new/g\' replaces text.',simpleExplanation:'Log analysis is like detective work — you follow clues (error messages), look at the timeline (timestamps), and piece together what went wrong.',realWorldExample:'Test failed at 2:30 AM in Jenkins. SSH in → grep for the test name → find the timestamp → check application logs around that time → find "Database connection timeout" → root cause identified.',projectExample:'When our regression suite had 5 random failures, I analyzed Jenkins logs using grep to find the common error pattern — all 5 had "StaleElementReferenceException" within 2 minutes, pointing to a deployment that happened during the test run.',codeCommand:'# Find errors with context\ngrep -B 5 -A 10 "NullPointerException" app.log\n\n# Count error types\ngrep "ERROR" app.log | awk \'{print $NF}\' | sort | uniq -c | sort -rn\n\n# Extract time range\nawk \'/14:00:00/,/14:30:00/\' app.log\n\n# Monitor specific pattern in real-time\ntail -f app.log | grep --color "ERROR\\|WARN"\n\n# Find large log files\nfind /var/log -name "*.log" -size +100M\n\n# Compress old logs\ngzip -k old-app.log',expectedOutput:'Relevant log entries extracted for analysis',followUpQ:'How do you handle log rotation?',followUpA:'Log rotation prevents disk space exhaustion. Linux uses logrotate utility configured in /etc/logrotate.d/. Settings: rotate 7 (keep 7 files), daily/weekly (rotation frequency), compress (gzip old logs), maxsize 100M (rotate at 100MB). For test logs, I configure our framework to: 1) Create date-stamped log files, 2) Archive logs older than 7 days, 3) Clean up in Jenkins post-build step.',commonMistake:'Searching entire log files instead of narrowing down by timestamp first.',bestPractice:'Always narrow by timestamp first, then search for errors. Use -B/-A for context. Learn awk for field extraction.'},
    {id:'LNX003',category:'Linux',topic:'Shell Scripting',subtopic:'Shell Basics',question:'Have you written shell scripts for test automation?',whyAsked:'Automation extends beyond Selenium — scripting shows versatility.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'Yes, I\'ve written bash scripts for environment setup, killing orphaned browser processes, archiving test reports, and automating repetitive tasks on CI servers.',interviewAnswer:'I write shell scripts for several automation tasks: 1) **Environment Setup** — Script that installs dependencies, sets environment variables, and configures the test environment on new machines. 2) **Process Cleanup** — Kills orphaned ChromeDriver/Chrome processes before test runs: `pkill -f chromedriver; pkill -f chrome`. 3) **Report Archiving** — Archives test reports with timestamps, compresses them, and uploads to shared storage. 4) **Health Checks** — Pre-test script that verifies application is up, database is accessible, and required services are running. 5) **Data Setup** — Script that restores test database from a backup before regression runs. These scripts are called from Jenkins pipeline stages — `sh \'./scripts/cleanup.sh\'` before test execution and `sh \'./scripts/archive-reports.sh\'` after.',detailedExplanation:'Shell script basics: #!/bin/bash (shebang), variables (VAR="value"), conditionals (if/then/fi), loops (for/while), functions, exit codes ($?), command substitution ($(command)).',simpleExplanation:'Shell scripts are like recipes for the computer — a list of commands that run automatically in sequence, handling different situations with conditions and loops.',realWorldExample:'Before every test run on Jenkins: a shell script checks if the app is responding (curl health endpoint), kills any leftover browser processes, clears temp files, and sets up the test database.',projectExample:'My cleanup.sh script saved 30 minutes per week of manual work — it automatically kills orphaned chromedriver processes, clears browser cache, archives old reports, and sets up fresh test data.',codeCommand:'#!/bin/bash\n# pre-test-setup.sh\n\n# Kill orphaned browser processes\necho "Cleaning up old browser processes..."\npkill -f chromedriver 2>/dev/null\npkill -f "chrome --headless" 2>/dev/null\nsleep 2\n\n# Check if application is up\nAPP_URL="https://qa.example.com/health"\nHTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" $APP_URL)\nif [ "$HTTP_CODE" != "200" ]; then\n  echo "ERROR: Application is not responding! HTTP $HTTP_CODE"\n  exit 1\nfi\necho "Application is UP"\n\n# Set environment variables\nexport BROWSER=chrome\nexport ENV=qa\nexport HEADLESS=true\n\n# Run tests\nmvn clean test -DsuiteFile=regression.xml\nTEST_EXIT_CODE=$?\n\n# Archive reports\nTIMESTAMP=$(date +%Y%m%d_%H%M%S)\ncp -r target/allure-results reports/run_$TIMESTAMP\n\nexit $TEST_EXIT_CODE',expectedOutput:'Script executes tasks sequentially',followUpQ:'How do you handle errors in shell scripts?',followUpA:'Error handling: 1) `set -e` — exit immediately on any error. 2) `set -o pipefail` — catch errors in piped commands. 3) Check exit codes: `if [ $? -ne 0 ]; then echo "Failed"; exit 1; fi`. 4) `trap` for cleanup on exit: `trap cleanup EXIT`. 5) Validate inputs at the beginning. I always start my scripts with `set -euo pipefail` for strict error handling.',commonMistake:'Not handling errors — script continues after a failure, causing cascading issues.',bestPractice:'Use set -e for strict mode. Log all actions. Validate prerequisites before proceeding. Use trap for cleanup.'},
    {id:'LNX004',category:'Linux',topic:'Commands',subtopic:'File Permissions',question:'Explain Linux file permissions (chmod, chown).',whyAsked:'Important for managing scripts and test files on Linux servers.',difficulty:2,importance:'good',interviewType:'Technical',thirtySecAnswer:'Linux permissions have three groups: Owner, Group, Others. Each has Read (4), Write (2), Execute (1) permissions. chmod 755 = owner rwx, group r-x, others r-x. chown changes file ownership.',interviewAnswer:'Linux file permissions control access at three levels: **Owner**, **Group**, **Others**. Each level has three permissions: **Read (r=4)**, **Write (w=2)**, **Execute (x=1)**. Reading `ls -l` output: `-rwxr-xr-x` means: owner can read/write/execute, group can read/execute, others can read/execute. Numeric notation: 755 = 7(rwx for owner) + 5(r-x for group) + 5(r-x for others). Common permissions in testing: `chmod 755 script.sh` — make script executable, `chmod 644 config.properties` — readable by all, writable only by owner, `chmod 600 credentials.txt` — owner only. `chown` changes ownership: `chown jenkins:jenkins test-report.html`. I deal with permissions when: test scripts aren\'t executing on Jenkins (need +x), log files can\'t be written (need write permission), or security requirements restrict config file access.',detailedExplanation:'Symbolic chmod: chmod u+x file (add execute for user), chmod g-w file (remove write for group), chmod o=r file (set others to read only). Special permissions: SUID (4), SGID (2), Sticky (1).',simpleExplanation:'File permissions are like access levels in a building — some doors are open to everyone (read), some need a key card (write), and some need a special badge (execute).',realWorldExample:'Jenkins job fails with "Permission denied" when trying to execute a test script. Solution: chmod +x script.sh or chmod 755 script.sh makes it executable.',projectExample:'On our Jenkins server, I configured test report directories with chmod 755 so that all team members can read reports, but only the Jenkins user can write to them. Credentials files are chmod 600.',codeCommand:'# View permissions\nls -la\n# -rwxr-xr-x 1 jenkins jenkins 1024 Jan 1 script.sh\n\n# Numeric chmod\nchmod 755 script.sh    # rwxr-xr-x (common for scripts)\nchmod 644 config.txt   # rw-r--r-- (common for files)\nchmod 600 secrets.txt  # rw------- (owner only)\n\n# Symbolic chmod\nchmod +x script.sh     # Add execute for all\nchmod u+w file.txt     # Add write for owner\nchmod go-w file.txt    # Remove write for group/others\n\n# Change ownership\nchown jenkins:jenkins report.html\nchown -R user:group directory/   # Recursive',expectedOutput:'Permissions changed successfully',followUpQ:'What happens if you try to execute a script without execute permission?',followUpA:'You get "Permission denied" error. Solution: chmod +x script.sh to add execute permission. Alternatively, run it with the interpreter directly: bash script.sh (this doesn\'t need execute permission because bash is the executable, not the script).',commonMistake:'Using chmod 777 (everything open) — major security risk.',bestPractice:'Follow principle of least privilege. Scripts: 755. Config files: 644. Secrets: 600. Never use 777.'}
  ]
};
