/* ═══════════════════════════════════════════════════════════════
   api-scenarios.js — API Testing Scenarios
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['api-scenarios'] = {
  title: '🎭 API Scenarios',
  description: 'Real-world API testing scenarios, edge cases, and complex integration patterns',
  questions: [
    {id:'APIS001',category:'API Testing',topic:'Scenarios',subtopic:'Data Flow',question:'How do you test an API that requires data from another API?',whyAsked:'Tests ability to handle complex, chained API scenarios.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'I use API chaining. I extract the required value (like an ID or token) from the first API response using JsonPath, store it in a variable, and pass it as a parameter, header, or body field to the second API request.',interviewAnswer:'This is called API Chaining, and it\'s very common. Scenario: To create an Order (API 2), I first need a UserID (from API 1). Approach: 1) Call POST /users to create a user. 2) Assert 201 Created. 3) Extract the `userId` from the response body using JsonPath (`response.jsonPath().getString("id")`). 4) Store this in a class-level or thread-local variable. 5) Call POST /orders, passing the extracted `userId` in the request body. 6) Assert the order is created successfully for that user. In my framework, I manage state between chained requests using a TestContext or ScenarioContext object, which allows me to pass data between steps safely, especially in parallel test execution.',detailedExplanation:'For complex workflows, consider setting up prerequisites directly in the DB to make tests independent. But true integration testing requires API chaining. Handle cleanup carefully (delete order, then delete user).',simpleExplanation:'It\'s like a relay race. The first API runs and passes the baton (data, like an ID) to the second API so it can do its job.',realWorldExample:'Checkout Flow: 1. POST /login (get token), 2. GET /products (get product ID), 3. POST /cart (add product), 4. POST /checkout (create order). Each step depends on the previous.',projectExample:'I automated a 5-step booking flow where the transaction ID from the booking API had to be passed to the payment API, and the payment confirmation ID had to be passed to the ticketing API.',codeCommand:'// API Chaining in REST Assured\n// Step 1: Create User and extract ID\nint newUserId = given()\n  .body(userPayload)\n.when()\n  .post("/api/users")\n.then()\n  .statusCode(201)\n  .extract().path("id");\n\n// Step 2: Use ID in the next request\ngiven()\n  .body("{ \\"userId\\": " + newUserId + ", \\"amount\\": 100 }")\n.when()\n  .post("/api/orders")\n.then()\n  .statusCode(201)\n  .body("status", equalTo("PENDING"));',expectedOutput:'Data successfully passed between API calls',followUpQ:'What if the first API fails? How do you handle the dependency?',followUpA:'If the first API fails, the dependent API tests will also fail (cascading failures). To mitigate this: 1) Use TestNG dependencies (`dependsOnMethods`) so the second test is skipped rather than marked as failed. 2) If testing the second API in isolation, use mocked data or directly insert prerequisites via database setup instead of relying on the UI/API. But for an E2E flow test, a failure in step 1 SHOULD fail the scenario.',commonMistake:'Hardcoding IDs instead of extracting them dynamically, leading to flaky tests.',bestPractice:'Extract and pass variables dynamically. Ensure parallel execution safety (ThreadLocal context) if storing variables.'},
    {id:'APIS002',category:'API Testing',topic:'Scenarios',subtopic:'Pagination',question:'How do you test an API endpoint that returns paginated results?',whyAsked:'Pagination is a standard API feature that has specific edge cases.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'I validate the default page size, test the `page` and `limit` query parameters, verify total count accuracy, and check boundary conditions like requesting a page that doesn\'t exist or a limit of 0.',interviewAnswer:'Testing pagination involves verifying the query parameters (e.g., `?page=1&size=20`) and the response metadata. My test strategy: 1) **Default Behavior**: Call without parameters, verify default size (e.g., 20 items) and correct metadata (totalElements, totalPages). 2) **Custom Limit**: Pass `size=5`, verify exactly 5 items are returned. 3) **Navigation**: Test `page=1` and `page=2`. Verify the items in page 2 are different from page 1. 4) **Boundaries**: Request `size=0`, `size=-1`, or `size=1000` (exceeding max limit) — verify proper 400 Bad Request or fallback to max allowed. Request a page number beyond the last page — should return 200 OK with an empty array. 5) **Data Integrity**: The total count in metadata must match the actual number of records in the database.',detailedExplanation:'API responses usually contain a `meta` or `page` object alongside the `data` array. e.g., { "data": [...], "meta": { "page": 1, "size": 20, "total": 105 } }',simpleExplanation:'Testing pagination is like testing a book. You check if page 1 has the right number of words, if page 2 has different words than page 1, and what happens if you ask for page 100 in a 50-page book.',realWorldExample:'GET /api/products?page=2&limit=10. Expected: Returns products 11-20, metadata shows current page is 2.',projectExample:'I found a bug where requesting `limit=999999` crashed the backend because it tried to load the entire database into memory. We implemented a max limit of 100 per request.',codeCommand:'// Testing Pagination\ngiven()\n  .queryParam("page", 2)\n  .queryParam("size", 5)\n.when()\n  .get("/api/products")\n.then()\n  .statusCode(200)\n  .body("data.size()", equalTo(5))\n  .body("meta.currentPage", equalTo(2))\n  .body("meta.totalElements", greaterThan(5));\n\n// Testing Boundary\ngiven()\n  .queryParam("page", 9999) // Non-existent page\n.when()\n  .get("/api/products")\n.then()\n  .statusCode(200)\n  .body("data", empty());',expectedOutput:'Pagination parameters are respected and metadata is accurate',followUpQ:'How would you test performance of a paginated API?',followUpA:'I would test the API response time with the maximum allowed page size (e.g., limit=100) and also request a very deep page (e.g., page=10000). Deep pagination can cause severe performance issues in databases (like SQL `OFFSET`) because it has to scan and discard thousands of rows. If deep pagination is slow, I\'d suggest the dev team implement cursor-based pagination.',commonMistake:'Only checking that an array is returned, without verifying the metadata or array size.',bestPractice:'Always verify array size matches the requested limit, and test boundary conditions (0, negative, very large numbers).'}
  ]
};
