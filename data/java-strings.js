/* java-strings.js */
var defined_sections = defined_sections || {};
defined_sections['java-strings'] = {
  title: '🔤 Strings & Exception Handling',
  description: 'Java String operations and Exception handling for automation',
  questions: [
    {id:'JS001',category:'Java',topic:'Strings',subtopic:'String Immutability',question:'Why are Strings immutable in Java?',whyAsked:'Fundamental Java concept frequently asked.',difficulty:2,importance:'important',interviewType:'Technical',thirtySecAnswer:'Strings are immutable for security (credentials can\'t be modified), thread-safety (shared without synchronization), and performance (String pool caching). Once created, a String object cannot be changed.',interviewAnswer:'String immutability means once a String object is created, its value cannot be changed. Any operation like concat, replace, or substring creates a NEW String object. This matters in automation: 1) **Security** — String passwords in memory can\'t be modified by malicious code (though they should be cleared). 2) **String Pool** — Java reuses String literals from a pool, saving memory. "hello" used 100 times = one object. 3) **Thread Safety** — Immutable Strings can be shared across threads without synchronization. In my automation code, when building dynamic strings (XPath, URLs), I use StringBuilder for efficiency instead of concatenating Strings in a loop.',detailedExplanation:'String pool: JVM maintains a pool of unique Strings. "abc" == "abc" is true (same pool object). new String("abc") creates a new heap object. StringBuilder: mutable, not thread-safe, fast. StringBuffer: mutable, thread-safe, slower.',simpleExplanation:'A String is like a printed document — you can\'t erase and rewrite it. You have to print a new one.',realWorldExample:'Building dynamic XPath in a loop: each iteration creates a new String object. Using StringBuilder avoids this overhead.',projectExample:'In my framework, URL builder uses StringBuilder: baseUrl + "/api/" + endpoint + "?param=" + value. StringBuilder is much more efficient than String concatenation.',codeCommand:'// String immutability\nString s1 = "Hello";\nString s2 = s1.concat(" World"); // s1 is UNCHANGED\nSystem.out.println(s1); // "Hello"\nSystem.out.println(s2); // "Hello World"\n\n// StringBuilder for dynamic strings (automation)\nStringBuilder xpath = new StringBuilder();\nxpath.append("//div[@class=\'");\nxpath.append(className);\nxpath.append("\']//span[text()=\'");\nxpath.append(labelText);\nxpath.append("\']");\ndriver.findElement(By.xpath(xpath.toString()));\n\n// Common String methods\nString url = driver.getCurrentUrl();\nboolean contains = url.contains("/dashboard");\nboolean starts = url.startsWith("https://");\nString lower = url.toLowerCase();\nString[] parts = url.split("/");',expectedOutput:'String operations create new objects; StringBuilder modifies in place',followUpQ:'When should you use StringBuilder vs String?',followUpA:'Use String for: simple assignments, comparisons, method parameters. Use StringBuilder for: building strings in loops, concatenating multiple parts (URLs, XPath, queries). Rule of thumb: if you\'re concatenating inside a loop, use StringBuilder.',seniorFollowUp:'How do you handle String comparison in assertions?',seniorAnswer:'For test assertions: 1) assertEquals(actual, expected) for exact match, 2) assertTrue(actual.contains(expected)) for partial match, 3) assertTrue(actual.equalsIgnoreCase(expected)) for case-insensitive, 4) Pattern.matches(regex, actual) for pattern matching. I also trim whitespace before comparison: actual.trim().equals(expected.trim()).',commonMistake:'Using == instead of .equals() for String comparison.',bestPractice:'Always use .equals() for Strings. Use StringBuilder for concatenation in loops.'},
    {id:'JS002',category:'Java',topic:'Exceptions',subtopic:'Checked vs Unchecked',question:'What is the difference between checked and unchecked exceptions?',whyAsked:'Core Java concept used throughout automation code.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'Checked exceptions must be handled at compile-time (IOException, SQLException). Unchecked exceptions occur at runtime (NullPointerException, ArrayIndexOutOfBoundsException). Selenium exceptions are mostly unchecked.',interviewAnswer:'**Checked exceptions** are verified at compile time — you must either catch them or declare them with throws. Examples: IOException (file operations), SQLException (database), InterruptedException (Thread.sleep). The compiler forces you to handle them. **Unchecked exceptions** (RuntimeException subclasses) happen at runtime — handling is optional. Examples: NullPointerException, ArrayIndexOutOfBoundsException, and most Selenium exceptions (NoSuchElementException, StaleElementReferenceException). In my automation code, Selenium exceptions are unchecked, so I don\'t need try-catch for every findElement. But I use try-catch strategically: for file operations in my framework utilities, for database connections in DB validation, and for JSON parsing in API tests. I also use custom exceptions: `throw new FrameworkException("Login failed: invalid credentials")` for meaningful error messages.',detailedExplanation:'Exception hierarchy: Throwable → Error (JVM, don\'t catch) + Exception → IOException (checked) + RuntimeException → NullPointerException (unchecked). Selenium exceptions extend RuntimeException = unchecked.',simpleExplanation:'Checked = the compiler reminds you to wear a seatbelt (must handle). Unchecked = you might slip on ice (happens at runtime, no compiler warning).',realWorldExample:'Reading test data from CSV: IOException is checked — must handle. findElement returning null: NullPointerException is unchecked — happens at runtime.',projectExample:'In my framework, file reading utilities handle IOException with proper error messages. Selenium operations are wrapped with explicit waits instead of try-catch.',codeCommand:'// Checked Exception - MUST handle\ntry {\n    BufferedReader reader = new BufferedReader(new FileReader("testdata.csv"));\n    String line = reader.readLine();\n} catch (IOException e) {\n    logger.error("Failed to read test data", e);\n}\n\n// Unchecked Exception - optional handling\ntry {\n    driver.findElement(By.id("missing")).click();\n} catch (NoSuchElementException e) {\n    // Handle gracefully\n}\n\n// Custom exception\npublic class FrameworkException extends RuntimeException {\n    public FrameworkException(String message) {\n        super(message);\n    }\n}\n\n// throw vs throws\npublic void readData() throws IOException { // declares checked exception\n    throw new IOException("File not found"); // throws the exception\n}',expectedOutput:'Checked exceptions enforced at compile time; unchecked at runtime',followUpQ:'What is the difference between throw and throws?',followUpA:'throw: actually throws an exception object — `throw new Exception("error")`. throws: declares that a method might throw an exception — `public void method() throws IOException`. throw is used inside method body, throws is in method signature. throw creates an exception instance, throws just warns callers.',seniorFollowUp:'How does your framework handle exceptions?',seniorAnswer:'1) Test failures use TestNG assertions (not try-catch) for clean reporting, 2) Framework utilities use checked exception handling with logging, 3) Custom FrameworkException for framework-specific errors, 4) Global exception handler via TestNG ITestListener captures screenshots on any failure, 5) Retry mechanism catches specific exceptions (StaleElement) and retries, 6) Never swallow exceptions silently — always log or rethrow.',commonMistake:'Wrapping every Selenium call in try-catch and swallowing exceptions.',bestPractice:'Let test failures propagate naturally for accurate reporting. Use try-catch only for expected, recoverable scenarios.'}
  ]
};
