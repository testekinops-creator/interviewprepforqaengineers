/* ═══════════════════════════════════════════════════════════════
   rest-assured.js — REST Assured Framework
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['rest-assured'] = {
  title: '🔗 REST Assured',
  description: 'REST Assured library for Java-based API test automation — given/when/then syntax, serialization, and framework design',
  questions: [
    {id:'RA001',category:'REST Assured',topic:'Basics',subtopic:'What is REST Assured',question:'What is REST Assured and why do you use it?',whyAsked:'Primary API automation tool for Java-based SDET roles.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'REST Assured is a Java library for testing REST APIs. It provides a BDD-style given/when/then syntax for sending HTTP requests and validating responses, integrating seamlessly with TestNG and Maven.',interviewAnswer:'REST Assured is a Java library that simplifies API testing with a fluent, BDD-style syntax: given() (preconditions — headers, body, auth) → when() (HTTP action — get, post, put, delete) → then() (assertions — status, body, headers). I use it because: 1) **Java integration** — fits naturally into our Java/TestNG/Maven framework. 2) **Readable syntax** — given-when-then makes tests self-documenting. 3) **Powerful assertions** — Hamcrest matchers for body validation, JsonPath for navigating JSON, XML path for XML. 4) **Serialization** — automatic Java object ↔ JSON conversion. 5) **Authentication support** — Basic, OAuth, Bearer built-in. 6) **Logging** — log().all() for debugging. In my framework, I combine REST Assured with TestNG for test execution, Maven for build management, and Allure for reporting. I have 200+ API tests covering all microservices.',detailedExplanation:'REST Assured uses Hamcrest matchers for assertions and JsonPath for JSON navigation. It supports multiple content types: JSON, XML, multipart. Response can be extracted as POJO using deserialization.',simpleExplanation:'REST Assured is like a conversation with an API: "Given I have these credentials, When I ask for user data, Then I should get a 200 response with the user name."',realWorldExample:'Testing user creation: given().body(userJson).post("/users").then().statusCode(201).body("id", notNullValue()). Clean, readable, and complete in one chain.',projectExample:'My API test framework uses REST Assured as the core library, with a RequestSpecBuilder for common configuration (base URL, auth, headers) shared across all tests.',codeCommand:'// Basic REST Assured test\nimport static io.restassured.RestAssured.*;\nimport static org.hamcrest.Matchers.*;\n\n@Test\npublic void testGetUser() {\n  given()\n    .baseUri("https://api.example.com")\n    .header("Authorization", "Bearer " + token)\n  .when()\n    .get("/api/users/1")\n  .then()\n    .statusCode(200)\n    .body("name", equalTo("John Doe"))\n    .body("email", containsString("@"))\n    .body("roles", hasSize(2))\n    .time(lessThan(2000L));\n}',expectedOutput:'All assertions pass',followUpQ:'How do you set up a base configuration for all tests?',followUpA:'I use RequestSpecBuilder in a @BeforeClass method: `RequestSpecification spec = new RequestSpecBuilder().setBaseUri(baseUrl).addHeader("Authorization","Bearer "+token).setContentType(ContentType.JSON).build();` Then in tests: `given().spec(spec).when().get("/users")`. This avoids repeating base URL, auth, and headers in every test. I also set RestAssured.baseURI and RestAssured.basePath as static defaults.',commonMistake:'Repeating base URL and auth headers in every single test method.',bestPractice:'Use RequestSpecBuilder for shared configuration. Use ResponseSpecBuilder for common assertions.'},
    {id:'RA002',category:'REST Assured',topic:'Advanced',subtopic:'Serialization',question:'How do you handle request/response serialization in REST Assured?',whyAsked:'Shows advanced understanding of API test design.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'REST Assured supports automatic serialization (Java POJO ↔ JSON) using Jackson or Gson. I create POJO classes for request/response bodies and pass them directly to body() method.',interviewAnswer:'Serialization in REST Assured works two ways: 1) **Serialization (POJO → JSON)** — Create a Java class matching the JSON structure, set values, pass to body(). REST Assured uses Jackson/Gson to convert it to JSON. 2) **Deserialization (JSON → POJO)** — Extract the response as a Java object: `User user = response.as(User.class)`. Benefits over raw JSON strings: type safety, IDE autocompletion, easier maintenance, reusable across tests. In my framework, I have POJO classes in a `models` package for every API entity — User, Product, Order, etc. I use Lombok for reducing boilerplate (@Data, @Builder annotations). For complex nested structures, I create nested POJOs. For dynamic test data, I use the Builder pattern.',detailedExplanation:'Jackson annotations: @JsonProperty (field name mapping), @JsonIgnore (exclude field), @JsonInclude(NON_NULL) (skip null fields). Lombok: @Data (getters/setters), @Builder (builder pattern), @NoArgsConstructor, @AllArgsConstructor.',simpleExplanation:'Serialization is like translating between languages — your Java objects become JSON for the API, and JSON responses become Java objects for your tests.',realWorldExample:'Instead of building JSON strings manually: body("{\"name\":\"John\"}"), you create User user = new User("John"); body(user). Much cleaner and type-safe.',projectExample:'My API framework has 15+ POJO classes with Lombok annotations. Tests create objects using Builder pattern: User.builder().name("John").email("john@test.com").build() and pass to REST Assured.',codeCommand:'// POJO class with Lombok\n@Data @Builder @AllArgsConstructor @NoArgsConstructor\npublic class User {\n  private String name;\n  private String email;\n  private String role;\n}\n\n// Serialization — POJO to JSON\nUser newUser = User.builder()\n  .name("John Doe")\n  .email("john@test.com")\n  .role("admin")\n  .build();\n\ngiven().body(newUser).post("/api/users")\n  .then().statusCode(201);\n\n// Deserialization — JSON to POJO\nUser user = given().get("/api/users/1")\n  .then().extract().as(User.class);\nassertEquals(user.getName(), "John Doe");',expectedOutput:'Java objects serialized/deserialized correctly',followUpQ:'What is the difference between Jackson and Gson?',followUpA:'Both are JSON libraries for Java. Jackson: Faster, more features, default in REST Assured and Spring. Supports annotations like @JsonProperty, @JsonIgnore. Gson: Simpler API by Google, fewer dependencies. I use Jackson because it\'s the default in REST Assured and Spring Boot, and it handles complex nested structures well.',commonMistake:'Building JSON strings manually instead of using POJO serialization.',bestPractice:'Create POJO classes for all request/response bodies. Use Lombok to reduce boilerplate. Use Builder pattern for flexible test data creation.'},
    {id:'RA003',category:'REST Assured',topic:'Advanced',subtopic:'JsonPath Extraction',question:'How do you extract values from API responses using JsonPath?',whyAsked:'Essential skill for data-driven API testing and chaining.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'JsonPath navigates JSON response using dot notation and array indices. response.jsonPath().getString("user.name") extracts nested values. Used for extracting IDs, tokens, and data for subsequent requests.',interviewAnswer:'JsonPath in REST Assured allows extracting values from JSON responses using path expressions: 1) **Simple field**: `jsonPath().getString("name")` 2) **Nested object**: `jsonPath().getString("address.city")` 3) **Array element**: `jsonPath().getString("users[0].name")` 4) **Array size**: `jsonPath().getList("users").size()` 5) **Filtering**: `jsonPath().getString("users.findAll{it.age > 25}.name")` — Groovy GPath syntax. 6) **All values**: `jsonPath().getList("users.name")` — extracts all names. I use JsonPath extensively for: extracting IDs from POST responses for subsequent requests, extracting tokens from auth responses, validating specific fields in complex nested responses, and building data-driven test flows where one API\'s output feeds the next.',detailedExplanation:'JsonPath in REST Assured uses Groovy GPath syntax (different from Jayway JsonPath). Supports: dot notation, array indexing, findAll/find with closures, collect, sum, min, max.',simpleExplanation:'JsonPath is like giving directions to find specific information in a JSON response — "go to the users array, pick the first item, get the name field."',realWorldExample:'Create a user (POST) → extract userId from response → use userId in GET /users/{userId} → validate the user was created correctly.',projectExample:'In my test flow: 1) POST /auth/login → extract token, 2) POST /orders → extract orderId, 3) GET /orders/{orderId} → validate order status, 4) PUT /orders/{orderId}/status → update to "shipped".',codeCommand:'// Extract values from response\nResponse response = given().get("/api/users");\n\n// Simple extraction\nString name = response.jsonPath().getString("name");\nint id = response.jsonPath().getInt("id");\n\n// Nested extraction\nString city = response.jsonPath().getString("address.city");\n\n// Array extraction\nList<String> names = response.jsonPath().getList("users.name");\nString firstName = response.jsonPath().getString("users[0].name");\n\n// Groovy GPath filtering\nList<String> activeUsers = response.jsonPath()\n  .getList("users.findAll{it.status==\'active\'}.name");\n\n// Extract and chain\nint userId = given().body(newUser).post("/api/users")\n  .then().extract().path("id");\ngiven().get("/api/users/" + userId)\n  .then().statusCode(200);',expectedOutput:'Values extracted correctly from JSON response',followUpQ:'What is the difference between path() and jsonPath()?',followUpA:'path() is a shorthand that returns the value directly: `response.path("name")` returns String/int/etc. jsonPath() returns a JsonPath object that provides typed extraction methods: `response.jsonPath().getString("name")`, `getInt()`, `getList()`, `getMap()`. jsonPath() is more powerful and type-safe. I use path() for simple extractions and jsonPath() for complex operations like filtering and list extraction.',commonMistake:'Hardcoding array indices without validating array size first.',bestPractice:'Use typed extraction methods (getString, getInt, getList). Validate array size before accessing indices. Use GPath filtering for complex queries.'},
    {id:'RA004',category:'REST Assured',topic:'Framework',subtopic:'API Test Framework',question:'How do you design an API test automation framework using REST Assured?',whyAsked:'Senior-level question — shows framework design skills.',difficulty:4,importance:'must',interviewType:'Technical',thirtySecAnswer:'My API framework has: Base test class (config, auth), API endpoint classes (encapsulate endpoints), POJO models (request/response), test data utilities, custom assertions, and Allure reporting. Organized in a modular, maintainable structure.',interviewAnswer:'My API test framework architecture: **1) BaseAPITest** — @BeforeClass sets up RestAssured baseURI, basePath, authentication. @AfterClass for cleanup. Common utilities. **2) API Endpoint Classes** — Each endpoint has a class encapsulating all operations. UserAPI has createUser(), getUser(), updateUser(), deleteUser() methods. Returns Response objects for flexible assertion in tests. **3) POJO Models** — Request/response body classes with Lombok. User, Product, Order models. **4) Test Data** — TestDataGenerator creates random test data using Faker library. DataProvider methods for data-driven tests. **5) Configuration** — Properties file for base URL, credentials, timeouts per environment. **6) Custom Assertions** — Reusable assertion methods: assertSuccess(), assertCreated(), validateSchema(). **7) Reporting** — Allure integration with REST Assured filters for logging requests/responses. **8) Utilities** — TokenManager for auth, RetryAnalyzer for flaky tests, SchemaValidator. This design follows SOLID principles — each class has a single responsibility, making it easy to maintain and extend.',detailedExplanation:'Project structure: src/main/java (api/, models/, config/, utils/), src/test/java (tests/, testdata/), src/test/resources (schemas/, config.properties, testng.xml). Maven modules for multi-project setup.',simpleExplanation:'The API framework is like a well-organized kitchen — separate stations for prep (test data), cooking (API calls), plating (assertions), and serving (reports).',realWorldExample:'Adding tests for a new microservice: 1) Create POJO models, 2) Create API endpoint class with CRUD methods, 3) Write test class extending BaseAPITest, 4) Add test data, 5) Run — all infrastructure (auth, logging, reporting) is already there.',projectExample:'My framework covers 5 microservices with 250+ API tests. New endpoint coverage takes ~1 hour (create endpoint class + test class). Jenkins runs the full suite in 5 minutes with detailed Allure reports.',codeCommand:'// BaseAPITest.java\npublic class BaseAPITest {\n  protected static String token;\n  @BeforeClass\n  public void setup() {\n    RestAssured.baseURI = Config.get("base.url");\n    token = AuthManager.getToken();\n  }\n}\n\n// UserAPI.java (endpoint class)\npublic class UserAPI {\n  public static Response createUser(User user) {\n    return given()\n      .header("Authorization","Bearer "+token)\n      .body(user)\n      .post("/api/users");\n  }\n  public static Response getUser(int id) {\n    return given()\n      .header("Authorization","Bearer "+token)\n      .get("/api/users/"+id);\n  }\n}\n\n// UserTests.java\npublic class UserTests extends BaseAPITest {\n  @Test\n  public void testCreateUser() {\n    User user = TestData.randomUser();\n    UserAPI.createUser(user)\n      .then().statusCode(201)\n      .body("name",equalTo(user.getName()));\n  }\n}',expectedOutput:'Modular, maintainable API test framework',followUpQ:'How do you handle data cleanup in API tests?',followUpA:'Cleanup strategies: 1) @AfterMethod deletes created resources using stored IDs, 2) Dedicated cleanup API endpoint that resets test data, 3) Database cleanup script that runs before test suite, 4) Test data with unique identifiers (timestamp/UUID) to avoid conflicts, 5) Independent tests — each test creates its own data and cleans up. I prefer option 1+4: create unique test data, store resource IDs, delete in teardown.',commonMistake:'Putting all API calls directly in test methods without abstraction.',bestPractice:'Encapsulate API calls in endpoint classes. Use POJO models. Keep tests focused on assertions only.'}
  ]
};
