/* selenium-exceptions.js */
var defined_sections = defined_sections || {};
defined_sections['selenium-exceptions'] = {
  title: '🚨 Selenium Exceptions',
  description: 'All major Selenium exceptions with root causes, solutions, and prevention',
  questions: [
    {id:'EX001',category:'Selenium',topic:'Exceptions',subtopic:'NoSuchElementException',question:'What causes NoSuchElementException and how do you fix it?',whyAsked:'Most common Selenium exception — debugging is a critical skill.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'Thrown when findElement cannot locate an element. Causes: wrong locator, element not loaded yet, element inside iframe, element dynamically generated. Fix: verify locator, add explicit wait, check iframe context.',interviewAnswer:'NoSuchElementException is the most common Selenium exception. Root causes I\'ve encountered: 1) **Wrong locator** — typo or incorrect attribute. Fix: verify in DevTools. 2) **Element not loaded** — page hasn\'t rendered yet. Fix: use Explicit Wait. 3) **Element in iframe** — Selenium can\'t see inside iframes. Fix: switchTo().frame() first. 4) **Dynamic content** — element generated by JavaScript after page load. Fix: wait for AJAX. 5) **Element removed from DOM** — no longer exists. Fix: verify flow. 6) **Wrong page** — test navigated somewhere unexpected. Fix: add URL verification. In my framework, I have a custom exception handler that captures a screenshot, page URL, and page source when this exception occurs — making debugging much faster.',detailedExplanation:'Debugging steps: 1) Open browser DevTools, 2) Verify locator finds the element, 3) Check if element is inside iframe, 4) Check if element loads dynamically, 5) Verify the page URL is correct, 6) Check browser console for JS errors.',simpleExplanation:'The element doesn\'t exist where Selenium is looking — like looking for a book on the wrong shelf.',realWorldExample:'Login button not found because a cookie consent banner is overlaying the page and the DOM structure changed.',projectExample:'In Salesforce, this frequently occurs because elements are inside Lightning iframes. My first debugging step is always checking for iframes.',codeCommand:'// WRONG - No wait\ndriver.findElement(By.id("dynamicElement")); // NoSuchElementException!\n\n// CORRECT - With wait\nWebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(10));\nWebElement el = wait.until(\n    ExpectedConditions.presenceOfElementLocated(By.id("dynamicElement"))\n);\n\n// Check iframe context\ntry {\n    driver.findElement(By.id("innerElement"));\n} catch (NoSuchElementException e) {\n    driver.switchTo().frame("contentFrame");\n    driver.findElement(By.id("innerElement")); // Found!\n}',expectedOutput:'Element found after proper wait or iframe switch',followUpQ:'How do you differentiate NoSuchElementException from other exceptions?',followUpA:'NoSuchElementException = element not in DOM at all. ElementNotInteractableException = element exists but can\'t be interacted with (hidden, disabled). StaleElementReferenceException = element was in DOM but got removed/refreshed. TimeoutException = explicit wait condition not met within timeout. Each has different solutions.',seniorFollowUp:'How does your framework handle this exception globally?',seniorAnswer:'My framework has: 1) A TestListener that captures screenshot + page source on failure, 2) A retry mechanism for specific exceptions (retries on StaleElement, not on NoSuchElement), 3) Smart logging that includes the locator, current URL, and page title in the error message, 4) An element existence check utility that uses findElements() to avoid the exception when checking optional elements.',commonMistake:'Catching the exception and suppressing it instead of fixing the root cause.',bestPractice:'Always investigate WHY the element wasn\'t found. Don\'t just add a longer wait.'},
    {id:'EX002',category:'Selenium',topic:'Exceptions',subtopic:'StaleElementReferenceException',question:'What is StaleElementReferenceException and how do you handle it?',whyAsked:'Common in dynamic web apps — shows real automation experience.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'Thrown when a previously found element is no longer attached to the DOM (page refreshed, DOM re-rendered). Fix: re-find the element before interacting.',interviewAnswer:'StaleElementReferenceException occurs when the DOM changes after you\'ve found an element but before you interact with it. The element reference becomes "stale" — it points to an element that no longer exists in the current DOM. Common causes: 1) **AJAX refresh** — the section containing the element reloads, 2) **Page navigation** — the page changes after finding the element, 3) **React/Angular re-renders** — the component re-renders, replacing the DOM node. Solutions: 1) **Re-find the element** before each interaction, 2) **Use Explicit Wait with refreshed condition**: `wait.until(ExpectedConditions.refreshed(ExpectedConditions.elementToBeClickable(locator)))`, 3) **Retry mechanism** — I have a retryOnStale utility that retries the operation up to 3 times with re-fetch. 4) **Avoid storing element references** — use locators instead and find elements just before interaction.',detailedExplanation:'Stale = the element reference is outdated. The element might still be visible on screen with the same text, but the underlying DOM node was replaced. Common in SPAs where the virtual DOM reconciliation replaces nodes.',simpleExplanation:'Like having a bookmark in a book, but someone replaced the page. Your bookmark points to the right position, but the page is different.',realWorldExample:'A product list refreshes via AJAX after applying a filter. Elements found before the filter are now stale because the list re-rendered.',projectExample:'In my e-commerce project, sorting search results re-renders the product list. I found elements before sorting and tried to click after — causing StaleElementReferenceException. Fixed by re-finding elements after the sort completes.',codeCommand:'// Problem: Stale reference\nWebElement element = driver.findElement(By.id("item"));\n// ... DOM refreshes here ...\nelement.click(); // StaleElementReferenceException!\n\n// Solution 1: Re-find before use\ndriver.findElement(By.id("item")).click();\n\n// Solution 2: Wait for refreshed element\nwait.until(ExpectedConditions.refreshed(\n    ExpectedConditions.elementToBeClickable(By.id("item"))\n)).click();\n\n// Solution 3: Retry utility\npublic void clickWithRetry(By locator, int maxRetries) {\n    for (int i = 0; i < maxRetries; i++) {\n        try {\n            driver.findElement(locator).click();\n            return;\n        } catch (StaleElementReferenceException e) {\n            if (i == maxRetries - 1) throw e;\n        }\n    }\n}',expectedOutput:'Element interaction succeeds after re-finding',followUpQ:'How do you prevent StaleElementReferenceException in your framework?',followUpA:'Prevention strategies: 1) Don\'t store WebElement references — use By locators and find elements just before interaction, 2) In Page Object Model, make element locators lazy — find them when needed, not in constructor, 3) After any action that might refresh the DOM (sorting, filtering, AJAX), add a wait before the next interaction, 4) Use the refreshed() ExpectedCondition wrapper.',seniorFollowUp:'How does your framework architecture handle stale elements systematically?',seniorAnswer:'In my framework: 1) Page actions always use fresh element references — `getElement(locator)` instead of stored fields, 2) A custom `@RetryOnStale` annotation on test methods enables automatic retry, 3) The BaseElement wrapper class catches StaleElementReferenceException and re-finds automatically, 4) After page transitions, a `waitForPageStable()` method ensures the DOM has settled before continuing.',commonMistake:'Storing WebElements in class fields and reusing them after page changes.',bestPractice:'Treat element references as disposable — find them right before you need them.'},
    {id:'EX003',category:'Selenium',topic:'Exceptions',subtopic:'ElementClickInterceptedException',question:'How do you handle ElementClickInterceptedException?',whyAsked:'Common in modern web apps with overlays, modals, and floating elements.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'Another element is covering the target element, preventing the click. Fix: wait for overlay to disappear, scroll to element, or use JavaScript click as last resort.',interviewAnswer:'ElementClickInterceptedException means Selenium can click the element, but another element (overlay, modal, sticky header, cookie banner) is covering it and would receive the click instead. My debugging approach: 1) **Identify the intercepting element** — the exception message includes "Element X would receive the click instead," telling you what\'s blocking. 2) **Wait for overlays** — `wait.until(ExpectedConditions.invisibilityOfElementLocated(By.className("overlay")))`. 3) **Scroll into view** — `js.executeScript("arguments[0].scrollIntoView({block:\'center\'})", element)` centers the element on screen. 4) **Close modals/banners** — dismiss cookie consents, close popups. 5) **JavaScript click** — `js.executeScript("arguments[0].click()", element)` as last resort. In my framework, I have a smartClick() utility that attempts normal click, handles the intercepted exception, scrolls, and retries.',detailedExplanation:'Root causes: Cookie consent banners, loading spinners, fixed headers/footers, tooltip overlays, modal backdrops, floating action buttons, chat widgets. The element IS in the DOM and interactable, but something is on top of it.',simpleExplanation:'You\'re trying to click a button, but another element is "standing in front of it" — like trying to shake someone\'s hand but another person is between you.',realWorldExample:'A chat widget icon overlaps with the footer "Submit" button. Selenium sees the button but the chat widget intercepts the click.',projectExample:'In our e-commerce site, a persistent notification bar at the top covered the navigation menu items when scrolled up. I added a utility to dismiss notifications before menu interactions.',codeCommand:'// Step 1: Wait for overlay to disappear\nwait.until(ExpectedConditions.invisibilityOfElementLocated(\n    By.className("loading-overlay")\n));\n\n// Step 2: Scroll element to center\nJavascriptExecutor js = (JavascriptExecutor) driver;\njs.executeScript("arguments[0].scrollIntoView({block:\'center\'});", element);\n\n// Step 3: Try clicking\nelement.click();\n\n// If still intercepted, use JS click\ntry {\n    element.click();\n} catch (ElementClickInterceptedException e) {\n    js.executeScript("arguments[0].click();", element);\n}',expectedOutput:'Element clicked successfully after removing obstruction',followUpQ:'Is using JavaScript click always a good solution?',followUpA:'No — JS click should be a last resort because: 1) It bypasses visibility checks that real users would encounter, 2) It doesn\'t trigger all browser events (hover, focus), 3) It might click an element a real user can\'t see. The right approach is to fix the underlying issue (dismiss overlay, scroll properly) rather than bypass it with JS click.',seniorFollowUp:'How do you systematically prevent click interception in your framework?',seniorAnswer:'Framework-level solutions: 1) A pre-click method that checks for common overlays (cookie banners, loaders, notifications) and dismisses them, 2) Auto-scrolling before every click to ensure the element is in the viewport center, 3) A configurable delay after page transitions for floating elements to settle, 4) A whitelist of known overlapping elements that get dismissed automatically.',commonMistake:'Always using JavaScript click to bypass this exception instead of investigating the root cause.',bestPractice:'Fix the obstruction first (dismiss overlays, scroll). Use JS click only as a documented last resort.'}
  ]
};
