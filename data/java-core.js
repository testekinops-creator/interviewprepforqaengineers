/* ═══════════════════════════════════════════════════════════════
   java-core.js — Java Core & Syntax
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['java-core'] = {
  title: '☕ Java Core & OOP',
  description: 'Core Java fundamentals, variables, loops, logic, and Object-Oriented Programming principles',
  questions: [
    {
      id: 'JC001',
      category: 'Java',
      topic: 'Data Types',
      subtopic: 'Primitives vs Objects',
      question: 'What is the difference between int and Integer in Java?',
      whyAsked: 'Tests fundamental knowledge of Java memory and boxing/unboxing.',
      difficulty: 1,
      importance: 'must',
      interviewType: 'Technical',
      thirtySecAnswer: '`int` is a primitive data type that stores a 32-bit signed integer. `Integer` is a wrapper class that wraps an `int` inside an object, allowing it to be used in Collections like ArrayList which cannot hold primitives.',
      interviewAnswer: '`int` is a primitive type. It holds raw values, takes less memory, and cannot be null (defaults to 0). `Integer` is a wrapper class for `int`. Because Java Collections (like `ArrayList`, `HashMap`) can only store Objects, we must use `Integer` if we want to store numbers in a list.\n\nJava handles the conversion between the two automatically. This is called **Autoboxing** (converting `int` to `Integer`) and **Unboxing** (converting `Integer` to `int`). However, because `Integer` is an object, it can be `null`, which can cause a `NullPointerException` if you accidentally try to unbox a null `Integer` into an `int`.',
      detailedExplanation: 'SIDE-BY-SIDE COMPARISON:\n\n| Feature | `int` | `Integer` |\n| :--- | :--- | :--- |\n| **Type** | Primitive | Class (Object) |\n| **Default Value**| `0` | `null` |\n| **Memory** | Stack memory | Heap memory (slower) |\n| **Collections** | Cannot be used (e.g., `List<int>` is invalid) | Can be used (`List<Integer>`) |\n| **Methods** | None | Has utility methods like `parseInt()` |',
      simpleExplanation: '`int` is just the raw number. `Integer` is the number put inside a box (an Object) so it can be stored in places that only accept boxes.',
      realWorldExample: 'N/A',
      projectExample: 'When retrieving database IDs or API responses, I use `Integer` because the value might be missing (null). If I used `int`, the system would default it to 0, which could cause a bug where the code thinks the ID is 0 instead of "not provided".',
      codeCommand: 'int a = 5;\nInteger b = 5; // Autoboxing\nint c = b;     // Unboxing\n\nList<Integer> list = new ArrayList<>();\nlist.add(a);   // Autoboxing behind the scenes',
      expectedOutput: 'N/A',
      followUpQ: 'What happens if you unbox a null Integer?',
      followUpA: 'A NullPointerException is thrown at runtime because Java tries to call `.intValue()` on a null object.',
      seniorFollowUpQ: 'Explain the Integer cache in Java.',
      seniorFollowUpA: 'Java caches Integer objects from -128 to 127 to save memory. So `Integer a = 100; Integer b = 100; System.out.println(a == b);` prints `true`. But `Integer c = 200; Integer d = 200; System.out.println(c == d);` prints `false` because they are outside the cache and refer to different objects in the heap.',
      commonMistake: 'Using `==` to compare two `Integer` objects instead of `.equals()`.',
      bestPractice: 'Always use `.equals()` when comparing wrapper classes like `Integer`.'
    },
    {
      id: 'JC002',
      category: 'Java',
      topic: 'Operators',
      subtopic: 'Written Test - String Pool',
      question: 'WRITTEN TEST: What is the output of the following code and why?\n`String s1 = "QA"; String s2 = "QA"; String s3 = new String("QA"); System.out.println(s1 == s2); System.out.println(s1 == s3); System.out.println(s1.equals(s3));`',
      whyAsked: 'Classic output prediction question testing String pool and equality vs identity.',
      difficulty: 3,
      importance: 'must',
      interviewType: 'Written Test',
      thirtySecAnswer: 'The output is `true`, `false`, `true`. `s1 == s2` is true because both point to the same object in the String Pool. `s1 == s3` is false because `new String()` creates a new object in heap memory. `.equals()` is true because the actual character values are identical.',
      interviewAnswer: 'This code outputs:\n`true`\n`false`\n`true`\n\n**Why?**\nJava optimizes memory using a String Constant Pool. When we declare `String s1 = "QA"`, Java creates the object in the pool. When we declare `s2 = "QA"`, Java sees "QA" already exists and points `s2` to the exact same object. Therefore, `s1 == s2` is true (they share the same memory reference).\n\nWhen we use `new String("QA")`, we force Java to create a completely new object in standard heap memory, bypassing the pool. So `s1` and `s3` point to different objects, making `s1 == s3` false.\n\nHowever, `.equals()` checks the actual value/contents of the string, which is "QA" in both cases, so `s1.equals(s3)` is true.',
      detailedExplanation: 'SIDE-BY-SIDE COMPARISON:\n\n| Feature | `==` Operator | `.equals()` Method |\n| :--- | :--- | :--- |\n| **What it checks** | Memory address (Reference identity) | Object contents (Value identity) |\n| **Usage for Strings**| DO NOT USE for value comparison | ALWAYS use for value comparison |\n| **Primitive types**| Safe to use (`5 == 5` is true) | Cannot be used on primitives |',
      simpleExplanation: '`==` asks "Are these two people wearing the exact same shirt?" (Same physical shirt). `.equals()` asks "Are these two shirts the same size and color?" (Different shirts, but look identical).',
      realWorldExample: 'Validating API responses: `if(responseStatus == "SUCCESS")` might fail due to reference mismatch. Always use `if("SUCCESS".equals(responseStatus))`',
      projectExample: 'In my data-driven testing, I had a bug where comparing the expected status from an Excel sheet to the actual UI status failed because I used `==`. The strings had identical text but different memory addresses. Changing it to `.equals()` fixed it.',
      codeCommand: 'String s1 = "QA";\nString s2 = "QA";\nString s3 = new String("QA");\n\nSystem.out.println(s1 == s2);      // true (Same memory address in String Pool)\nSystem.out.println(s1 == s3);      // false (Different memory addresses)\nSystem.out.println(s1.equals(s3)); // true (Same value)',
      expectedOutput: 'true\nfalse\ntrue',
      followUpQ: 'How can you force `s3` to point to the String Pool?',
      followUpA: 'By using the `.intern()` method. If you do `String s4 = new String("QA").intern();`, Java will check the pool for "QA". Since it exists, it returns the reference from the pool. Then `s1 == s4` would be true.',
      seniorFollowUpQ: 'Is the String Pool stored in Heap memory or Stack memory?',
      seniorFollowUpA: 'Before Java 7, the String Pool was stored in PermGen (Permanent Generation) space which had a fixed size and often caused OutOfMemory errors. From Java 7 onwards, it was moved to the main Heap space, allowing it to be garbage collected.',
      commonMistake: 'Using `==` to compare Strings.',
      bestPractice: 'Always use `"Constant".equals(variable)` to avoid NullPointerExceptions if the variable is null.'
    },
    {
      id: 'JC003',
      category: 'Java',
      topic: 'OOP Concepts',
      subtopic: '4 Pillars of OOP',
      question: 'Explain the 4 Pillars of Object-Oriented Programming with examples from an Automation Framework.',
      whyAsked: 'The absolute core of Java architecture. If you cannot explain these, you cannot design frameworks.',
      difficulty: 2,
      importance: 'must',
      interviewType: 'Technical',
      thirtySecAnswer: 'Inheritance allows code reuse (e.g., BaseTest). Polymorphism allows overriding methods (e.g., same method acting differently for Chrome vs Firefox). Abstraction hides complex implementation (e.g., WebDriver interface). Encapsulation protects data via private variables and getters/setters (e.g., Page Objects).',
      interviewAnswer: 'The four pillars are essential for a scalable automation framework:\n\n1. **Inheritance (Code Reusability):** A class acquires properties of another. Example: A `BaseTest` class contains `@BeforeMethod` setup (driver initialization). All test classes `extend BaseTest` to inherit this setup without writing it again.\n\n2. **Polymorphism (Many Forms):** Using the same interface but different implementations. Example: `WebDriver driver = new ChromeDriver();` or `driver = new FirefoxDriver();`. The method `driver.get()` takes many forms depending on the browser instantiated at runtime (Method Overriding). Method Overloading is having multiple `waitForElement` methods with different parameters (e.g., one takes a timeout, one uses default).\n\n3. **Abstraction (Hiding Implementation):** Exposing only necessary functionality. Example: We use the `WebDriver` interface. We just call `.click()`. We don\'t know or care how Chrome actually executes the C++ code to click the pixel. Another example is a custom Wrapper method like `clickElement(locator)` that hides the explicit wait logic inside it.\n\n4. **Encapsulation (Data Hiding):** Bundling data and methods together and restricting access. Example: In the Page Object Model (POM), locators (`By usernameBtn`) are made `private`. Tests cannot access them directly. Instead, they use `public` methods like `login()` which act upon those private variables. This prevents tests from corrupting the page structure.',
      detailedExplanation: 'SIDE-BY-SIDE COMPARISON:\n\n| Pillar | Automation Concept | Real-World Example |\n| :--- | :--- | :--- |\n| **Inheritance** | `extends BasePage` | A child inheriting a parent\'s physical traits |\n| **Polymorphism**| `WebDriver` instantiation | A person acting as a boss at work and parent at home |\n| **Abstraction** | Interfaces, wrapper methods | Driving a car (using steering wheel without knowing engine mechanics) |\n| **Encapsulation**| `private` locators in POM | A capsule containing medicine (you swallow it without touching the powder) |',
      simpleExplanation: 'Inheritance = Share it. Polymorphism = Change it. Abstraction = Hide the complex stuff. Encapsulation = Protect the sensitive stuff.',
      realWorldExample: 'N/A',
      projectExample: 'In my framework, I used Encapsulation strictly. Junior testers were bypassing Page Objects and injecting locators directly into tests. I made all locators `private final` in the page classes, forcing them to use the public API methods, which reduced flaky tests significantly.',
      codeCommand: 'N/A',
      expectedOutput: 'N/A',
      followUpQ: 'What is the difference between Method Overloading and Method Overriding?',
      followUpA: 'Overloading (Compile-time polymorphism) is having multiple methods with the same name but different parameters in the same class (e.g., `wait(int sec)` vs `wait(int sec, String message)`). Overriding (Run-time polymorphism) is a subclass providing a specific implementation of a method already defined in its parent class (e.g., `@Override`).',
      seniorFollowUpQ: 'Does Java support multiple inheritance?',
      seniorFollowUpA: 'Java does not support multiple inheritance with classes (you cannot `extend A, B`) to avoid the Diamond Problem (ambiguity if both classes have the same method). However, Java supports multiple inheritance through Interfaces (you can `implements A, B`).',
      commonMistake: 'Confusing Abstraction with Encapsulation.',
      bestPractice: 'Always apply Encapsulation in Page Objects to ensure strict separation of concerns.'
    },
    {
      id: 'JC004',
      category: 'Java',
      topic: 'OOP Concepts',
      subtopic: 'Abstract Class vs Interface',
      question: 'What is the difference between an Abstract Class and an Interface?',
      whyAsked: 'Critical for designing framework architectures, base pages, and utility structures.',
      difficulty: 3,
      importance: 'must',
      interviewType: 'Technical',
      thirtySecAnswer: 'An Abstract Class can have both implemented methods and abstract (unimplemented) methods, and a class can only extend one abstract class. An Interface traditionally has only abstract methods, and a class can implement multiple interfaces.',
      interviewAnswer: 'In my frameworks, I use both for different purposes.\n\nAn **Abstract Class** is a class that cannot be instantiated (`new`). It can contain regular implemented methods and variables, as well as abstract methods (methods without a body). I use it when classes share a lot of common implementation logic. For example, a `BaseTest` abstract class handles all WebDriver setup/teardown logic, and subclasses inherit it. A class can only `extend` one abstract class.\n\nAn **Interface** is a contract. Traditionally, it only contains abstract methods (though Java 8 added default methods). An interface defines *what* a class should do, not *how* it should do it. A class can `implements` multiple interfaces. I use interfaces for cross-cutting capabilities, for example, `IRetryAnalyzer` or creating an `ILogger` interface that multiple different tools can implement.',
      detailedExplanation: 'SIDE-BY-SIDE COMPARISON:\n\n| Feature | Abstract Class | Interface |\n| :--- | :--- | :--- |\n| **Inheritance** | A class can extend only ONE | A class can implement MULTIPLE |\n| **Methods** | Can have both implemented and abstract methods | Traditionally only abstract (default/static added in Java 8) |\n| **Variables** | Can have static, final, or normal variables | All variables are implicitly `public static final` |\n| **Constructors** | Can have constructors (called by subclasses) | Cannot have constructors |\n| **Usage Idea** | "Is-A" relationship (Dog is an Animal) | "Can-Do" relationship (Dog can implement Runnable) |',
      simpleExplanation: 'Abstract class is a half-finished blueprint (some walls built, some missing). Interface is just a list of rules (you MUST build a door, a window, and a roof, but you build it yourself).',
      realWorldExample: 'N/A',
      projectExample: 'I created an `ITextExtractor` interface because we extracted text from PDFs and Images. I had two classes: `PdfExtractor` and `OcrExtractor` that both implemented the interface, allowing my tests to just call `extractText()` without caring which engine was running behind the scenes.',
      codeCommand: '// Abstract Class\\npublic abstract class BasePage {\\n    WebDriver driver;\\n    public BasePage(WebDriver d) { this.driver = d; } // Constructor allowed\\n    public void refresh() { driver.navigate().refresh(); } // Implemented\\n    public abstract void waitForPageLoad(); // Abstract\\n}\\n\\n// Interface\\npublic interface IReporter {\\n    void logPass(String msg);\\n    void logFail(String msg);\\n}',
      expectedOutput: 'N/A',
      followUpQ: 'Since Java 8 added `default` methods to interfaces, why do we still need Abstract Classes?',
      followUpA: 'Because Abstract Classes can hold state (instance variables) and have constructors. Interfaces cannot have instance variables (only `public static final` constants). If your shared code requires keeping track of state (like holding a `WebDriver` variable), you must use an Abstract Class.',
      seniorFollowUpQ: 'What happens if a class implements two interfaces that both have a `default` method with the exact same signature?',
      seniorFollowUpA: 'The compiler will throw an error due to the Diamond Problem. To fix it, the class MUST override the conflicting method and explicitly specify which interface\'s method to use via `InterfaceA.super.methodName();`.',
      commonMistake: 'Trying to instantiate an abstract class (`new BaseTest()`).',
      bestPractice: 'Use interfaces to define capabilities. Use abstract classes to share core implementation logic.'
    },
    {
      id: 'JC005',
      category: 'Java',
      topic: 'Keywords',
      subtopic: 'final, finally, finalize',
      question: 'WRITTEN TEST: Explain the difference between `final`, `finally`, and `finalize`.',
      whyAsked: 'Very common Java trivia question testing keywords and garbage collection.',
      difficulty: 2,
      importance: 'important',
      interviewType: 'Written Test',
      thirtySecAnswer: '`final` is a keyword to restrict modification (constant variables, un-overridable methods, un-inheritable classes). `finally` is a block in exception handling that always executes regardless of errors. `finalize` is an obsolete method called by the Garbage Collector before an object is destroyed.',
      interviewAnswer: 'These three words sound similar but have completely unrelated purposes in Java:\n\n1. **`final` (Keyword):** Used to apply restrictions. A `final` variable cannot be reassigned (it\'s a constant). A `final` method cannot be overridden by a subclass. A `final` class cannot be extended (e.g., the `String` class is final).\n\n2. **`finally` (Block):** Used in `try-catch` exception handling. The code inside the `finally` block is guaranteed to execute whether an exception was thrown or not. In automation, I use this to ensure the `driver.quit()` or database connection closure always happens, even if the test fails violently.\n\n3. **`finalize()` (Method):** This is a method belonging to the `Object` class. The Garbage Collector calls it just before an object is destroyed to clean up resources. However, it is highly unpredictable and was officially deprecated in Java 9. You should never rely on it.',
      detailedExplanation: 'SIDE-BY-SIDE COMPARISON:\n\n| Concept | `final` | `finally` | `finalize()` |\n| :--- | :--- | :--- | :--- |\n| **What is it?** | A modifier keyword | A code block | A method |\n| **Associated with** | Variables, Methods, Classes | try-catch exception handling | Garbage Collection (Deprecated) |\n| **Purpose** | Prevent modification/inheritance | Guarantee execution of cleanup code | Resource cleanup before GC |\n| **Automation use**| `private final By locator` | `finally { driver.quit(); }` | Do not use |',
      simpleExplanation: '`final` means "You can\'t change this." `finally` means "Do this no matter what happens." `finalize` means "The trash collector is here, throw this away." (but don\'t use it).',
      realWorldExample: 'N/A',
      projectExample: 'In my framework, all page locators are declared as `private final By loginBtn = By.id("login");` so no one can accidentally change the locator value during runtime. I also use a `try-finally` block when reading Excel files to ensure the `FileInputStream` is closed and memory is not leaked, even if the file format is corrupt.',
      codeCommand: '// final variable\\nfinal int MAX_TIMEOUT = 30;\\n\\n// finally block\\ntry {\\n    driver.findElement(By.id(\"missing\")).click();\\n} catch (Exception e) {\\n    System.out.println(\"Failed!\");\\n} finally {\\n    driver.quit(); // Always runs\\n}\\n\\n// finalize method (Avoid)\\nprotected void finalize() throws Throwable {\\n    // cleanup\\n}',
      expectedOutput: 'N/A',
      followUpQ: 'If `System.exit(0)` is called in the `try` block, does the `finally` block execute?',
      followUpA: 'No. That is the only scenario where a `finally` block will not execute. `System.exit(0)` immediately shuts down the entire JVM.',
      seniorFollowUpQ: 'Since `finalize()` is deprecated, what is the modern Java approach to ensuring resources are closed?',
      seniorFollowUpA: 'The modern approach is the "try-with-resources" statement introduced in Java 7. Any resource that implements the `AutoCloseable` interface (like Streams, DB connections) can be declared inside the try parentheses: `try (FileInputStream fis = new FileInputStream("file.txt")) { ... }`. Java will automatically close it when the block finishes, completely eliminating the need for a `finally` block.',
      commonMistake: 'Using `finalize()` for standard cleanup tasks instead of `finally`.',
      bestPractice: 'Use try-with-resources whenever possible to avoid messy try-catch-finally blocks.'
    }
  ]
};
