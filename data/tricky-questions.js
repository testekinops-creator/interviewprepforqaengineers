/* tricky-questions.js — Tricky Interview Questions */
var defined_sections = defined_sections || {};
defined_sections['tricky-questions'] = {
  title: '🧩 Tricky Questions',
  description: 'Comparison and trick questions commonly asked to catch candidates off-guard',
  questions: [
    {id:'TQ001',category:'Tricky',topic:'Selenium',subtopic:'close vs quit',question:'What is the difference between close() and quit()?',whyAsked:'Tests attention to detail about driver lifecycle management.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'close() closes only the current window. quit() closes ALL windows and ends the WebDriver session. Always use quit() in teardown.',interviewAnswer:'`close()` closes the current focused browser window or tab. If there are multiple windows open, only the active one closes — the WebDriver session stays alive. `quit()` closes ALL browser windows AND terminates the WebDriver process (ChromeDriver, GeckoDriver). In my framework, I always use `quit()` in @AfterMethod teardown to ensure clean resource release. I use `close()` only when testing multi-window scenarios — close the child window, then switch back to the parent. A common production issue: using close() in teardown leaves orphaned driver processes that accumulate on CI servers, eventually consuming all memory.',detailedExplanation:'close() = window only, session continues. quit() = all windows + session + driver process. Forgetting quit() causes resource leaks in CI environments.',simpleExplanation:'close() = close one tab. quit() = close the entire browser and shut it down completely.',realWorldExample:'Testing with 3 windows open: close() closes current. quit() closes all 3 and ends the session.',projectExample:'On our Jenkins server, we found 200+ orphaned chromedriver processes because a developer used close() instead of quit(). Switch to quit() resolved the memory issue.',codeCommand:'// close() - current window only\ndriver.close(); // closes current tab/window\n// WebDriver session still active, can switch to other windows\n\n// quit() - everything\ndriver.quit(); // closes ALL windows + kills driver process\n// WebDriver session ended, cannot use driver anymore\n\n// Correct teardown pattern\n@AfterMethod\npublic void tearDown() {\n    if (driver != null) {\n        driver.quit(); // Always quit, never just close\n    }\n}',expectedOutput:'close() = 1 window closed. quit() = all windows + session terminated.',followUpQ:'What happens if you call close() when only one window is open?',followUpA:'The window closes and the WebDriver session becomes unusable — any subsequent commands throw NoSuchSessionException. But the driver process may still be running as an orphan. This is why quit() is always preferred for cleanup.',seniorFollowUp:'How do you ensure driver cleanup even when tests fail?',seniorAnswer:'1) quit() in @AfterMethod with try-catch to handle already-closed scenarios, 2) A shutdown hook: Runtime.getRuntime().addShutdownHook(), 3) Jenkins job cleanup script that kills stale driver processes before each run, 4) ThreadLocal<WebDriver> pattern for parallel execution with per-thread cleanup.',commonMistake:'Using close() in teardown instead of quit().',bestPractice:'Always use quit() in teardown. Use close() only for multi-window testing.'},
    {id:'TQ002',category:'Tricky',topic:'Selenium',subtopic:'get vs navigate',question:'What is the difference between get() and navigate().to()?',whyAsked:'Classic trick question — subtle but important difference.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'Both load a URL and wait for page load. get() is direct. navigate() provides additional methods: back(), forward(), refresh(). In practice, behavior is nearly identical.',interviewAnswer:'Both `driver.get(url)` and `driver.navigate().to(url)` load a URL and wait for document.readyState to be "complete." The key difference: `navigate()` returns a Navigation object with additional methods — `back()`, `forward()`, `refresh()` — simulating browser navigation buttons. `get()` is equivalent to typing a URL in the address bar. In modern Selenium implementations, both behave nearly identically for URL loading. I use `get()` for initial page loads and `navigate()` methods for testing browser navigation flows. One subtle difference: `navigate().to()` maintains browser history better in some implementations, which matters when testing back/forward behavior.',detailedExplanation:'get(String url): loads URL, waits for page load. navigate().to(String url): loads URL via navigation interface. navigate().back(): browser back. navigate().forward(): browser forward. navigate().refresh(): reload.',simpleExplanation:'get() = type URL and press Enter. navigate() = use the browser\'s navigation buttons.',realWorldExample:'Test scenario: User goes to page A, navigates to page B, presses Back. Use get("A"), navigate().to("B"), navigate().back().',projectExample:'In my test for browser history behavior, I use navigate().back() and navigate().forward() to validate that the application handles browser navigation correctly.',codeCommand:'// get() - direct URL load\ndriver.get("https://example.com");\n\n// navigate().to() - via Navigation interface\ndriver.navigate().to("https://example.com/page2");\ndriver.navigate().back();    // go back to example.com\ndriver.navigate().forward(); // go forward to page2\ndriver.navigate().refresh(); // reload current page',expectedOutput:'Both load the URL. navigate() provides back/forward/refresh.',followUpQ:'Can you pass a relative URL to get()?',followUpA:'No, get() requires a full URL with protocol (https://). If you pass a relative URL, it won\'t work as expected. For relative navigation, you\'d need to construct the full URL: driver.get(baseUrl + "/path").',seniorFollowUp:'When would the difference between get() and navigate() actually matter?',seniorAnswer:'In most practical scenarios, they\'re interchangeable. The difference matters when: 1) Testing SPA navigation — navigate().to() may trigger different router behavior than get() which causes full page reload, 2) Testing browser history — navigate() maintains proper history stack, 3) Cookie behavior — get() might clear cookies in some edge cases while navigate() preserves them.',commonMistake:'Saying they are completely different. The practical difference is minimal.',bestPractice:'Use get() for simplicity. Use navigate() when you specifically need back/forward/refresh.'},
    {id:'TQ003',category:'Tricky',topic:'Selenium',subtopic:'implicit vs explicit',question:'What happens if you mix Implicit Wait and Explicit Wait?',whyAsked:'Advanced synchronization knowledge — catches common mistakes.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'Mixing them can cause unpredictable wait times — they may add up (e.g., 10s implicit + 15s explicit = up to 25s). Selenium documentation warns against mixing.',interviewAnswer:'Mixing Implicit and Explicit waits is dangerous and officially discouraged by Selenium. Here\'s why: Implicit Wait applies to every findElement() call. Explicit Wait internally polls using findElement(). So if Implicit Wait is 10 seconds and Explicit Wait is 15 seconds, each polling attempt inside the Explicit Wait waits 10 seconds for the Implicit Wait to expire before returning "not found." This can result in a total wait of up to 25 seconds (or even 10 × number_of_polls). The behavior is implementation-dependent and varies across browser drivers. In my framework, I set Implicit Wait to 0 and use Explicit Waits exclusively. This gives me precise, predictable timing control.',detailedExplanation:'The conflict: Explicit Wait polls every 500ms using findElement(). Implicit Wait makes each findElement() wait up to its timeout. Result: polling is no longer every 500ms — it\'s every (implicit wait timeout) seconds. Total wait = (implicit × polls) + explicit overhead.',simpleExplanation:'It\'s like setting two alarm clocks that interfere with each other — instead of waking you up at the right time, they confuse each other.',realWorldExample:'Implicit=10s, Explicit=15s for invisibilityOf: Selenium polls for element presence (waits 10s each time), making the total wait much longer than expected.',projectExample:'When I joined my project, tests had both Implicit(10s) and Explicit(20s) waits. Some tests took 30+ seconds to fail instead of 20. I removed Implicit Wait and all timeouts became predictable.',codeCommand:'// ❌ BAD - Don\'t mix\ndriver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));\nWebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(15));\nwait.until(EC.elementToBeClickable(By.id("btn"))); // Unpredictable!\n\n// ✅ GOOD - Explicit only\ndriver.manage().timeouts().implicitlyWait(Duration.ofSeconds(0)); // disabled\nWebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(15));\nwait.until(EC.elementToBeClickable(By.id("btn"))); // Predictable!',expectedOutput:'With mixing: unpredictable timing. Without mixing: precise control.',followUpQ:'Is it safe to have Implicit Wait set to 0?',followUpA:'Yes! Setting Implicit Wait to 0 means findElement() throws NoSuchElementException immediately if the element isn\'t in the DOM. This is actually desired when using Explicit Waits because it makes polling precise (every 500ms as intended). All element finding should then go through your Explicit Wait utilities.',seniorFollowUp:'How is your framework\'s wait strategy designed?',seniorAnswer:'In my framework: 1) Implicit Wait = 0 (disabled), 2) Every page action uses Explicit Wait via a BasePage utility: waitForClickable(), waitForVisible(), waitForInvisible(), 3) Custom waits for AJAX completion, 4) Configurable timeout via properties file, 5) No Thread.sleep() anywhere (enforced via code review). This gives us complete, predictable control over synchronization.',commonMistake:'Having Implicit Wait set to a non-zero value while also using Explicit Waits.',bestPractice:'Choose one approach: Explicit Waits only. Set Implicit Wait to 0.'},
    {id:'TQ004',category:'Tricky',topic:'Java',subtopic:'== vs equals',question:'What is the difference between == and equals() in Java?',whyAsked:'Fundamental Java question — frequently catches candidates.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'== compares references (memory addresses). equals() compares values/content. For Strings: "abc" == "abc" may be true (string pool), but new String("abc") == new String("abc") is false.',interviewAnswer:'`==` compares whether two references point to the same object in memory. `equals()` compares the content/value of two objects. For Strings: `"hello" == "hello"` returns true because Java\'s String pool reuses the same object. But `new String("hello") == new String("hello")` returns false because `new` creates separate objects. `"hello".equals("hello")` always returns true because it compares content. In my automation code, I always use `.equals()` for String comparison: `actualTitle.equals(expectedTitle)` instead of `actualTitle == expectedTitle`. For null-safe comparison, I use: `expectedTitle.equals(actualTitle)` (constant first) or `Objects.equals(a, b)` in Java 7+.',detailedExplanation:'== for primitives: compares values (5 == 5 is true). == for objects: compares references. equals() for objects: compares content (if properly overridden). String pool: literal strings are interned, so == might work but is unreliable.',simpleExplanation:'== asks "are these the same box?" equals() asks "do these boxes contain the same thing?"',realWorldExample:'Two printouts of the same document: == asks "is it the same piece of paper?" equals() asks "is the content identical?"',projectExample:'In my test assertions, I always use assertEquals(actual, expected) which internally uses equals(). A junior developer once used == for String comparison in a validation method, causing intermittent test failures.',codeCommand:'String a = "hello";\nString b = "hello";\nString c = new String("hello");\n\nSystem.out.println(a == b);       // true (same string pool ref)\nSystem.out.println(a == c);       // false (different objects)\nSystem.out.println(a.equals(c));  // true (same content)\n\n// In automation - ALWAYS use equals()\nString actualTitle = driver.getTitle();\nassertTrue(actualTitle.equals("Expected Title")); // ✅\n// NOT: assertTrue(actualTitle == "Expected Title"); // ❌',expectedOutput:'== compares references (may be false). equals() compares content (reliable).',followUpQ:'What about equalsIgnoreCase()?',followUpA:'equalsIgnoreCase() compares string content ignoring case: "Hello".equalsIgnoreCase("hello") returns true. I use this in test validation when the expected text might have case variations: pageTitle.equalsIgnoreCase(expectedTitle).',seniorFollowUp:'How does equals() work with custom objects?',seniorAnswer:'By default, equals() in Object class uses ==. For custom objects, you must override equals() (and hashCode()). In my framework, I have a TestData POJO with overridden equals() for comparing test records. Important: if you override equals(), always override hashCode() too — otherwise HashMap/HashSet won\'t work correctly with your objects.',commonMistake:'Using == to compare Strings and getting intermittent false results.',bestPractice:'Always use .equals() for object comparison. Use == only for primitives and null checks.'},
    {id:'TQ005',category:'Tricky',topic:'Testing',subtopic:'Severity vs Priority',question:'Can you have High Severity but Low Priority? Give an example.',whyAsked:'Classic QA trick question — tests real understanding vs memorized definitions.',difficulty:2,importance:'must',interviewType:'Phone/Technical',thirtySecAnswer:'Yes. Example: Application crashes on a legacy browser (IE) that only 0.1% of users use. High severity (crash) but low priority (minimal user impact).',interviewAnswer:'Absolutely. Severity and Priority are independent dimensions. **High Severity + Low Priority** example: The application crashes when accessed from Internet Explorer 11 on Windows 7. The crash is severe (application is unusable), but since only 0.1% of our users use IE11 and we\'ve already announced end of IE support, the business priority is low — we\'ll fix it eventually but it won\'t delay the release. **Low Severity + High Priority** example: The company logo on the homepage is pixelated. Technically it\'s a minor cosmetic issue (low severity), but since it affects brand perception for every visitor, the VP of Marketing wants it fixed before launch (high priority). In my experience, Priority trumps Severity for scheduling fixes — we fix based on business impact, not just technical impact.',detailedExplanation:'The four combinations: High-High (critical feature crash), High-Low (crash on unused feature), Low-High (typo on homepage), Low-Low (tooltip misalignment on admin page).',simpleExplanation:'A broken elevator in an empty building = high severity (broken!) but low priority (nobody\'s there). A squeaky door at a hotel entrance = low severity (just annoying) but high priority (every guest hears it).',realWorldExample:'E-commerce: "Sort by price" shows wrong order on mobile Chrome specific version. Severe (wrong results) but low priority if 99% of users use desktop.',projectExample:'In my Salesforce project, a data export feature corrupted special characters in CSV. High severity (data corruption) but low priority because users had an alternative PDF export and the fix was complex.',codeCommand:'',expectedOutput:'',followUpQ:'Who decides the severity and who decides the priority?',followUpA:'Severity is typically assessed by QA based on technical impact (how badly does it break?). Priority is decided by the Product Owner/Manager based on business impact (how urgently should we fix it?). In triage meetings, both are discussed — QA provides severity assessment, PO/PM sets priority based on business context.',seniorFollowUp:'How do you handle disagreements about severity or priority?',seniorAnswer:'I present data: 1) Number of affected users, 2) Business impact (revenue, reputation), 3) Workaround availability, 4) Proximity to release. If the PO disagrees, I document my recommendation and respect their decision — they understand the business context better. However, for genuine blockers (data loss, security), I escalate firmly with evidence.',commonMistake:'Always setting severity and priority to the same value.',bestPractice:'Evaluate severity and priority independently. Present both in defect triage with clear justification.'},
    {id:'TQ006',category:'Tricky',topic:'Testing',subtopic:'Smoke vs Sanity',question:'What is the difference between Smoke Testing and Sanity Testing?',whyAsked:'Frequently confused — interviewers test precise understanding.',difficulty:2,importance:'must',interviewType:'Phone/Technical',thirtySecAnswer:'Smoke = broad, shallow, tests critical functionality across the whole app (build stable?). Sanity = narrow, deep, tests specific area after a change (fix correct?).',interviewAnswer:'**Smoke Testing** is broad and shallow — it\'s the first test after a new build to verify that the critical functionality works. It covers major features across the entire application: Can users log in? Can they search? Can they checkout? If smoke fails, we reject the build — it\'s not stable enough for detailed testing. Think of it as "build verification testing." **Sanity Testing** is narrow and deep — it\'s a focused test after a specific bug fix or small change. If a developer fixed the search filter, sanity testing verifies that specific filter functionality thoroughly. It doesn\'t cover the whole application — just the area affected by the change. Key differences: Smoke is documented and repeatable (often automated), Sanity is usually informal. Smoke tests the entire build stability, Sanity tests a specific change. In my project, our automated smoke suite runs after every deployment and takes 15 minutes. Sanity testing is done manually for specific fixes.',detailedExplanation:'Smoke: Broad scope, shallow depth, build stability, scripted, often automated, done first. Sanity: Narrow scope, deep depth, change verification, informal, usually manual, done after specific fixes.',simpleExplanation:'Smoke = turning on every light switch in a new house (does electricity work?). Sanity = checking if the specific fixed faucet now works properly.',realWorldExample:'New build deployed: Smoke = verify login, homepage, search, cart, checkout, profile. Sanity = developer fixed checkout discount bug, verify only discount scenarios.',projectExample:'In my CI pipeline, smoke tests (30 automated scenarios) run after every deployment. If they pass, the build moves to the QA environment for detailed testing including sanity checks on specific changes.',codeCommand:'',expectedOutput:'',followUpQ:'Can smoke tests be automated?',followUpA:'Yes, and they should be! Smoke tests are ideal for automation because they\'re stable, repeatable, and run frequently. In my project, our smoke suite covers login, navigation, key CRUD operations, and basic search — about 30 automated tests that run in 15 minutes after every deployment.',seniorFollowUp:'How do you decide what goes into the smoke suite?',seniorAnswer:'My criteria: 1) Revenue-critical paths (checkout, payment), 2) Core user journeys (login, search, primary feature), 3) Integrations (API connectivity, database connections), 4) Business-critical validations (pricing, permissions). I keep the smoke suite lean — maximum 30-40 tests running under 20 minutes. If it\'s too large, it\'s no longer a smoke test.',commonMistake:'Using smoke and sanity interchangeably — they have different scope and purpose.',bestPractice:'Automate smoke tests. Keep sanity testing focused and manual for specific changes.'},
    {id:'TQ007',category:'Tricky',topic:'Java',subtopic:'final finally finalize',question:'Explain final, finally, and finalize in Java.',whyAsked:'Classic Java trick question — three keywords that sound similar but are completely different.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'final = constant/immutable (variable, method, class). finally = always-execute block in try-catch. finalize() = cleanup method before garbage collection (deprecated).',interviewAnswer:'These three are completely different: **final** — a keyword for immutability: `final int MAX = 10` (constant variable), `final void method()` (can\'t be overridden), `final class` (can\'t be extended). In my framework, I use `final` for constants like timeouts: `public static final int DEFAULT_TIMEOUT = 10`. **finally** — a block that always executes after try-catch, regardless of whether an exception occurred. I use it for cleanup: `finally { driver.quit(); }` ensures the browser closes even if the test throws an exception. **finalize()** — a method called by the garbage collector before destroying an object. It\'s deprecated since Java 9 and should never be relied upon. I don\'t use it in my automation code.',detailedExplanation:'final: compile-time restriction. finally: runtime guarantee. finalize(): garbage collection hook (deprecated). They share no relationship other than similar spelling.',simpleExplanation:'final = "this can\'t change." finally = "this always runs." finalize = "this runs before garbage collection" (don\'t use it).',realWorldExample:'In automation: final String BASE_URL = "https://app.com"; try { test code } finally { driver.quit(); }',projectExample:'In my framework, config values are final (immutable after loading). The teardown method uses finally to guarantee driver cleanup.',codeCommand:'// final - constant\npublic static final int TIMEOUT = 10;\npublic static final String BASE_URL = "https://app.com";\n\n// final method - can\'t override\npublic final void setup() { /* can\'t be changed by subclass */ }\n\n// final class - can\'t extend\npublic final class Config { /* no subclassing */ }\n\n// finally - always executes\ntry {\n    driver.findElement(By.id("btn")).click();\n} catch (Exception e) {\n    logger.error("Click failed", e);\n} finally {\n    driver.quit(); // ALWAYS runs, even if exception\n}\n\n// finalize() - DEPRECATED, don\'t use\n// @Override protected void finalize() { ... }',expectedOutput:'final enforces immutability, finally ensures cleanup, finalize is deprecated',followUpQ:'Can a finally block prevent an exception from propagating?',followUpA:'A finally block runs whether or not an exception occurs, but it doesn\'t prevent the exception from propagating — the exception still reaches the caller. However, if finally has a return statement, it overrides the exception (bad practice). If finally throws its own exception, it replaces the original exception (also bad practice).',seniorFollowUp:'How do you ensure resource cleanup in modern Java?',seniorAnswer:'In modern Java (7+), I use try-with-resources: `try (InputStream is = new FileInputStream("data.csv")) { ... }` — the resource is automatically closed. For WebDriver, since it doesn\'t implement AutoCloseable directly, I use @AfterMethod with quit(). For file I/O in my framework utilities, I always use try-with-resources.',commonMistake:'Confusing finally with finalize, or using finalize for cleanup logic.',bestPractice:'Use final for constants, finally for guaranteed cleanup, and never use finalize.'},
    {id:'TQ008',category:'Tricky',topic:'API',subtopic:'PUT vs PATCH',question:'What is the difference between PUT and PATCH?',whyAsked:'API knowledge test — frequently misunderstood.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'PUT replaces the entire resource (full update). PATCH updates only specific fields (partial update). PUT is idempotent, PATCH may or may not be.',interviewAnswer:'**PUT** replaces the entire resource. If a user has fields {name, email, phone} and I PUT with only {name, email}, the phone field might be removed or set to null — because PUT means "here\'s the complete new version." **PATCH** updates only the fields I send. If I PATCH with {email: "new@email.com"}, only the email changes — name and phone remain unchanged. In my API testing: PUT tests must verify that unspecified fields handle correctly. PATCH tests verify that only specified fields change. Both must be validated against the database to ensure the correct update behavior.',detailedExplanation:'PUT: full replacement, idempotent (same request = same result), requires complete resource. PATCH: partial update, may not be idempotent, requires only changed fields. Both return 200 OK on success.',simpleExplanation:'PUT = replace the entire document. PATCH = correct a typo in the document without rewriting everything.',realWorldExample:'User profile: PUT sends all 10 fields. PATCH sends only the 1 field that changed. If you PUT with 9 fields, the 10th might be deleted.',projectExample:'In our e-commerce API, updating order status uses PATCH {status: "shipped"} — we don\'t need to resend the entire order. Updating a product uses PUT because we want to ensure all fields are explicitly set.',codeCommand:'// PUT - Full update (replace)\nResponse putRes = given()\n    .body("{\\\"name\\\":\\\"John\\\",\\\"email\\\":\\\"j@x.com\\\",\\\"phone\\\":\\\"123\\\"}") \n    .put("/users/1"); // All fields required\n\n// PATCH - Partial update\nResponse patchRes = given()\n    .body("{\\\"email\\\":\\\"new@x.com\\\"}") \n    .patch("/users/1"); // Only email changes',expectedOutput:'PUT: full resource replaced. PATCH: only specified fields updated.',followUpQ:'What is the difference between 401 and 403?',followUpA:'401 Unauthorized = "I don\'t know who you are" — authentication failed (no/invalid token). 403 Forbidden = "I know who you are, but you\'re not allowed" — authenticated but lacking permissions. 401 can be resolved by providing valid credentials. 403 means you need different permissions, not just credentials.',seniorFollowUp:'How do you test PUT vs PATCH behavior in automation?',seniorAnswer:'My approach: 1) Create a resource with all fields via POST, 2) PUT with a subset of fields — verify omitted fields\' behavior (null, default, error?), 3) PATCH with single field — verify only that field changed, 4) Validate database state after each operation, 5) Test idempotency — send the same PUT/PATCH twice, verify the result is the same, 6) Test conflict scenarios — concurrent PUT/PATCH to the same resource.',commonMistake:'Treating PUT and PATCH as interchangeable.',bestPractice:'Use PUT for full updates, PATCH for partial. Always validate database state after API updates.'},
    {id:'TQ009',category:'Tricky',topic:'Git',subtopic:'merge vs rebase',question:'What is the difference between git merge and git rebase?',whyAsked:'Essential Git knowledge for senior engineers.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'merge creates a merge commit preserving history. rebase replays commits on top of another branch for a linear history. merge is safer, rebase is cleaner.',interviewAnswer:'**git merge** takes the changes from one branch and merges them into another by creating a merge commit. It preserves the complete history including when branches diverged and merged. The commit graph shows a "fork and join" pattern. **git rebase** takes your commits and replays them on top of another branch, creating a linear history as if you worked sequentially. It\'s cleaner but rewrites commit history. My workflow: I use `rebase` for local feature branches before pushing — `git rebase main` makes my commits sit on top of the latest main branch, keeping history clean. I use `merge` for shared branches (never rebase a branch others are working on). Golden rule: never rebase commits that have been pushed to a shared repository.',detailedExplanation:'merge: preserves history, creates merge commit, safe for shared branches. rebase: rewrites history, linear graph, dangerous for shared branches. Interactive rebase (git rebase -i) allows squashing, reordering commits.',simpleExplanation:'merge = "combine two roads into one junction." rebase = "reroute one road to continue from the other\'s end."',realWorldExample:'Feature branch with 5 commits behind main by 3 commits. merge = shows the diverge point. rebase = makes it look like you started your feature after those 3 commits.',projectExample:'In my team, we rebase feature branches locally before creating pull requests for clean history, then merge PRs into main with a merge commit for traceability.',codeCommand:'# Merge - preserves history\ngit checkout main\ngit merge feature-branch   # creates merge commit\n\n# Rebase - linear history\ngit checkout feature-branch\ngit rebase main             # replays feature commits on main\ngit checkout main\ngit merge feature-branch    # fast-forward merge (linear)\n\n# Interactive rebase - clean up commits\ngit rebase -i HEAD~5        # squash/reorder last 5 commits',expectedOutput:'merge: commit graph with merge commit. rebase: linear commit history.',followUpQ:'When should you NOT use rebase?',followUpA:'Never rebase commits that have been pushed to a shared remote branch. Rewriting shared history causes conflicts for every team member who has already pulled those commits. Only rebase local, unpushed commits.',seniorFollowUp:'What is your team\'s Git workflow?',seniorAnswer:'We use Git Flow: main (production), develop (integration), feature branches (feature/*), release branches (release/*), hotfix branches (hotfix/*). Developers create feature branches from develop, rebase locally before PR, and merge via PR with code review. We squash merge feature branches for clean history. All merges to main require PR approval from at least 2 reviewers.',commonMistake:'Rebasing shared branches, causing history conflicts for the team.',bestPractice:'Rebase only local unpushed branches. Merge shared branches. Never force-push to shared branches.'}
  ]
};
