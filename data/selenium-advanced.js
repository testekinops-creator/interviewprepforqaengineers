/* selenium-advanced.js — Alerts, Frames, Windows, Actions, JSExecutor */
var defined_sections = defined_sections || {};
defined_sections['selenium-advanced'] = {
  title: '⚡ Advanced Selenium',
  description: 'Alerts, Frames, Windows, Actions, JavaScriptExecutor, Dropdowns, Screenshots',
  questions: [
    {id:'SA001',category:'Selenium',topic:'Alerts',subtopic:'Alert Handling',question:'How do you handle JavaScript alerts in Selenium?',whyAsked:'Common automation scenario.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'Use driver.switchTo().alert() to switch to the alert, then accept(), dismiss(), getText(), or sendKeys() to interact with it.',interviewAnswer:'Selenium handles three types of JavaScript alerts: **Simple Alert** (OK button only) — `driver.switchTo().alert().accept()`. **Confirm Alert** (OK + Cancel) — `alert.accept()` for OK, `alert.dismiss()` for Cancel. **Prompt Alert** (input + OK/Cancel) — `alert.sendKeys("text"); alert.accept()`. I always wrap alert handling with an Explicit Wait: `wait.until(ExpectedConditions.alertIsPresent())` because alerts may not appear instantly. A common issue is UnhandledAlertException when trying to interact with page elements while an alert is open — always dismiss the alert first. In my framework, I have a utility method `handleAlertIfPresent()` that safely checks for and handles unexpected alerts.',detailedExplanation:'Alert interface methods: accept(), dismiss(), getText(), sendKeys(). Authentication alerts (HTTP Basic Auth) need different handling — pass credentials in URL or use browser-specific approaches.',simpleExplanation:'When a popup says "Are you sure?", you need to tell Selenium to click OK or Cancel — it can\'t interact with the page until the popup is handled.',realWorldExample:'Delete confirmation: "Are you sure you want to delete this record?" — accept() to confirm, dismiss() to cancel.',projectExample:'In our e-commerce checkout, a price change alert appears if the item price changed while in cart. I capture the alert text for logging, then accept to continue.',codeCommand:'// Wait for alert and accept\nWebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(5));\nAlert alert = wait.until(ExpectedConditions.alertIsPresent());\nString alertText = alert.getText();\nSystem.out.println("Alert: " + alertText);\nalert.accept(); // Click OK\n// alert.dismiss(); // Click Cancel\n\n// Prompt alert\nAlert prompt = driver.switchTo().alert();\nprompt.sendKeys("My Input");\nprompt.accept();\n\n// Safe alert handling utility\npublic boolean handleAlertIfPresent(WebDriver driver) {\n    try {\n        Alert alert = driver.switchTo().alert();\n        alert.accept();\n        return true;\n    } catch (NoAlertPresentException e) {\n        return false;\n    }\n}',expectedOutput:'Alert handled, interaction continues with the page',followUpQ:'What if the alert is not a JavaScript alert but a custom modal?',followUpA:'Custom modals (Bootstrap/React modals) are NOT JavaScript alerts — they\'re regular HTML elements. You interact with them using normal findElement and click. The key distinction: if you can inspect it in DevTools, it\'s a custom modal (use findElement). If DevTools becomes unresponsive, it\'s a JavaScript alert (use switchTo().alert()).',seniorFollowUp:'How do you handle authentication popups?',seniorAnswer:'HTTP Basic Auth popups can\'t be handled by switchTo().alert() in modern browsers. Solutions: 1) Pass credentials in URL: driver.get("https://user:pass@example.com"), 2) Use Chrome DevTools Protocol in Selenium 4 to set credentials, 3) Pre-authenticate via API and set cookies, 4) Use browser profile with stored credentials. In my framework, we prefer API-based pre-authentication.',commonMistake:'Trying to use findElement on JavaScript alerts.',bestPractice:'Always use WebDriverWait before switchTo().alert() to handle timing.'},
    {id:'SA002',category:'Selenium',topic:'Frames',subtopic:'Frame Handling',question:'How do you handle iframes in Selenium?',whyAsked:'Common web element pattern — must demonstrate frame switching knowledge.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'Use driver.switchTo().frame(index/name/element) to enter an iframe, interact with elements inside, then driver.switchTo().defaultContent() to return to the main page.',interviewAnswer:'Iframes create a separate DOM context — Selenium can\'t see elements inside an iframe until you switch to it. My approach: 1) **Identify the iframe** — by index, name/id, or WebElement. 2) **Switch to it** — `driver.switchTo().frame("frameName")` or `driver.switchTo().frame(driver.findElement(By.cssSelector("iframe.editor")))`. 3) **Interact** — now findElement works for elements inside the iframe. 4) **Switch back** — `driver.switchTo().defaultContent()` returns to main page, `driver.switchTo().parentFrame()` goes up one level. For nested iframes, I switch frame by frame. I always use Explicit Wait before switching: `wait.until(ExpectedConditions.frameToBeAvailableAndSwitchToIt(By.id("myFrame")))`. Common in my projects: Salesforce uses iframes extensively for embedded components.',detailedExplanation:'Frame switching: frame(int index), frame(String nameOrId), frame(WebElement). Return: defaultContent() (main page), parentFrame() (one level up). Nested: switch to outer first, then inner.',simpleExplanation:'An iframe is like a picture within a picture. You need to "step into" the inner picture to interact with it, then "step out" when done.',realWorldExample:'Rich text editor (CKEditor, TinyMCE) is inside an iframe. To type text, switch to the iframe first.',projectExample:'In Salesforce Lightning, many components are inside iframes. I created a utility method switchToSalesforceFrame() that waits for and switches to the correct iframe based on the component being tested.',codeCommand:'// Switch by name or ID\ndriver.switchTo().frame("editorFrame");\n\n// Switch by index\ndriver.switchTo().frame(0);\n\n// Switch by WebElement\nWebElement iframe = driver.findElement(By.cssSelector("iframe.content"));\ndriver.switchTo().frame(iframe);\n\n// With explicit wait\nwait.until(ExpectedConditions.frameToBeAvailableAndSwitchToIt(By.id("frame1")));\n\n// Interact inside frame\ndriver.findElement(By.id("inputField")).sendKeys("Hello");\n\n// Return to main page\ndriver.switchTo().defaultContent();\n\n// Nested frames\ndriver.switchTo().frame("outer");\ndriver.switchTo().frame("inner");\n// interact...\ndriver.switchTo().defaultContent(); // back to main',expectedOutput:'Successfully interact with elements inside iframes',followUpQ:'How do you handle nested iframes?',followUpA:'For nested iframes (iframe inside iframe), switch to each level sequentially: main → outer frame → inner frame. To return, use defaultContent() to go all the way back to the main page, or parentFrame() to go up one level at a time. I avoid deeply nested iframe scenarios when possible by coordinating with developers.',seniorFollowUp:'How do you handle dynamically loaded iframes?',seniorAnswer:'I use ExpectedConditions.frameToBeAvailableAndSwitchToIt() which waits for the iframe to exist AND successfully switches to it in one step. For iframes loaded via AJAX, I also wait for a known element inside the frame to be present after switching. In my framework, the switchToFrame() utility handles both waiting and switching, with a configurable timeout.',commonMistake:'Forgetting to switch back to defaultContent() after interacting with iframe elements.',bestPractice:'Always switch back to defaultContent() after iframe interaction. Use try-finally to ensure it happens.'},
    {id:'SA003',category:'Selenium',topic:'Windows',subtopic:'Window Handling',question:'How do you handle multiple browser windows and tabs?',whyAsked:'Important for testing scenarios with popups and new tabs.',difficulty:3,importance:'must',interviewType:'Technical',thirtySecAnswer:'Use driver.getWindowHandles() to get all window handles, iterate to find the new window, and driver.switchTo().window(handle) to switch. Use driver.close() for current and switchTo for parent.',interviewAnswer:'When a click opens a new window/tab, I handle it with a systematic approach: 1) **Store parent handle** — `String parentWindow = driver.getWindowHandle()`. 2) **Perform action** that opens new window. 3) **Get all handles** — `Set<String> allWindows = driver.getWindowHandles()`. 4) **Switch to new window** — iterate handles, switch to the one that\'s not the parent. 5) **Perform actions** in new window. 6) **Close and switch back** — `driver.close(); driver.switchTo().window(parentWindow)`. In Selenium 4, there\'s a simpler API: `driver.switchTo().newWindow(WindowType.TAB)` creates a new tab directly. I always use Explicit Wait before getting handles: `wait.until(ExpectedConditions.numberOfWindowsToBe(2))`.',detailedExplanation:'Key methods: getWindowHandle() (current), getWindowHandles() (all), switchTo().window(handle), close() (current window), quit() (all windows). Selenium 4: switchTo().newWindow(WindowType.WINDOW/TAB).',simpleExplanation:'Like having multiple browser tabs open — you need to tell Selenium which tab to look at before you can interact with it.',realWorldExample:'Clicking "Terms and Conditions" link opens a new tab. Switch to it, verify content, close it, switch back to registration form.',projectExample:'In our B2B e-commerce, clicking "View Invoice" opens a PDF in a new tab. I switch to the new tab, verify the PDF URL contains expected parameters, close it, and continue the main flow.',codeCommand:'// Store parent window handle\nString parentWindow = driver.getWindowHandle();\n\n// Click link that opens new window\ndriver.findElement(By.linkText("Open New")).click();\n\n// Wait for new window\nwait.until(ExpectedConditions.numberOfWindowsToBe(2));\n\n// Switch to new window\nfor (String handle : driver.getWindowHandles()) {\n    if (!handle.equals(parentWindow)) {\n        driver.switchTo().window(handle);\n        break;\n    }\n}\n\n// Interact in new window\nSystem.out.println(driver.getTitle());\n\n// Close and switch back\ndriver.close();\ndriver.switchTo().window(parentWindow);\n\n// Selenium 4 — create new tab\ndriver.switchTo().newWindow(WindowType.TAB);\ndriver.get("https://example.com");',expectedOutput:'Successfully switch between windows and interact with each',followUpQ:'How do you handle more than 2 windows?',followUpA:'For multiple windows, I store handles in a List and switch by index or title: `List<String> handles = new ArrayList<>(driver.getWindowHandles())`. I can then switch by position: `driver.switchTo().window(handles.get(2))`. I also switch by matching window title: iterate through handles, switch, check getTitle(), and stay on the match.',seniorFollowUp:'What happens if a window doesn\'t open or opens slowly?',seniorAnswer:'I handle this with: 1) WebDriverWait for numberOfWindowsToBe() — catches both "window didn\'t open" (TimeoutException) and "window opens slowly" scenarios, 2) Retry logic — sometimes popup blockers prevent the window, 3) Logging — capture the window handle count for debugging, 4) Alternative approach — if a popup is blocked, use JavaScriptExecutor to open the URL in the same window.',commonMistake:'Not waiting for the new window to open before trying to switch to it.',bestPractice:'Always use numberOfWindowsToBe() wait and store parent handle before performing actions.'},
    {id:'SA004',category:'Selenium',topic:'Actions',subtopic:'Actions Class',question:'Explain the Actions class in Selenium.',whyAsked:'Tests knowledge of advanced user interactions.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'Actions class performs complex user interactions: mouse hover, right-click, double-click, drag-and-drop, keyboard actions (keyDown, keyUp), and composite action chains.',interviewAnswer:'The Actions class simulates complex user interactions that simple click() and sendKeys() can\'t handle. I use it for: **Mouse hover** — `actions.moveToElement(menuItem).perform()` to trigger dropdown menus. **Right-click** — `actions.contextClick(element).perform()` for context menus. **Double-click** — `actions.doubleClick(element).perform()` for edit modes. **Drag and drop** — `actions.dragAndDrop(source, target).perform()` or `actions.clickAndHold(source).moveToElement(target).release().perform()`. **Keyboard combos** — `actions.keyDown(Keys.CONTROL).click(el1).click(el2).keyUp(Keys.CONTROL).perform()` for multi-select. **Scroll to element** — `actions.scrollToElement(element).perform()` (Selenium 4). Always call `.perform()` at the end to execute the action chain.',detailedExplanation:'Actions is a builder pattern — chain multiple actions and execute with perform(). Key methods: moveToElement, click, doubleClick, contextClick, clickAndHold, release, dragAndDrop, keyDown, keyUp, sendKeys, scrollToElement, pause.',simpleExplanation:'Actions class mimics what a human does with mouse and keyboard — hover, drag, right-click, hold Shift while clicking, etc.',realWorldExample:'Navigation menu: hover over "Products" to reveal dropdown, then click "Electronics". This requires moveToElement before click.',projectExample:'In our e-commerce admin panel, I use drag-and-drop to reorder product images, and right-click context menus to access quick actions.',codeCommand:'Actions actions = new Actions(driver);\n\n// Mouse Hover\nactions.moveToElement(driver.findElement(By.id("menu"))).perform();\n\n// Right-click (Context Menu)\nactions.contextClick(driver.findElement(By.id("item"))).perform();\n\n// Double-click\nactions.doubleClick(driver.findElement(By.id("editCell"))).perform();\n\n// Drag and Drop\nWebElement source = driver.findElement(By.id("drag"));\nWebElement target = driver.findElement(By.id("drop"));\nactions.dragAndDrop(source, target).perform();\n\n// Keyboard: Ctrl+A, Ctrl+C\nactions.keyDown(Keys.CONTROL).sendKeys("a").keyUp(Keys.CONTROL).perform();\n\n// Composite: hover then click\nactions.moveToElement(menu).pause(Duration.ofMillis(500))\n       .moveToElement(subItem).click().perform();',expectedOutput:'Complex user interactions executed successfully',followUpQ:'Why might drag and drop not work in some cases?',followUpA:'HTML5 drag-and-drop often doesn\'t work with Selenium\'s native dragAndDrop() because it uses a different event model. Solutions: 1) Use JavaScript-based drag-and-drop helper script, 2) Use clickAndHold().moveToElement().release() chain instead, 3) Use Actions with intermediate offset moves. In my framework, I have both native and JavaScript drag-and-drop utilities.',seniorFollowUp:'How do you handle tooltips and hover menus that disappear quickly?',seniorAnswer:'For disappearing hover elements: 1) Use moveToElement() and immediately interact while hover is active, 2) Chain all actions in one sequence without separate perform() calls, 3) Use pause() in the action chain to give time for hover effects: actions.moveToElement(trigger).pause(500).moveToElement(dropdown).click().perform(), 4) If hover is unreliable, use JavaScriptExecutor to dispatch mouseover event directly.',commonMistake:'Forgetting to call .perform() — the action chain is built but never executed.',bestPractice:'Always chain related actions into a single sequence and call perform() once.'},
    {id:'SA005',category:'Selenium',topic:'JSExecutor',subtopic:'JavaScriptExecutor',question:'When and how do you use JavaScriptExecutor?',whyAsked:'Advanced technique for handling tricky scenarios.',difficulty:3,importance:'important',interviewType:'Technical',thirtySecAnswer:'JavaScriptExecutor runs JavaScript in the browser context — used for clicking hidden elements, scrolling, getting element attributes, and handling scenarios Selenium can\'t natively handle.',interviewAnswer:'JavaScriptExecutor is my escape hatch for scenarios Selenium can\'t handle natively. Common uses: 1) **Click hidden elements** — when ElementNotInteractableException occurs: `js.executeScript("arguments[0].click()", element)`. 2) **Scroll** — `js.executeScript("arguments[0].scrollIntoView(true)", element)` or `js.executeScript("window.scrollTo(0, document.body.scrollHeight)")`. 3) **Get attributes** — `js.executeScript("return arguments[0].getAttribute(\'value\')", element)`. 4) **Set values** — `js.executeScript("arguments[0].value=\'text\'", element)` for read-only fields. 5) **Page load status** — `js.executeScript("return document.readyState")`. 6) **Highlight elements** for debugging — `js.executeScript("arguments[0].style.border=\'3px solid red\'", element)`. I use it sparingly because JS clicks don\'t trigger all browser events that real clicks do.',detailedExplanation:'JavaScriptExecutor interface: executeScript(script, args...) for sync, executeAsyncScript(script, args...) for async. "arguments[0]" refers to the first argument passed. Return values can be String, Long, Boolean, WebElement, List.',simpleExplanation:'JavaScriptExecutor is like having a remote control for the browser — you can do things directly through the browser\'s engine.',realWorldExample:'A cookie banner covers the submit button. Instead of closing the banner, JS click bypasses the overlap: js.executeScript("arguments[0].click()", submitBtn).',projectExample:'In our Salesforce automation, some custom components intercept clicks. I use JavaScriptExecutor as a fallback click mechanism when standard click() throws ElementClickInterceptedException.',codeCommand:'JavascriptExecutor js = (JavascriptExecutor) driver;\n\n// Click via JS (bypasses interactability checks)\njs.executeScript("arguments[0].click();", element);\n\n// Scroll to element\njs.executeScript("arguments[0].scrollIntoView({behavior:\'smooth\',block:\'center\'});", element);\n\n// Scroll to bottom\njs.executeScript("window.scrollTo(0, document.body.scrollHeight)");\n\n// Get inner text\nString text = (String) js.executeScript("return arguments[0].innerText", element);\n\n// Set value (for readonly fields)\njs.executeScript("arguments[0].value = \'test@email.com\';", inputField);\n\n// Check page load\nString state = (String) js.executeScript("return document.readyState");\n\n// Highlight element (debugging)\njs.executeScript("arguments[0].style.border=\'3px solid red\';", element);',expectedOutput:'JavaScript executes in browser context and returns results',followUpQ:'What are the risks of using JavaScriptExecutor?',followUpA:'Risks: 1) JS clicks don\'t trigger all events (blur, focus, mousedown) that real user clicks do — some event listeners may not fire, 2) It bypasses visibility checks — you might click elements a real user can\'t see, 3) It makes tests less representative of real user behavior, 4) Hard to debug when JS throws errors. I use it only as a last resort, not as a primary interaction method.',seniorFollowUp:'How do you use JavaScriptExecutor for performance optimization?',seniorAnswer:'For performance: 1) Batch operations — execute multiple JS statements in one call instead of multiple findElement calls, 2) Check multiple element states in one script: return {visible: el.offsetParent !== null, text: el.innerText}, 3) Pre-check page state before Selenium interactions, 4) Use it for custom waits: wait until a specific JS variable changes. But always balance performance with test reliability.',commonMistake:'Using JavaScriptExecutor as the default click method instead of standard click().',bestPractice:'Use standard Selenium methods first. Use JavaScriptExecutor only when Selenium can\'t handle the interaction natively.'}
  ]
};
