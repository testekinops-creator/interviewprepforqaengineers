/* ═══════════════════════════════════════════════════════════════
   playwright-commands.js — Playwright Commands
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['pw-commands'] = {
  title: '📋 Command Cheat Sheet',
  description: 'Quick reference for common Playwright commands, assertions, and configurations',
  questions: [
    {id:'PWC001',category:'Playwright',topic:'Commands',subtopic:'Locators & Actions',question:'What are the most common locators and actions in Playwright?',whyAsked:'Assesses familiarity with the Playwright API.',difficulty:2,importance:'must',interviewType:'Technical',thirtySecAnswer:'Playwright uses the `page.locator()` API. Common actions include `click()`, `fill()`, `check()`, and `selectOption()`. Built-in locators like `getByRole`, `getByText`, and `getByTestId` are preferred over CSS/XPath.',interviewAnswer:'In Playwright, we primarily use the Locator API which is strict and auto-waits. \n\n**Best Practice Locators**: \n- `page.getByRole(\'button\', { name: \'Submit\' })` (Most preferred for accessibility)\n- `page.getByText(\'Welcome\')`\n- `page.getByTestId(\'login-input\')` (Requires `data-testid` attribute)\n\n**Common Actions**: \n- `await locator.click()` (Clicks element)\n- `await locator.fill(\'text\')` (Clears and enters text)\n- `await locator.check()` (Checks a checkbox/radio)\n- `await locator.selectOption(\'value\')` (Selects from dropdown)\n- `await locator.hover()` (Mouse hover)\n- `await page.goto(url)` (Navigation)\n\nPlaywright discourages direct CSS/XPath in favor of user-facing locators (getByRole, getByText) because they make tests more resilient to DOM changes.',detailedExplanation:'Locators are strictly evaluated; if a locator resolves to more than one element and you try to click it, Playwright throws a StrictMode violation error. Use `.first()`, `.nth()`, or refine the locator.',simpleExplanation:'Locators find things. Actions do things to them. Playwright wants you to find things the way a user would (by what they say or do), not by hidden code (like CSS classes).',realWorldExample:'To log in: get the textbox named "Email", fill it. Get the button named "Submit", click it.',projectExample:'We refactored our framework to use `getByTestId` for all dynamic elements and `getByRole` for standard buttons, reducing maintenance overhead by 40% when frontend frameworks changed.',codeCommand:'// Locators and Actions\nawait page.getByTestId(\'username\').fill(\'admin\');\nawait page.getByPlaceholder(\'Password\').fill(\'secret\');\nawait page.getByRole(\'button\', { name: \'Log In\' }).click();\n\n// Dropdown\nawait page.getByLabel(\'Country\').selectOption(\'US\');\n\n// Checkbox\nawait page.getByLabel(\'I agree\').check();\n\n// Hover\nawait page.getByText(\'Menu\').hover();',expectedOutput:'Interactions succeed with auto-waiting applied.',followUpQ:'How do you handle elements inside an iframe?',followUpA:'Playwright makes iframe handling very easy using `.frameLocator()`. You don\'t need to switch context back and forth like in Selenium. Example: `await page.frameLocator(\'#my-iframe\').getByRole(\'button\', { name: \'Submit\' }).click();`. It handles the wait for the iframe to load automatically.',commonMistake:'Using `.type()` instead of `.fill()`. `.type()` simulates individual keystrokes (slower, used for testing keyboard events). `.fill()` is for entering text quickly and reliably.',bestPractice:'Always use `getByRole`, `getByText`, or `getByTestId`. Avoid CSS/XPath unless absolutely necessary.'}
  ]
};
