/* ═══════════════════════════════════════════════════════════════
   playwright-coding.js — Playwright Coding Snippets
   ═══════════════════════════════════════════════════════════════ */
var defined_sections = defined_sections || {};

defined_sections['pw-coding'] = {
  title: '💻 Playwright Coding',
  description: 'Practical coding snippets and solutions in Playwright',
  questions: [
    {id:'PWCod001',category:'Playwright',topic:'Coding',subtopic:'Network Interception',question:'Write a Playwright script to intercept and mock an API response.',whyAsked:'Network interception is a killer feature of Playwright. Tests ability to write advanced tests.',difficulty:4,importance:'must',interviewType:'Coding',thirtySecAnswer:'Use `page.route()`. Provide the URL pattern, and in the handler, call `route.fulfill()` with a mocked JSON response body, status code, and headers.',interviewAnswer:'Playwright allows intercepting and modifying network traffic effortlessly. To mock an API response, I use `page.route(url, handler)`. \n\nInside the handler, I use `route.fulfill()` to provide a fake response. This is incredibly useful for testing edge cases in the UI (like a 500 Server Error) without actually breaking the backend, or for testing frontend logic before the API is ready.\n\nCode structure: `await page.route(\'**/api/users\', route => { route.fulfill({ status: 200, contentType: \'application/json\', body: JSON.stringify({ name: \'Mock User\' }) }); });`. After setting this up, any request the page makes to that URL will return my mock data instantly.',detailedExplanation:'You can also modify requests before they go out using `route.continue({ headers: ... })`, or abort them using `route.abort()` to simulate network failures (e.g., blocking images to speed up tests).',simpleExplanation:'It\'s like intercepting the mailman. When the website asks for data, Playwright intercepts the request, hands the website a fake letter (mock data) you wrote, and the website never knows the difference.',realWorldExample:'Testing a "No Results Found" UI state. Instead of trying to find a search term that yields no results in the real DB, mock the search API to return an empty array `[]`.',projectExample:'I created a suite of offline/error state tests by mocking critical APIs to return 500 status codes, ensuring our frontend error boundaries and toast notifications worked perfectly.',codeCommand:'// Mocking an API Response\nconst { test, expect } = require(\'@playwright/test\');\n\ntest(\'Mock user profile API\', async ({ page }) => {\n  // 1. Intercept the network request\n  await page.route(\'**/api/profile\', async route => {\n    // 2. Fulfill with mock data\n    await route.fulfill({\n      status: 200,\n      contentType: \'application/json\',\n      body: JSON.stringify({\n        username: \'FakeAdmin\',\n        role: \'SuperUser\'\n      })\n    });\n  });\n\n  // 3. Navigate or trigger the request\n  await page.goto(\'https://myapp.com/profile\');\n\n  // 4. Assert UI reflects the mocked data\n  await expect(page.getByText(\'FakeAdmin\')).toBeVisible();\n});',expectedOutput:'The UI displays the mocked data instead of real backend data.',followUpQ:'How would you block all images from loading to speed up a test?',followUpA:'I would use `page.route()` to intercept requests for images and abort them. `await page.route(\'**/*.{png,jpg,jpeg,gif}\', route => route.abort());`. This prevents the browser from downloading images, saving bandwidth and significantly reducing page load time during test execution.',commonMistake:'Putting the `page.route` command AFTER `page.goto`. The route must be set up *before* the request is made.',bestPractice:'Set up routing/mocking before navigation or action. Use wildcards `**/*` carefully to avoid intercepting unintended requests.'}
  ]
};
